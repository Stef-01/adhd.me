node_modules/
.next/
out/
*.tsbuildinfo
next-env.d.ts
.env*
!.env.example
.data/
test-results/
playwright-report/
# test-regenerated binary artifact (zip timestamps make it non-deterministic)
reports/weekly-w8.docx
reports/load-w48.md
# test-regenerated binary artifacts (zip timestamps make them non-deterministic — W20 precedent)
reports/adhd-me-one-pager.docx
reports/adhd-me-sales-deck.pptx

.vercel

# macOS
.DS_Store

# O143: e2e runs write their captures here. The unit-named qa/ directories are the
# design record and a test run may not mutate them — see src/quality/qa-record.test.ts.
qa/_runs/
.data-visual/
.pnpm-store/
