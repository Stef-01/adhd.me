// O188 (founder-directed): joining is an email now. The address is written once, here, and read
// by the page and its e2e pin — Next.js page files may not export constants of their own.
//
// The founder's instruction read "info.adhdme.au", which is not a deliverable address (no @);
// implemented as the instruction's own domain with the @ restored. One-line change if that
// reading is wrong.
export const JOIN_EMAIL = "info@adhdme.au";
