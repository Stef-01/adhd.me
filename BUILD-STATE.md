# Meherr BUILD-STATE — claim ledger + protocol

> Machine-maintained by the careyield-build-loop. One row per week-unit. This file IS the lock.

## Claim protocol (every loop session follows this exactly)

1. `git pull --rebase` the working branch (see **Home resolution** below) so this file is current.
2. Pick ONE unit, in this priority order:
   a. an `in-progress` unit whose continuation notes you can finish;
   b. a `claimed`/`in-progress` unit that the **staleness rule** below says you may reclaim;
   c. the lowest-numbered `available` unit whose dependencies are `done` (units marked `[P]` in the plan are claimable out of order).
3. Claim it: set status `claimed`, your session id (short), and UTC timestamp in this table. **Commit and push this claim edit immediately, before building.**
3b. **Validate your own timestamp before you write it.** A claim stamped in the FUTURE is never
   reclaimable — W54's rule refuses to steal live claims on clock skew — so a typo (observed
   2026-08-10: W151 stamped `2026-08-11T13:20Z`) plus a dying holder equals a row locked forever.
   That one self-closed; the next might not. Stamp the real UTC time, and if you find a
   future-dated claim whose holder is demonstrably FAILED, correct the timestamp to its real
   claim time first and then apply the normal rule — repairing a dead row, never stealing a live one.
4. If the push is rejected (another session claimed simultaneously): `git pull --rebase`, pick the *next* eligible unit, repeat. Never fight over a row.
5. Build the unit to its verify gate. Commit work incrementally (green only). **If a unit runs
   long, push something at least every 90 minutes** — a green WIP commit, or a heartbeat edit to
   your own row. That push is what tells other sessions you are alive; without it your row is
   reclaimable after 90 minutes (see the staleness rule).
6. Finish: set status `done` + commit SHA, or `in-progress` + concrete continuation notes (what's left, where, how to verify), or `blocked` + reason (e.g. founder gate). Push with rebase-retry (up to 4 attempts).
7. Parallelisation is expected: overlapping sessions hold different rows. Never edit another session's non-stale claimed row.

## Staleness — when you may reclaim someone else's row (W54)

A flat window cannot tell a slow holder from a dead one. On 2026-08-09 builder-A hit a model
limit mid-claim on W51; because the only rule was "6 hours", its dead row idled *both* routines
for over two hours and two firings found nothing to do. The rule is now **evidence-based**:

| Holder has… | Window | Measured from |
|---|---|---|
| pushed nothing since claiming | **90 minutes** | the claim time |
| pushed a commit / heartbeat | **6 hours** | its last push |
| visibly failed (model limit, dead session) | **none — reclaim now** | — |

Progress, not elapsed time, is the liveness signal: a holder that has pushed something has proven
it is running and keeps the long leash; one that has pushed nothing has proven nothing. Both are
checkable from `git log` alone, so no session needs to ping another.

Two deliberate refusals: a claimed row whose timestamp is **missing or unreadable** is left alone
and flagged (stealing a live claim is worse than an idle row), and a claim timestamped in the
future is never reclaimed on clock skew.

The normative definition is `classifyClaim` in `src/loop/claims.ts`, simulated against this
incident's real timestamps in `src/loop/claims.test.ts`. Change the rule there and here together.

## Fleet — builders run on different models (W54)

W51 §Process: a single-model fleet is a single point of failure. When builder-A exhausted its
model limit, it did not fail loudly — it held a claim and stopped, which is the worst failure
mode for a ledger-locked loop.

- **Run the builders on different models.** One provider limit, one model deprecation or one
  capacity incident must not be able to stop every builder at once.
- **Record the model in the session id** where it helps (`builder-A`, `builder-B`, …) and name
  the model in the session log entry, so a post-mortem can see which model stalled.
- **A builder that hits its limit is `knownDead`** — any other session may reclaim its row
  immediately under the staleness rule; there is nothing to wait for.
- **Routines outlive sessions.** A routine firing into a dead session is wasted, so a fleet of
  two on one model is worth less than a fleet of two on two models.

## Home

This repo — `stef-01/stef-01-careyield-`, branch `main` — is the permanent home (W-MIGRATE
completed 2026-08-08; the tree previously lived in Stefan-Brain `careyield/`, now a pointer).
Session logs still go to Stefan-Brain `wiki/_log/` (non-fatal if unavailable).


> **⌛ LEDGER DRY 2026-08-09 14:10Z — EXPECTED, NOT A FAULT.** Zero units are claimable: the whole
> Q5 chain (W57→W58→W59→W61→W63→W64→W65, and W62) sits transitively behind **W56, which is blocked
> on a founder G5-scope ruling**. W60 was the last parallel unit and is done. Idle firings should do
> the protocol fallback (re-verify the last done unit's gate, end quietly) — do **not** unblock W56,
> invent units, or pull Y3 work forward to manufacture activity. Note for the plan owner: the W52
> Y2 expansion chained Q5 far more tightly than Y1 was chained, which is why one blocked unit stops
> everything; future expansions should keep more `[P]` units genuinely independent.


> **D1 (design, out-of-band) — DONE 2026-08-10.** Finder opening screen: the example prompt and the
> archetype label were one idea rendered as two orphans with a ~250px dead band between them and the
> bottom-pinned CTA (every viewport, caused by `margin:auto` on `.voice-prompt` claiming all free
> space). Merged the example into the switcher, grouped prompt+switcher as `.voice-core`, dropped the
> "Find your fit" eyebrow. **Tried and reverted** a two-column desktop layout: the finder lives in a
> fixed ~520px shell shared by every stage, so a viewport-keyed media query split that 520px and
> overflowed the headline — rationale left in globals.css; a real desktop composition needs the shell
> widened for all stages and deserves its own unit. Gate: 58 files / 501 tests, audit gate PASS.

## Ledger — Year 1

| Unit | Status | Session | Claimed (UTC) | SHA | Notes |
|---|---|---|---|---|---|
| W-MIGRATE | done | interactive-0808 | 2026-08-08T07:25Z | (initial commit) | tree migrated from Stefan-Brain PR #5 @ 5efe413; CI workflow activated at .github/workflows/ci.yml |
| W1 | done | interactive-0808 | 2026-08-08T07:05Z | 90a044f | CI staged in ci/github-workflow.yml (workflow-push perms absent in vault repo; activates at W-MIGRATE) |
| W2 | done | interactive-0808 | 2026-08-08T07:12Z | 9c2c2f4 | verify adapted (no live postgres): migration + TS registry consistency-tested; live apply deferred to Supabase wiring |
| W3 | done | interactive-0808 | 2026-08-08T07:20Z | 9f53943 | — |
| W4 | done | interactive-0808 | 2026-08-08T07:28Z | 316055b | — |
| W5 | done | interactive-0808 | 2026-08-08T07:36Z | 193737a | — |
| W6 | done | interactive-0808 | 2026-08-08T07:12Z | a1d4f1e | — |
| W7 | done | builder-B | 2026-08-08T08:05Z | 2cf4107 | Playwright e2e added (pnpm e2e; not in CI — runner lacks pre-provisioned browser); mock rail behind /book/[token] |
| W8 | done | builder-A | 2026-08-08T08:54Z | 548c25f | audit kind constraint extended in migration 0002 |
| W9 | done | builder-A | 2026-08-08T17:46Z | de3da79 | definitions doc at docs/ATTRIBUTION.md (v1, doc+code versioned together) |
| W10 | done | builder-A | 2026-08-08T18:16Z | 6e8a365 | spine at src/spine; replay refuses corrupt logs; rail transitions captured by diff |
| W11 | done | builder-B | 2026-08-08T18:40Z | 807ae78 | mock auth (Supabase deferred — no prod credentials gate); design QA via manual checklist at docs/DESIGN-QA.md (impeccable/taste skills unavailable in loop env) |
| W12 | done | builder-A | 2026-08-08T19:16Z | 34c66e5 | report artifact committed at reports/sim-26w.md; sim test regenerates it each run |
| W13 | done | builder-B | 2026-08-08T19:40Z | 9353793 | Q1 security review dossier at docs/SECURITY-REVIEW-Q1.md; 3 findings (1 High server-action authz, 2 Medium) all fixed + regression-tested; code-review sweep clean. Zero criticals |
| W14 | done | builder-A | 2026-08-08T20:16Z | 2717256 | e2e runs in loop env via PW_CHROMIUM_PATH=/opt/pw-browsers/chromium (6/6 green — updates W7 note) |
| W15 | done | builder-B | 2026-08-08T20:40Z | a1f900b | one-tap audit at /console/usefulness; mock store (synthetic); outcome_records/OutcomeRecord from W2 reused; tally feeds later dashboard/guardrails |
| W16 | done | builder-A | 2026-08-08T21:15Z | 1dafcac | monitors pure over metrics; complaint log typed here, full workflow arrives at W43; W19/W20 surface alerts |
| W17 | done | builder-B | 2026-08-08T21:40Z | 14f90c4 | config at src/session/config.ts, honored in sim (gate met); appointmentType added to domain+SQL+generator (deterministic, no RNG shift); e2e now single-worker (global mock stores). Console editing surface deferred (needs clinician roster) |
| W18 | done | builder-A | 2026-08-08T22:16Z | 7e4d39e | authorize() at src/tenancy is the app-layer decision point; RLS policies in migration 0003; manage_members UI + invite flow deferred to W41 onboarding wizard |
| W19 | done | builder-B | 2026-08-08T22:40Z | 680fe91 | ops switches at src/ops honored in sim (kill/pause → 0 sends); /console/ops queue + toggles gated by pause_sending (W18); global kill-switch uses practice pause_sending grant in synthetic phase (Meherr-superadmin role deferred) |
| W20 | done | builder-A | 2026-08-08T23:16Z | e083257 | golden at reports/weekly-w8.md (+docx artifact); revenue = incremental × configurable $/visit, naive shown as contrast only; MBS-accurate rebates arrive at W34 |
| W21 | done | builder-B | 2026-08-08T23:40Z | 65e42cb | model at src/economics/roi.ts (gate: matches brief figures — net $45,029, 4.8x at defaults); living xlsx at reports/roi-calculator.xlsx (formulas + fullCalcOnLoad; LibreOffice recalc unavailable in sandbox → Excel recalcs on open); widget at /console/roi; pricing assumption finalises at W47 |
| W22 | done | builder-A | 2026-08-09T00:17Z | a73278d | /demo presenter page + docs/DEMO.md script; launch action idempotently reseeds all mock stores; e2e drives the full walkthrough |
| W23 | done | builder-B | 2026-08-09T00:40Z | e3c01fb | landing at app/page.tsx; copy as data (src/compliance/landing-copy.ts) lint-gated by src/compliance/landing.ts (B2B twin of W6 linter); design QA in docs/DESIGN-QA.md (taste-skill unavailable in loop env) |
| W24 | done | builder-A | 2026-08-09T01:16Z | 12fec5a | continuity at src/engine/continuity.ts; UPC independent of nominated GP by design; dashboard surfacing joins W42 practice-facing v2 |
| W25 | done | builder-B | 2026-08-09T01:40Z | 7e6bc81 | telehealth message variant in W6 templates (linted); booking page video-appt copy; mock rail ?scenario=telehealth (default seed unchanged); config = W17 fillable "telehealth" type; also fixed pre-existing dashboard.spec race |
| W26 | done | builder-A | 2026-08-09T02:16Z | af5db12 | dossier at docs/HARDENING-Q2.md: 10 findings, 2 criticals fixed (session-config booking bypass; onboard privilege escalation), zero criticals remain; finding #10 (W15 worthwhile+no-action semantics) filed for founder |
| W27 | done | builder-B | 2026-08-09T02:40Z | dca1644 | PmsReadAdapter + reusable contract suite (src/pms/contract.ts); SyntheticPmsAdapter passes it (gate); read-only/credential-free (G1); real BP/Halo adapters reuse the contract at W28. Also fixed 2 cold-cache perf-flakes (W14 unit timeout, dashboard e2e nav timeout) |
| W28 | done | builder-B | 2026-08-09T03:00Z | c4d617b | VendorPmsAdapter (src/pms/vendors.ts) maps BP + Halo raw shapes to W27 contract; both pass contract on recorded fixtures (gate); G1 enforced in constructor (live hosts refused); live HTTP client + field reconciliation = W38/W39 follow-up |
| W29 | done | builder-B | 2026-08-09T03:20Z | da1e0cc | buildBookingLink (src/booking/deeplink.ts): internal/hotdoc/healthengine behind per-practice flags, falls back to internal link with recorded reason; https-only + no-patient-identifier guard enforced in code; link-format tests are the gate. Vendor URL shapes configurable, reconciled at W39; not yet wired into sim (same posture as W28 skeletons — wiring lands at W38) |
| W30 | done | builder-A | 2026-08-09T03:16Z | 322ee34 | fast path behind lateCancellationRate (default 0 — goldens stable); latency budget max<100ms p50<25ms met in sim; PMS cancellation feed wiring joins W32/W36 |
| W31 | done | builder-A | 2026-08-09T02:36Z | e43eea5 | G3 enforced in constructor (twilio.com refused); integration tests use in-process fake API; real Twilio sandbox creds + live wiring = founder gate follow-up (W39 dossier) |
| W32 | done | builder-A | 2026-08-09T03:22Z | d9e6640 | ingest at src/pms/ingest.ts; STOP one-way door upheld at ingestion boundary; consent conflicts flagged not resolved; SQL persistence of identities/provenance joins Supabase wiring |
| W33 | done | builder-A | 2026-08-09T03:38Z | 14209e9 | delete keeps hashed record + suppression (re-ingest can never re-contact); ADM page at /privacy/automated-decisions; policy DRAFT pending counsel; W37 security pass covers the new surface |
| W34 | done | interactive-0809 | 2026-08-09T02:42Z | 0da1194 | revenue-estimation only; verified 1-Jul-2025 rebates pinned in tests |
| W35 | done | interactive-0809 | 2026-08-09T03:15Z | c3d8cfe | composition only — no metric recomputed; synthetic usefulness records stand in until G4 |
| W36 | done | builder-B | 2026-08-09T03:50Z | e4e79e5 | ResilientPmsReader at src/pms/resilience.ts: failure isolation (throw/hang/malformed → degrade, never raise), stale-data guard fails CLOSED past freshness budget, circuit breaker, readFleet isolation. 12 chaos tests are the gate; clock injected. Wiring into the send path lands with W38 |
| W37 | done | builder-A | 2026-08-09T03:51Z | 4dbaf79 | dossier at docs/SECURITY-REVIEW-Q3.md: 1 critical (demo gate) + 2 highs (session expiry, rate limits) fixed; zero criticals; prod deploy checklist items flow to W39. Review conducted manually — skill's diff entry point had no diff (loop commits green to main) |
| W38 | done | builder-B | 2026-08-09T04:05Z | 7993f5b | drift detection at src/pms/drift.ts (per-vendor declared shape; additive change is not drift, removal/retype/null/unknown-enum/missing-collection are) + error taxonomy at src/integration/errors.ts unifying W36/W29/W28/drift failures with severity + blocksSending + operator action. Suite green (gate): 8 drift + 11 taxonomy tests |
| W39 | done | builder-A | 2026-08-09T04:06Z | bd8375b | GATE DOSSIER AT docs/GATE-DOSSIER-Q3.md — FOUNDER ACTION REQUIRED on G1/G2/G3 (credentials + exact activation steps enumerated; all gates remain CLOSED and code-enforced). Grounded in 6-agent code survey + 61-claim adversarial verify; survey also found+fixed a W33 authz gap (export render now takes the stewardship grant) |
| W40 | done | interactive-0809 | 2026-08-09T03:39Z | 7589c38 | docs unit; success criteria pre-registered incl. holdout non-negotiable + stop rules |
| W41 | done | builder-B | 2026-08-09T04:25Z | 596fdce | 5-step wizard at /console/setup/[step]; lands the W17 deferral (clinician roster + persisted SessionConfig + validateSessionConfig) and setupCompletedAt. /console/onboarding left intact (6 redirect sites + 3 specs drive it) — collapsing it into a redirect to the wizard is a clean follow-up, filed for W49/W50. Adversarial review found + fixed 2 defects in my own code: reflected error string in console chrome, and completion attesting to never-reviewed defaults. Timed e2e asserts structure (≤5 steps, real persisted config) + wall-clock ceiling — REVIEW FIXES @ 16382bf: stable clinician ids (positional ids re-pointed the offering allowlist at a different clinician) + allowlist now fails CLOSED instead of widening to "all" (my own test had blessed the fail-open); both regression-tested |
| W42 | done | builder-B | 2026-08-09T04:45Z | f355ca6 | practice-facing /console/results; v1 dashboard untouched (its spec pins jargon verbatim + is DOM-coupled) — consolidating the two views is a W49/W51 follow-up. Reuses W14 chart via optional label/scale props (defaults reproduce v1 byte-identically); palette re-validated both modes per dataviz skill. Copy as data, linted by the W23 compliance linter + a jargon ban asserted on the rendered page. Gate: comprehension checklist in docs/DESIGN-QA.md, each question asserted in e2e. NOTE: a concurrent session wiped this unit's uncommitted files mid-build — shared working dir, commit early |
| W43 | done | builder-A | 2026-08-09T04:35Z | 234a535 | workflow at src/complaints + /console/complaints; opt-out terminal at intake (rail offers close immediately); open complaints banner console home + feed W16 monitor; intake open to all staff, triage/resolve behind pause_sending grant |
| W44 | done | interactive-0809 | 2026-08-09T03:52Z | dfae8bf | FINDING: sim defaults show generated-DNA worse than organic (~ratio>1.25) — calibration owner should review the generated-booking DNA model (W3/W12); real answer comes from pilot holdout |
| W45 | done | builder-A | 2026-08-09T04:56Z | 64bbb7c | golden at reports/case-study-w45.md; de-identification + W23 copy-lint + no-holdout-refusal enforced in code; synthetic marker until G4 |
| W46 | done | builder-B | 2026-08-09T05:20Z | 4336455 | figures register (src/collateral/figures.ts) is the gate: every asset number declared with a checkable source, anti-drift test vs live computation, copy may only cite {{id}} placeholders (bare numbers banned by test). Market sizing / benchmarks / competitor claims NOT shipped — listed in NEEDS_FOUNDER_VERIFICATION and in the register appendix as explicitly not claimed. Assets generated by test (W20 precedent): reports/careyield-{sales-deck.pptx,one-pager.docx,figure-register.md}. pptx validate PASSED; content QA caught 2 real defects (stat card rendering "A"; article error) — fixed + invariant asserted. Pixel visual QA unavailable in sandbox (LibreOffice cannot load the file, no pdftoppm) → geometry bounds test stands in |
| W47 | done | interactive-0809 | 2026-08-09T04:08Z | 2880cbf | agreement is a skeleton for a lawyer, not a contract; pricing encodes the no-fee-splitting posture |
| W48 | done | builder-A | 2026-08-09T05:16Z | 708a6be | 100×(1.5k pat, 4w) fleet: 12.4s total, p95 week 37ms (budget 150), backfill p95 1ms (budget 50), sends 0.74× priced envelope; W26's indexed-rail idea measured unnecessary at this scale; report regenerated per run (gitignored) |
| W49 | done | builder-A | 2026-08-09T05:22Z | bb879e2 | axe (WCAG 2.1 A/AA) e2e over all surfaces, zero violations enforced; contrast sweep stone-400→500 (14 files); manual checklist at docs/A11Y-W49.md; post-G2 re-run required |
| W50 | done | interactive-0809 | 2026-08-09T05:18Z | 23635ae | 8 surfaces mapped, zero unmapped in app/; update-in-same-commit rule established |
| W51 | done | interactive-0809 | 2026-08-09T07:50Z | 1038ab4 | audit at docs/AUDIT-W51.md — 2 findings both FIXED (A1 fire-and-forget send could log unsent-as-sent at G3, guarded fail-closed; A2 pnpm audit never run: 5/7 transitive advisories fixed via overrides, 2 image-size DoS accepted with rationale), 8 controls confirmed clean. Process: add pnpm audit to the gate + builders are a single-model SPOF (see report §Process). **FOLLOW-UP (builder-A, 2026-08-09T12:15Z, second audit at docs/AUDIT-Y1.md):** the reclaimed session's own audit had already started and finished afterwards; it went wider (6 dimensions, every serious finding handed to a verifier prompted to refute it — 44 raised, 9 confirmed, 3 refuted, 1 downgraded) and found non-overlapping defects, incl. 2 CRITICAL: patient erasure + APP-12 export both missed the complaints store (raw patientId survived "delete everywhere" while the console reported no data held), and complaint opt-out reported "no further contact" for any identifier supplied while being neither honest about what it closed nor durable across re-ingest. Both fixed, plus 4 highs (results page evaluated guardrails against a hardcoded EMPTY complaints list so the zero-tolerance monitor could never fire there; submitUsefulness never checked record_usefulness and recordOutcome takes no caller identity; complaint intake/list open to any session though intake mutates the rail; pilot case study + report labelled the north star "per 1,000 eligible" while the denominator is the whole invite arm, ~4.6x on the committed golden that becomes the G4 template). Engine and ATTRIBUTION.md were correct throughout — only the labels lied, conservatively. The clinician positional-id high was already fixed on main by the W41 review pass; that fix is better, so this one was dropped. 32 medium/low left unfixed and listed in the report — the largest is compliance-lint COVERAGE (booking page, console copy, public privacy pages, demo, weekly report all unlinted), which wants its own unit. LESSON: two audits of the same tree found disjoint defect sets; "audited" is not a property a tree has once |
| W52 | done | interactive-0809 | 2026-08-09T08:05Z | 7c037e8 | YEAR 1 COMPLETE. Y2 Q5–Q8 expanded to §5b + 52 rows; gates inherited (W69 blocked on G5); W51 process findings became W53/W54 |

> Y2–Y5 rows are appended by the expansion rule (plan §6) at W52/W104/W156/W208 or when the backlog runs dry.

> **Year 2 — Condition Yield** (expanded by W52, 2026-08-09; definitions in docs/FIVE-YEAR-PLAN.md §5b).

| W53 | done | builder-B | 2026-08-09T08:47Z | 0be0a57 | `pnpm audit` in the gate + CI via `pnpm verify` (typecheck·test·build·audit:gate). Accepted risks live in src/security/audit-allowlist.ts, where the AllowlistEntry TYPE makes reason+reviewBy mandatory — the compiler enforces "rationale + review date", and past reviewBy the acceptance stops working. Evaluator is a pure function over the audit JSON so the unit's verification is 13 unit tests, not a manual observation; also confirmed end-to-end through the CLI (drop an acceptance → exit 1, restore → exit 0). Fail-closed on unknown severity, malformed advisory, bad date, and unreadable report (no network ⇒ FAIL, not skip). Stale entries warn but never block. Runner is scripts/audit-gate.mts under node 22 type stripping, so it is typechecked like the tree (needed tsconfig allowImportingTsExtensions). Gate doc updated in all 3 sites per W50. NOTE: the 2 image-size acceptances expire 2026-11-09 — the gate will start failing then, by design |
| W54 | done | builder-B | 2026-08-09T09:47Z | 7f08cf3 | staleness is now EVIDENCE-based, not clock-based: pushed nothing since claiming → 90m from the claim; pushed a commit/heartbeat → 6h from that push; visibly failed (model limit) → reclaim now. Progress is the liveness signal and is readable from git log, so no session pings another; protocol §5 now requires a push every 90m on a long unit. Fail-safe both ways: unreadable/missing claim time is left alone and flagged, future-dated claims are never reclaimed on clock skew (stealing a live claim is worse than an idle row). Normative rule is classifyClaim in src/loop/claims.ts; BUILD-STATE points at it so prose and rule cannot drift. Mixed-model fleet documented (single-model fleet = SPOF; a limit-exhausted builder fails SILENTLY holding a lock). Verify: simulation replays the W51 incident on real timestamps — old rule still held the row at the manual reclaim, new rule frees it 45m earlier, and both of my idle firings decide correctly. NOTE: the routine prompt text still says "stale claim older than 6h" — founder may want to align it with this row |
| W55 | done | builder-B | 2026-08-09T10:47Z | 6aeffd1 | migration 0004_registers.sql + TS twin in src/domain/types.ts. Provenance is STRUCTURAL: all four source columns NOT NULL (+ https-only, retrieved >= published), and IntervalProvenance mirrors it, so an unsourced interval is unrepresentable on both sides. Register membership `source` admits only pms_condition_flag / practice_confirmed — no inferred/predicted/symptom value exists, so the G7 boundary holds in STORAGE before W57's engine. New concept: REFERENCE_TABLES (conditions, guideline_intervals) are national guidance, NOT practice-scoped — readable by any staff identity, writable by none; first sanctioned exception to the 0003 membership rule, declared explicitly and pinned on both halves. That exposed a weak W18 assertion (checked only that SOME policy existed, which a read-all reference policy satisfies vacuously) — now checks the membership predicate itself. G5: ships the container and ZERO clinical content; a test asserts no inserts into either guideline table. Verify: 10 consistency assertions, each mutation-tested (6 deliberate mutations each fail exactly one test). NOTE for W56: it populates guideline_intervals with real clinical intervals + citations — whoever takes it should settle whether that needs G5 clinical sign-off before shipping values |
| W56 | in-progress | builder-A | 2026-08-10T06:05Z | container @ eb9c27d | **CONTAINER SHIPPED — VALUES STILL BLOCKED ON THE G5 RULING. This row is NOT a decision.** src/registers/intervals.ts + 16 tests. Built the half that is safe under BOTH rulings, so the chain stops idling without pre-empting Stefan: a catalogue containing zero intervals ships zero clinical content, so it cannot breach G5. Both halves of the unit's stated gate are enforced by the LOADER, not by the values — "traceable to a cited source" is a refusal rule (incomplete provenance, non-https url, retrievedOn before publishedOn, bad cadence, duplicate id are each rejected WITH a reason, never silently dropped), and "no interval hardcoded in logic" holds because consumers read through lookupInterval() and every one must already handle null — which with an empty catalogue is the only path, and is exactly the path an unsigned-off condition takes in production. A test asserts SHIPPED_INTERVALS is empty, so values cannot arrive without the ruling. Test fixtures cite a placeholder source and a fictional condition code (W60 posture) so this file cannot become a back door for unreviewed clinical content. **REMAINING:** add the real intervals (diabetes cycle of care, KHA-CARI CKD by stage, GPCCMP cadence) to SHIPPED_INTERVALS with citations, and flip the emptiness test — a DATA change, no code change. W57 can proceed against this now (membership derives from condition flags, not intervals); W58 needs values or fixtures. Verify: pnpm verify green, 501 tests / 58 files. Original escalation preserved: **FOUNDER DECISION (G5 scope) — raised by W55's builder, escalated rather than guessed.** W56 ships REAL clinical intervals (diabetes cycle of care, KHA-CARI CKD monitoring by stage, GPCCMP cadence) that drive care-gap detection. G5 covers "clinical pathway content sign-off". Arguable either way: these are published national guidance transcribed as cited data, not authored pathways — but a wrong or stale interval silently changes who gets contacted about their health. Blocking is reversible; shipping unreviewed clinical values is not. Stefan decides: (a) NOT G5 — transcription of cited public guidance, build it; or (b) G5 — build the loader + tests with zero values, values land at sign-off (the W55 precedent). Unblock by editing this row. W55's container is already shipped, so nothing downstream is idle-blocked except W57+. |
| W57 | done | builder-A | 2026-08-10T06:20Z | 76b94f1 | src/registers/membership.ts + 16 tests. Membership has exactly TWO origins — a PMS condition flag or an explicit practice confirmation — and the G7/TGA boundary is held by ABSENCE: there is no code path that infers, so "inferred from symptoms" is not something this module can be asked to do. Non-inference is tested as a property, not an example: a patient carrying every symptom-shaped signal a naive implementation might key off (chronicCare + activeRecall + frequent recent attendance + upcoming booking) and NO condition flag yields zero members, and a test asserts the emitted source set is exactly the two sanctioned values, so a third origin fails the suite. chronicCare is deliberately NOT a register — the coarse ongoing-care marker and a condition flag are different facts. PMS flag beats practice confirmation for the same pair (the clinical record is the stronger source) and a confirmation can ADD but never REMOVE a flagged patient, so unticking a box in Meherr cannot silently contradict the PMS. Reconciliation keeps history: a persisting membership keeps its ORIGINAL addedAt (not reset each ingest), a vanished basis CLOSES the row with removedAt rather than deleting it (a deleted row is indistinguishable from one that never existed), and a rejoin opens a fresh row beside the closed one. explain() names the missing input for every refusal, so "why is she on this list?" and "why isn't he?" both have answers. Needs no interval values, so W56's G5 block does not reach it. Verify: pnpm verify green, 517 tests / 59 files |
| W58 | done | builder-A | 2026-08-10T06:35Z | 24a3a0d | src/registers/caregap.ts + 16 tests. The "not a clinical recommendation" boundary is TYPED, not documented: CareGap carries notAClinicalRecommendation with the literal type `true`, so the disclaimer travels with the value (survives serialisation, in scope for anything that destructures a gap) and a construction site that claimed otherwise would not compile. A gap is arithmetic about a calendar — member + cited interval + last relevant visit — and says nothing about need, urgency or what happens at a visit. Ordering is recency (never-visited first, then longest-overdue, then stable by id), explicitly NOT clinical priority: it says who has waited longest, never who is sickest. Refuses to manufacture a gap in five ways, each pinned: no signed-off interval, closed membership, future-dated visit (data error), unparseable date, and inside cadence. Cadence boundary is inclusive at exactly N whole months and partial months do not count; month-end arithmetic tested (31 Jan → 28 Feb is not a month). Fixtures per condition per the gate: two registers, 12- and 3-month, plus a named-cadence case where the same date is a gap against one interval and not the other. Fixtures cite a placeholder source (G5), which costs nothing — every cadence is read from the catalogue, so fixture values and signed-off values travel an identical path. A test pins that the SHIPPED (empty) catalogue yields zero gaps, which is the correct production posture while W56's values are blocked. Verify: pnpm verify green, 533 tests / 60 files |
| W59 | done | builder-A | 2026-08-10T06:55Z | 348395b | src/registers/eligibility.ts + 11 tests. "Can only narrow, never widen" is STRUCTURAL, not conventional: withCareGapFilter takes the W4 decision as input and has no branch that produces `eligible: true`, so no gap, register or future edit to this file can widen the W4 rules — widening requires changing W4 itself, where the consent/safety exclusions live and are tested. Property test is EXHAUSTIVE, not sampled: all 11 W4 exclusion reasons x {gap, no gap} x {filter on, off} all stay ineligible, AND the original reason survives (the audit trail keeps the most serious cause instead of it being overwritten by no_care_gap). A compile-time Record<ExclusionReason,true> means adding a 12th reason to W4 breaks this test until it is covered, so the property cannot silently stop being exhaustive. Headline case pinned explicitly: an opted-out patient WITH a gap is still refused, and still for opting out — a scheduling observation never overrules consent. Off by default (opt-in narrowing, not a new default). narrowToCareGaps fails CLOSED: an empty gap set narrows to nothing, never falls back to the whole panel, and a gap for a patient outside the base set cannot add them. Verify: pnpm verify green, 559 tests / 61 files |
| W60 | done | builder-B | 2026-08-09T11:48Z | 5d72a95 | /console/registers + src/registers/store.ts. Isolation is held by the DATA SHAPE (enable/disable keyed by practice id), so one practice cannot change what another sees; not observable from the browser (console signs in as one practice) so it is unit-tested directly with two practices. G5: W56 was blocked ~1h before this claim, so the catalogue ships PLACEHOLDERS asserting nothing clinical (neutral names; provenance literally says "not clinical guidance"), and a test asserts the shipped catalogue names no real condition/guideline — shipping realistic registers here would have pre-empted an escalated founder decision by the back door. Gap counts are CARRIED by the store, not computed: detection is W58, and this store is the seam W58 becomes the real source for. W13 authz inside the server action (reuses edit_rules, no new grant); W41 error-keys-not-messages; W50 dossier row added (9 surfaces); joined the W49 axe sweep. Verify: 4 e2e + isolation unit tests. NOTE: my first copy test passed VACUOUSLY against the onboarding redirect — it now asserts the page identity before scanning. CHECKED 12:50Z: this does NOT generalise — results.spec Q4 already guards with a visible-heading assertion, and the privacy/setup absence-checks are element-scoped, so no other spec needs the guard. Loose end closed; do not re-investigate |
| W61 | done | builder-A | 2026-08-10T07:10Z | 6c45bef | src/registers/ranking.ts + 12 tests. Ranking decides who is contacted FIRST; eligibility decides who is contacted AT ALL — and the signature enforces the split: rankGapAware takes an already-eligible list and returns a PERMUTATION of it, so it cannot add or drop a patient and a gap can never become a reason to contact someone the rules exclude (W59's asymmetry, held for ordering). Implemented by PARTITIONING and applying W5's rankCandidates to each side, deliberately not by adding a comparator term: W5 stays the single definition of the base order, so if W5's rule changes this inherits it instead of drifting. Consequence pinned by test: with no gaps the output is byte-identical to W5's, so a practice with registers off sees ZERO behaviour change; when everyone has a gap it collapses back to W5's order too. W5 properties carried over and all still hold: permutation, never adds (incl. a gap for a patient outside the set), never drops (ranking is not a filter), deterministic, no input mutation, empty panel. Verify: pnpm verify green, 571 tests / 62 files |
| W62 | done | builder-B | 2026-08-10T05:02Z | 5fdf005 | src/registers/provenance.ts + provenance UI on /console/registers. W55 made provenance impossible to STORE unsourced and W56 impossible to LOAD unsourced; this closes the RENDER hole — intervals reach the page only as ViewInterval, which cannot be constructed without citation(>=8)+https url+ISO review date, so an unsourced interval row is not expressible. Rows with unusable provenance are WITHHELD and counted on-page, never rendered bare. Presenter re-checks the loader bar because views can be handed data that skipped the loader (mock store, fixture, future API). Shows citation as a link + "last reviewed <date> (<relative age>)", age from an injected clock so the page self-agrees. Deliberately NO staleness policy — what counts as out-of-date guidance is a clinical call, not this unit's. Verify both ways: 17 unit tests (6 malformed provenance shapes each refused) + e2e counting provenance lines against interval rows with a non-vacuous guard; mutation-tested (removing the line fails the spec) |
| W63 | done | builder-A | 2026-08-10T07:40Z | 5cc52ed | src/registers/{sim-registers,compare}.ts + 10 tests + reports/registers-w63.md. Same seed run twice changing ONLY the register flag, so every difference is the register layer and not a different random world; both arms keep their holdout so each is measured per ATTRIBUTION.md and the two are never compared raw. Integration is surgical: buildInvitationPool gained an OPTIONAL rank param defaulting to W5's rankCandidates, and the register layer draws NO RNG (flags derive from patient id + seed, not the sim stream), so registers-off is byte-identical to the pre-register sim — pinned by a test comparing undefined vs explicitly-disabled configs, and by all 611 tests incl. every existing golden staying green. Chain-level property tested: the on-arm's recipients are a SUBSET of the off-arm's (W59/W61 restated end-to-end), and no invited patient is holdout or never-attended. RESULT: narrowing cut sending 2474 -> 412 (17%) and the on-arm incremental went NEGATIVE (-33.8). That is honest and expected, not a defect: the synthetic world models NO relationship between having a gap and responding, so no benefit CAN appear, and the narrowed arm is too small for its holdout comparison to resolve. The report says all of that in a 'What this does NOT show' section plus a sample-size warning, and three tests assert those caveats are present — an artefact that could be misread as 'registers harm patients' is a compliance problem, not just a wording one. Intervals are FIXTURES citing a placeholder source (G5). Verify: pnpm verify green, 611 tests / 65 files |
| W64 | done | builder-B | 2026-08-10T05:19Z | 2119a9f | src/registers/analytics.ts. Closure rate alone is a VANITY number (most gaps close anyway), so both arms are reported and only the DIFFERENCE is treated as meaningful — withheld on no-holdout or thin arms, reusing W72's WithheldReason vocabulary rather than inventing a second one. Test pins that both-arms-close-everything gives difference 0 however good the invite rate looks. Stated limitation: appointments carry no condition, so closuresFromAppointments emits UNSCOPED closures (a visit closes all that patient's gaps) and ambiguousClosures counts how many closures rested on that assumption; scoped closures (PMS reason-for-visit, practice-confirmed) always win and remove the ambiguity. Verify: golden fixture asserted as ONE object so arithmetic changes show as a golden diff rather than a still-passing test; fixture rates round on purpose (diabetes 60 vs 30, CKD 50 vs 25) |
| W65 | done | builder-A | 2026-08-10T08:20Z | c90cdb8 | docs/HARDENING-Q5.md. Ten-angle code-review fan-out over src/registers/, two severest findings verified by runnable probe rather than by reading; 15 survived dedup, 11 fixed, 4 recorded. ZERO CRITICALS OUTSTANDING. Mattered more than usual because I wrote most of Q5 — a self-review would have been worthless, and the independent review found the worst defect in MY code: the W63 report printed "invariants held in both arms" without ever calling checkInvariants(), guarded by a tautological test. Also fixed: reconcileMemberships gave a patient MULTIPLE LIVE memberships after repeated close/rejoin (verified by running it); CareGap dropped practiceId so one tenant's gaps could narrow/reorder another's pool (+ recalls and analytics both ignored practiceId entirely — 3 separate tenancy leaks in one chain); lookupInterval picked by ROW ORDER so a data-only reorder silently flipped gap counts; describeAge rendered "reviewed in the future" for 10h every AEST day and was non-monotonic (719d = 23 months, 720d = 1 year); gapCountsByCondition silently lost a __proto__ condition code; dead guard-bypassing export deleted. ambiguousClosures was ambiguous in two ways at once so BOTH figures are now named explicitly rather than me reversing builder-B's intent on a coin flip. Verify: pnpm verify green, 637 tests / 67 files |
| W66 | done | builder-A | 2026-08-10T09:00Z | b8982d0 | src/messaging/condition-lint.ts + 22 tests. KEY DECISION: there is deliberately NO per-condition message BODY. The register decides WHO is messaged, never WHAT they are told — a patient invited off the diabetes register and one invited off spare capacity get byte-identical text (asserted). An SMS naming a register is an unsolicited health disclosure to a device that may be shared or read on a lock screen. The leak check is RELATIONAL, not a keyword list: given the condition that targeted the message, does the text disclose it by code, display name, or any significant word of either — so a register a practice invents next month is covered without editing the linter (tested with an invented perinatal_mental_health register). Stopword list keeps generic words (care/health/review) from firing, because a linter that cries wolf trains people to route around it. ALSO bans targeting TELLS that reveal selection without naming a condition (we noticed / our records show / because you / based on your / recall framing) — naming the condition is not the only way to disclose it. W6's rules are APPLIED, not re-implemented, so a condition-targeted send can never be held to a weaker standard than an untargeted one (pinned by test). Verify: every seeded leak blocked, W6 rules still enforced; pnpm verify green, 689 tests / 71 files |
| W67 | done | builder-A | 2026-08-10T09:30Z | 4606516 | src/messaging/approval.ts + 17 tests. Approval is keyed by CONTENT HASH, not template name or version counter — editing the text silently revokes approval, because a version number can be forgotten to bump and a hash cannot. Even a whitespace-only change needs re-approval: "approximately the approved text" is not a standard anyone can audit. assertSendable THROWS rather than returning a boolean, so a caller who forgets to read the result still cannot send (a boolean nobody reads is not a gate). Per-practice: one practice's approval grants nothing to another, and withdrawal at one does not reach the other. Refusals name WHICH of the four ways it failed and explainRefusal() renders each as something a practice manager can act on — a bare refusal is what makes people work around a gate. Own-bug caught by my own test while writing it: evaluateSendable used .find() on hash alone, so an old withdrawal MASKED a later re-approval of the same text and refused a send the practice had explicitly authorised; now looks for a live approval first. Verify: unapproved template cannot be sent, fail-closed; pnpm verify green, 706 tests / 72 files |
| W68 | done | builder-A | 2026-08-10T10:00Z | 6496c83 | src/registers/safety-rails.ts + 15 tests. MECHANISM ONLY — SHIPPED_SAFETY_RULES is EMPTY and a test pins it: deciding what counts as a red flag for a condition IS clinical authorship, i.e. the same G5 gate W56's intervals sit behind, so this follows the W55/W56/W60 pattern rather than guessing. Two design points: (1) exclusion ROUTES, it does not drop — withheld patients come back in routedToUsualGp with the flag and rationale that excluded them, because a patient silently never contacted is the exact failure a safety rail exists to prevent, and a rail that produced it would be worse than no rail; a test asserts invitable+routed == input so nothing can vanish. (2) rails run AHEAD of ranking, and the tests prove exclusion is a property of the SET not of position — a rail running after ranking would let batch size decide who was protected. NOT symptom inference: a flag is a fact already on the record (PMS or practice-set), read never derived, so the G7 boundary holds exactly as it does in W57. Rules load with a mandatory rationale >=10 chars — a rail nobody can explain is a rail nobody can review. Tenancy scoped (one practice's flag cannot route another's patient). Verify: pnpm verify green, 721 tests / 73 files |
| W69 | done | builder-A | 2026-08-10T10:30Z | bb6897a | src/registers/authoring.ts + 17 tests. NOTE: this row was marked `blocked`, but its own text said "workspace may be built, content stays unusable until sign-off" — I had read it as fully blocked and reported a harder blocker than existed. The workspace is built; ZERO content is signed off and SHIPPED_WORKSPACE is empty (pinned). "Unusable" is enforced by the TYPE SYSTEM, not a runtime check: ApprovedContent is branded with a `unique symbol` so only usableContent() can produce one and `as` is the sole bypass (greppable in review). Three stages, not two, because a specialist and the founder answer DIFFERENT questions — "is this clinically correct?" vs "do we accept the liability of shipping it?" — and collapsing them loses the distinction G5 exists for. Gate asserted at EVERY stage incl. the dangerous one: REVIEWED content looks finished and is still unusable. Author cannot review own content (self-review is the paper trail of a check that never happened). Sign-off requires content unchanged since review, and content tampered with AFTER sign-off stops being usable. Any amend resets to draft and clears BOTH attestations, else the founder's approval silently transfers to text they never saw (W67's content-hash reasoning, applied to clinical content). Verify: unapproved content is unusable; pnpm verify green, 751 tests / 75 files |
| W70 | done | builder-B | 2026-08-10T05:55Z | 9774c65 | src/guardrails/condition-monitors.ts. W16 watches the practice AS A WHOLE, which is exactly how a bad register hides: a register at 8% opt-out inside a 1% practice-wide aggregate stays invisible until everyone on it has been messaged — pinned by a test. Threshold logic NOT reimplemented: metrics go through W16 evaluateGuardrails so bands/severities/wording stay identical (test asserts byte-identical alerts via both paths). Small registers get insufficient_data instead of a 25%-from-4-invitations alarm, but COMPLAINTS are exempt from that suppression (an event, not a rate). TWO COUNTING PROPERTIES found by running against the real sim rather than fixtures: (1) opt-outs must be counted as DISTINCT PATIENTS — a STOP closes every invitation that patient holds, so counting statuses charges one decision to every register they are on; my first cut had this wrong and the sim caught it (43 statuses vs 27 real opt-outs); (2) invitation-derived attribution UNDERCOUNTS — a STOP after invitations resolved leaves nothing to attribute, so per-register totals never EXCEED practice-wide and W16 stays the authority on the total. Sim carries one register today (W63); W75 multi-register needs no change here since monitors are keyed by condition |
| W71 | done | builder-B | 2026-08-10T05:10Z | 43bf85e | src/registers/recalls.ts + src/synthetic/recalls.ts. Dedup vs the practice's OWN recalls: same-condition open recall suppresses the gap; UNSCOPED open recall suppresses ALL that patient's gaps (fail-safe — a recall we cannot attribute might be about this gap, and guessing otherwise is what produces the duplicate contact); CLOSED recalls never suppress (else one completed cycle silences a register forever); legacy coarse patient.activeRecall still honoured. Suppression is REPORTED with a reason, never silent — ops must be able to tell a practice why a register with members produced no offers. Synthetic recalls in their OWN module with their OWN seed per the W17 lesson (drawing from the practice generator stream shifts every later draw); determinism pinned by test. Verify: 14 tests over 400 patients x 2 registers, incl. a fixture-integrity test that the generated spread actually contains open/unscoped/closed rows (a dedup test proves nothing against data with nothing to dedup), and both directions asserted — nothing survives that an open recall covers, nothing suppressed that none covers |
| W72 | done | builder-B | 2026-08-10T05:15Z | a65fe48 | src/registers/attribution.ts. Per-condition incrementality by REUSING countAttribution with the panel narrowed to a register's live members — v1 law stays in one place, no restatement. Two properties the practice-wide figure lacks, both made explicit: (1) COHORTS OVERLAP — a dual-register patient counts in both, so cohort figures must NEVER be summed to a practice total (that is the W9 number); overlapCount reported + test pins the double-count; (2) COHORTS ARE SMALL — below MIN_ARM_PATIENTS=30 in BOTH arms the counts still report but the CLAIM is withheld, same fail-closed posture as W9's no-holdout rule; threshold is an explicit option, not a hidden constant. `claim` is null-or-figures rather than number+flag so a caller cannot headline a withheld cohort by reading past a boolean. explainWithheld gives plain-words reasons and never invents a figure. Verify: 10 hand-built fixtures (60/40 arms, 30/10 attending → 500 vs 250 per 1000, incremental 15) so the arithmetic is visible in the test |
| W73 | done | builder-B | 2026-08-10T06:08Z | 535ef7c | src/registers/escalation.ts. W68 decides who must not be SENT to; this decides what happens once something HAS happened (free-text reply, complaint, repeated generated DNA, opt-out with a message). Rule: an escalation routes A PERSON TO THEIR USUAL GP and states A FACT — never what might be wrong, how urgent, or what to do. The record has NOWHERE to put advice (no severity/cause/action field) and a test asserts the key list, because the cheapest way for advice to appear later is someone adding a field. Observations linted with W6's clinical/urgency/diagnosis vocabulary — staff text gets that scrutiny because an escalation note is read under time pressure and is the most tempting place to editorialise; list kept SEPARATE from templates.ts because the two readers will diverge and a shared list would be loosened for one and silently loosen the other. FAIL-CLOSED on wording: a trigger failing its own linter is DROPPED and the drop is reported. Shipped set is deliberately NOT empty (unlike W68) — routing a human conversation to a human is not clinical authorship, so no G5 needed. Verify: 21 tests; "no trigger emits clinical advice" asserted twice — over the definitions AND as a property over everything escalate() returns |
| W74 | done | builder-B | 2026-08-10T05:23Z | 19aec13 | src/messaging/preferences.ts + capture on /book/[token] + /api/mock/preferences. NOT W17 (which governs which SLOTS to offer) — this governs when a PATIENT agreed to hear from the practice. Three rules, each because the alternative IS the harm: never substitute a channel (unsupported = refusal, not fallback); never send outside stated hours (DEFER, do not deliver early or silently drop); a deferral that outlives the offer becomes SILENCE (a text about a session already gone is worse than none). Default is weekday business hours, never "any time" — no stated preference is not consent to be messaged at 5am. Every refusal names a reason. Capture offered AFTER booking, never as a gate in front of it. Unauthenticated action writes only against the invitation its own signed token names + rate-limited like confirmBooking; invalid window refused WITHOUT clobbering the previous good answer; error KEYS only. Verify: 18 unit tests + 5 e2e (stored, survives reload, HONOURED — opt-out yields send:false/no_channel_chosen not a quieter schedule — invalid refused non-destructively, copy clinical-claim-free). Dossier row added. Taken out of order under [P] with W66 not done; condition-specific preferences, if any, land with W66. ALSO fixed a /practices contrast regression from a concurrent design change (#697255 = 4.45:1 on tint) |
| W75 | done | builder-B | 2026-08-10T06:13Z | bdacc6b | src/registers/multi-sim.test.ts + additionalConditions on RegisterSimConfig. W63 proved ONE register works; this asks what happens when a patient is on SEVERAL — where double-contact, double-counting and one-person-trips-every-alarm live. Config extension is OPTIONAL so W63 runs stay byte-identical; each register gets its own seed offset, hash-based and RNG-free, so adding registers never shifts the sim stream (W17 lesson). Invariants: fleet checkInvariants() + the two that only matter multi-register (holdout never invited despite 3 chances to be picked; no patient offered the same session twice) + adding a register never shrinks the pool nor disturbs the primary register's gaps. REAL FINDING: my first cut asserted the healthy practice trips no register alarm — it DOES. Slicing by register surfaces one above the 2% opt-out threshold while practice-wide stays clear, which is exactly what W70 exists to reveal; asserting silence would have encoded the blind spot. Test now asserts the MECHANISM (every critical alert must actually exceed its threshold; a rate-alerted register must have had volume). Report at reports/registers-w75.md, cannot claim invariants held without them holding |
| W76 | done | builder-B | 2026-08-10T05:36Z | 5db9e20 | src/report/registers-section.ts. Composes W64 closure + W72 per-condition attribution into the weekly report. Rendered as a SEPARATE OPTIONAL section, never interleaved, so a register-less practice gets byte-identical v1 output and the W20 golden stays valid (that golden test IS the assertion — it calls the renderer with no register arg and still matches the committed file). Carries two honesty rules all the way to the page: a withheld figure prints as a REASON, never a bare dash and never the withheld arithmetic (test asserts the withheld cohort's numbers appear nowhere in the output); and cohorts OVERLAP so rows do not sum to the practice total — said in the report, not just the code, because a manager with a calculator will try. Ambiguous closures disclosed the same way. Both notes omitted when there is nothing to disclose so they stay meaningful rather than becoming skimmed boilerplate. Verify: golden section asserted as one string, fixture carries one claimable + one withheld register so both paths are in the golden |
| W77 | done | builder-A | 2026-08-10T11:05Z | 74710d1 | docs/COMPLIANCE-DOSSIER.md. ZERO UNMAPPED: swept every route in app/ against the surface map, not just the ones I added. Added the 4 Q6 controls (W66/W67/W68/W69) and 3 routes nobody had mapped — app/finder and app/console/interest from founder commit 3317340, plus app/demo. Resolved the 3 rows previously marked UNMAPPED rather than leaving placeholders: each now states which controls apply TODAY and which are MISSING. That distinction is the point of the document — a surface with no controls, described accurately, is mapped; a surface nobody has assessed is not. Replaced the stale app/page.tsx row (W23's B2B guarantee describes /practices now, not /). FLAGGED for the founder: app/console/interest is the first surface that would hold contact details of people who are NOT patients of a subscribing practice, which CHANGES the privacy analysis (APP 5 collection notice, retention, erasure) rather than extending it — synthetic-only today so the exposure is theoretical. Verify: zero unmapped; pnpm verify green, 751 tests / 75 files |
| W78 | done | builder-B | 2026-08-10T06:22Z | 78b7b9c | docs/HARDENING-Q6.md. ZERO CRITICALS. Q6-1 MEDIUM fixed: W74 contact preferences keyed by patient id ALONE — two practices with a colliding id shared one record, and because preferences govern WHEN a patient may be contacted, a collision could impose one practice's patient's quiet hours on another's. Now practiceId::patientId, practice taken from the invitation itself so a signed token can only write inside its own practice; collision regression-tested both directions. THIRD instance of one class in my code (W65 found it in W71 recalls + W64 closures) — which is why the sweep hunted the CLASS rather than reading freely. Q6-2 LOW hardened: conditionMetricsFrom had the same shape; took the optional practiceId filter W65 established rather than waiting for a fourth. Dossier states plainly that 7 of 12 Q6 units are the reviewer's own and self-review is a real weakness, with the two mitigations used. Recorded no-change: W74 preferences are CAPTURED but not honoured in a live send path (send path unwired, same gap as W28/W29/W36) — must not be mistaken for an active control |
| W79 | done | builder-B | 2026-08-10T06:30Z | 4c09059 | migration 0005_capability.sql + TS twin. Brief §5 law (never conflate interest/experience/competence) held STRUCTURALLY: THREE tables, not one with a type column, each pinning a single-valued source CHECK, and the three vocabularies DISJOINT — so promoting a self-report into experience means inserting 'clinician_self_reported' into a column whose CHECK permits only 'derived_case_mix' and the DB refuses; TS twin pins the same literals so it also fails to typecheck. Two asymmetries: EXPERIENCE HAS NO PROSE (only text columns are condition_code + source; any free-text field would be a self-report wearing a derived label) and COMPETENCE CANNOT BE SELF-ASSERTED (attested_by + verified_on NOT NULL — no path to "verified" without recording a verifier). Counts cannot float free of their window; interest is a bounded 1-5 preference, not an unbounded score that reads like a capability rating. Verify: 8 consistency assertions, each MUTATION-TESTED (conflate provenance / add prose to experience / anonymous verification / unbound interest / seed data each fail exactly one test). Ships no data — populated by W80 (derived) and W81 (stated) |
| W80 | done | builder-B | 2026-08-10T06:34Z | 9bf378f | src/capability/experience.ts. Only writer of experience. Gate ("no self-reported data promoted") enforced THREE ways not trusted: (1) the INPUTS cannot carry a self-report — CaseMixInput takes appointments/memberships/outcomes and has no parameter a stated claim could arrive through, asserted by a test on the input key list, so the guarantee is an ABSENCE not a rule; (2) output pinned to the literal W79 CHECK admits, so any other provenance is unconstructible in SQL and in TS; (3) attendance is the only qualifying event (booked/DNA/cancelled/open all excluded). W15 usefulness carried ALONGSIDE the count, never folded in — a visit judged unreasonable still counts as experience because they still saw it; discounting would make a number that is neither a count nor a judgement, the blend W79 exists to prevent. Dual-register visits count toward BOTH (same overlap W72 reports) so cells do not sum to the clinician total and nothing claims they do. Unmeasured pairs return null, never a zero row. Verify: 20 fixtures |
| W81 | done | builder-B | 2026-08-10T06:40Z | ce3eab6 | src/capability/{interest,store}.ts + /console/case-mix. Only self-reported input to the capability graph, so the work is keeping it in its lane: (1) acting clinician resolved from the SESSION never the form (a posted clinicianId cannot make you someone else) + saveInterest refuses when subject != actor; (2) scale bounded 1-5 with appetite-only labels and NO expert rung — unit test bans ability words from labels, e2e bans them from the rendered page. Identity is EXACT email match only: no fuzzy name match, no "only one clinician so it must be them" — both would occasionally attribute a preference to the wrong person, the single failure the lookup exists to avoid; unlinked refuses and renders no form. ClinicianRecord gains optional email (null default, W41 wizard unaffected). NAMING: /console/case-mix not /console/interest — the latter already exists and means community demand signups. Clearing records ABSENCE not zero (matches W80 null-not-zero). Verify: 5 e2e on the real surface + 16 unit tests; joined the W49 axe sweep |
| W82 | done | builder-B | 2026-08-10T06:52Z | 44f879e | src/capability/threshold.ts. Brief §5 law and the load-bearing safety property of the capability graph — the one place W79's three fields meet. Ordering is LEXICOGRAPHIC not additive: candidates partitioned by clearing the floor, interest consulted ONLY inside the cleared group, no weight/bonus, so no amount of enthusiasm closes the gap. Interest below the floor is NOT CARRIED on the ranked record, so no downstream consumer can reintroduce it. Unmeasured experience = zero not sufficient (else a clinician with no telemetry clears every floor). Expired credential is a DISTINCT failure from absent (different actions). Below-floor candidates RETURNED not dropped — W86 must explain non-selection and a missing candidate cannot be explained. Floor value is a practice setting; we fix the SHAPE of the rule, never the threshold. Verify: property test over 128 candidate shapes (8 experience x 4 credential x 4 interest) x 4 floors + the adversarial pair constructed not hoped for; MUTATION-TESTED — additive scoring fails 8 assertions |
| W83 | done | builder-A | 2026-08-10T13:10Z | 14772f3 | src/capability/graph.ts + app/console/capability + 9 unit + 4 e2e. The two views are DIFFERENT QUESTIONS, not one query rendered twice: ownProfile answers "what does the system believe about me?" and shows the DERIVED case-mix counts in full — a record a clinician cannot inspect is one they cannot correct. panelView answers "who can take this work?" and is deliberately COARSER: presence + freshness per field, never raw visit tallies, because surfacing them turns a capability graph into a PRODUCTIVITY BOARD — a different product, not one a clinician agreed to when stating an interest. That coarseness is asserted STRUCTURALLY (the serialised panel must not contain the count, the field names or the credential), not by trusting a template. Isolation unit-tested both ways incl. that ownProfile with a foreign clinician id returns nothing. Copy discipline: e2e asserts the page never says expert/best/specialist/rating — provenance, not praise. NOTE: module is capability/graph.ts, NOT store.ts — builder-B's W81 took capability/store.ts for the interest store in the same hour; collision resolved in favour of theirs, both kept. Verify: e2e + isolation |
| W84 | done | builder-B | 2026-08-10T06:59Z | a731d66 | src/capability/routing.ts. IN-PANEL is the whole constraint — never routes outside the practice (cross-practice is G6). Default is NOBODY IS ROUTED: usual GP unless the practice opted in FOR THAT REGISTER, because continuity is what we are measured on (W24) and redirecting to a marginally better match trades a measured good for an unmeasured one. Key rule: if the usual GP CLEARS THE FLOOR there is no case for moving the patient however good the alternative — routing covers gaps, it does not optimise; pinned with the most adversarial fixture (usual GP barely over the line vs 999 visits + credential + max interest). Fallback is always toward the STATUS QUO (nobody else clears → stay put; a worse match than today is not an improvement). Interest still orders only inside the cleared group (W82), so a keen below-floor clinician is never the target. Every decision carries its REASON + the ranked field it decided from — refuses any choice it cannot explain (W86 renders these); reason copy asserted to name continuity/threshold and make no clinical claim. Verify: 12 routing fixtures |
| W85 | done | builder-B | 2026-08-10T07:03Z | 91005c4 | src/capability/continuity-guard.ts. W84 only moves BELOW-FLOOR patients, which sounds self-limiting but is not in AGGREGATE: one under-experienced GP + a register most of the panel is on can redirect a large share of that GP's patients, and the W24 index falls without anyone deciding it should. Second limit checked on the aggregate, asking TWO different questions that must both pass: would the projected usual-GP share fall below the practice floor, and would routing move more of the panel than agreed even if the share holds. Projection is EXACT arithmetic not an estimate (a routed patient still attends, so the denominator is unchanged). On breach routing STOPS WHOLESALE rather than trimming — a partial plan chosen by iteration order is not defensible to a practice; held decisions keep their original reason so the practice sees what routing would have done. Verify: sim comparison against a REAL 26-week continuity baseline, not a hand-made number — modest plan passes, aggressive plan refused with continuity preserved exactly |
| W86 | done | builder-B | 2026-08-10T07:08Z | c51a8fc | src/capability/explain.ts. Guards a specific failure: manager asks why Mrs Nguyen got a different GP and the answer is "the algorithm decided" — that ends a pilot. Explanation is TOTAL not best-effort: indexes ROUTING_REASON_COPY by the RoutingReason union so a new reason without copy is a TYPE ERROR, and there is deliberately NO fallback branch (a fallback is how "unexplained" ships). KEPT decisions explained too — "why was this patient not moved" is asked as often, and W85 can hold a whole plan back so a practice that enabled routing and saw nothing needs the reason surfaced per decision. Alternatives carry their own reasons (qualified: threshold + stated preference; unqualified: W82 failures) so "not chosen" is never a bare fact. No explanation mentions patient health — test bans clinical vocabulary. Verify: plan spanning EVERY RoutingReason has zero unexplained. NOTE: 2 fixtures initially failed because a 1-2 decision plan with one routed is a 50-100% routed share and correctly tripped W85 ceiling — fixtures were wrong, not the guard |
| W87 | done | builder-B | 2026-08-10T07:12Z | 53d1671 | src/capability/routing-sim.test.ts + reports/routing-w87.md. Same seeded run both arms (W63 method). FIRST CUT WAS VACUOUS: a hard-coded 25-visit floor left all 10 clinicians above it, nothing routed, arms identical — a routed-vs-unrouted comparison comparing nothing (same class as W60's wrong-page copy test). Floor now DERIVED from the run's median attended count, with a test asserting the roster actually splits. With a real floor the result is worth having: 5 of 10 clinicians below, routing would move ~half the panel, and W85's guardrail holds the ENTIRE plan back on both limits — the strongest evidence yet that W85 was worth building, since W84 alone would have redirected half a practice. Test deliberately asserts NO direction on the verdict (that is a property of the clinician distribution, not to hard-code) — only that verdict and plan agree. Verify: fleet checkInvariants(), zero unexplained decisions both arms (W86), determinism, report cannot claim invariants held without them holding |
| W88 | done | builder-A | 2026-08-10T12:50Z | f2c525d | src/capability/provenance.ts + 18 tests. W79 made the three fields structurally distinct; this adds the dimension it left out — WHEN, and therefore whether the record is still worth believing. The unit's claim is that they go stale in DIFFERENT ways, so the tests are organised around that (a suite checking one threshold three times would pass while the distinction collapsed): INTEREST decays because people change their minds — stale but STILL USABLE, since dropping it would silently reshape a panel view the practice never asked to change. EXPERIENCE is measured from windowTo, NOT computedAt — recomputing a 2023 window today does not make it describe today, and computedAt would claim it did; stale experience stays usable because it is a true statement about its period. COMPETENCE goes VOID not stale on expiry — an expired credential is not weak evidence, it is none — and "no stated expiry" does NOT mean never expires (W79 said so; this is where that decision gets its cost: it ages and needs re-attestation). Unreadable dates fail closed. Void sorts first because those change what the system MAY DO, not just what it should ask about. Thresholds are a visible policy, not a hidden constant (W64 posture). usableCompetence() is the input W82's floor should read. Verify: stale profiles flagged; pnpm verify green, 844 tests / 80 files |
| W89 | done | builder-A | 2026-08-10T12:15Z | a0ad13d | docs/SUPERVISION-HOOKS-W89.md. Design only, no code. The governing rule, argued rather than asserted: a supervision hook may make specialist KNOWLEDGE available but may never make a specialist a party to an individual patient's care unless that specialist has explicitly accepted that patient — because software that appears to put a specialist between a GP and a decision both invites the GP to defer on a decision that stays theirs AND exposes a specialist to responsibility for a patient they have never seen, and neither is fixed by a disclaimer. Three hooks in increasing commitment: (1) content authorship = W69, already built and empty, specialist relates to CONTENT not patients; (2) cohort review = de-identified aggregates only, output attaches to the REGISTER not a patient, structurally not procedurally; (3) named-patient consultation = crosses the line and would make CareYield a referral-adjacent clinical service, needing recorded specialist acceptance, APP 6 patient consent, verified indemnity, and an answer to whose medical record it is. Written down precisely so a future unit cannot arrive at (3) by incremental extension of (2) — the path where every step looks small and the destination is a regulated clinical service nobody decided to build. Two blocked rows added below. |
| W90 | done | builder-A | 2026-08-10T12:30Z | 8b14b0f | docs/GATE-DOSSIER-Y2.md. Every gate G0–G7 with current status, code-level enforcement, what changed in Y2, and the specific founder action where one is needed. HEADLINE: G5 stopped being theoretical — in Q3 it was "out of Y1 scope", it now holds W56's values, W68's rules, W60's catalogue and both SUP rows. And it contains a LIVE CONTRADICTION the founder must resolve: /clinicians ships real clinical guidance TODAY (Rotterdam criteria, COCP contraindications vs WHO MEC, metformin titration, a 2025 trial) while W56 has held the entire Q5 chain ~19h for transcribing published cycle-of-care INTERVALS — which is less clinically consequential. Both positions defensible, not simultaneously. Dossier recommends ruling on BOTH AT ONCE because they are one question, and sets out what each answer implies. Also flags: app/console/interest would hold contact details for NON-patients, which is a different G2 collection (APP 5 notice/retention/erasure) than the existing analysis assumes; G6 has an unassessed directory at / with no Ahpra review and no linter reaching its copy; G7 holds structurally in the register chain but is open on the finder's matching. Asks for 3 rulings in order of what they unblock. Verify: dossier complete; pnpm verify green, 806 tests / 78 files |
| W91 | done | builder-B | 2026-08-10T07:15Z | 9384258 | docs/HARDENING-Q7.md. ZERO CRITICALS. Q7-1 MEDIUM fixed: the competence floor IGNORED RECORD PROVENANCE — Candidate carries experience/competence/interest each naming a practiceId and clearsFloor/rankByCapability never read it, so another practice's 500 visits cleared this floor, their credential satisfied a requirement, their interest ordered this ranking. Worse than the earlier instances because W84's whole claim is IN-PANEL routing: a floor cleared by foreign evidence could name a clinician this practice's data does not support (the G6 boundary the unit says it never approaches). Fixed fail-closed — a foreign record is ABSENT, not valid; routeFor passes its own practice so callers need not be right about scope. FOURTH INSTANCE OF ONE CLASS IN MY CODE (W71+W64 by W65, W74 by W78, now W82) — and written in the SAME SESSION W78 hardened the third and claimed to have caught the pattern. Noticing the tendency did not stop me repeating it two units later, so the dossier recommends a MECHANICAL CHECK (lint: function consuming a practiceId-bearing type must scope) filed as a unit, not more author vigilance. Also recorded: W87 was vacuous on first write, 2nd vacuous-test instance after W60 — both caught by reading the artefact, not the suite |
| W92 | done | builder-B | 2026-08-10T07:22Z | 00b89cc | src/referrals/capture.ts. Three boundaries W93 depends on: (1) READ-ONLY from ingested data, same posture as W57 condition flags — nothing creates/edits/infers a referral; (2) "NOT COMPLETED" IS AN ABSENCE NOT A JUDGEMENT — a specialist letter that never came back looks identical to an appointment never made, so the module refuses to distinguish them; a test bans "did not attend"/"failed to"/"non-compliant" from the copy because that collapse is easy to make in wording even when the code is careful; (3) NO CLINICAL READING — specialty is an opaque code, nothing ranks by urgency or infers a condition from the fact of a referral (G7 by another route). Minimum age before a referral counts (one written yesterday is not evidence); unparseable/future dates are data errors not gaps; cancelled excluded because someone DECIDED. Nothing dropped silently — every exclusion carries a reason and a test asserts outstanding+excluded accounts for every input. Verify: 14 fixtures |
| W93 | done | builder-B | 2026-08-10T07:26Z | 738d4a8 | src/referrals/leakage.ts. Stage is REPLAYED from events, not read from a status column (W10 reasoning): "booked, cancelled, rebooked, completed" and "booked, completed" both END at completed but only the first deserves attention — test pins same stage, different histories. Distinguishes the two stopping points because they need DIFFERENT responses (no appointment = booking problem; appointment, no letter = correspondence problem). Two refusals carried from W92 since a state machine is where they get lost: NO INFERENCE FROM SILENCE (stalls where last told, never advances on "enough time has passed") and NO CLINICAL READING / NO BLAME (test bans did-not-attend/failed/non-compliant from stage copy). Two paths that look like errors and are not: rebooking after cancellation is explicit, and a completion with no appointment ever recorded is ACCEPTED (some services send only a letter; refusing would invent leakage). Events that cannot apply are REJECTED WITH A REASON, never skipped — a replay that discards input is not a replay; tests assert applied+rejected covers every event and that ingest order cannot change the verdict. Verify: 18 replay tests |
| W94 | done | builder-B | 2026-08-10T07:30Z | 3c92110 | src/referrals/barriers.ts. One rule: RECORDED, NEVER INFERRED. Guarantee is an ABSENCE (W80 shape) — no function turns patient attributes into barriers, and a test asserts the module EXPORT LIST contains no inference entry point. Writing that test honestly exposed a real gap: the source union stopped OUR code writing an inferred origin, but the value arrives from a form/ingest/API which are not typechecked — so source is now validated at RUNTIME too (W55 CHECK belt-and-braces). Refuses rather than defaults (no fallback source = no invented origin); NO "unknown" bucket because an unknown bucket invites filling it in. Taxonomy coarse on purpose — 4 categories a receptionist picks in a second beat 20 they will not use, and finer invites guessing. Every label phrased as something a person SAID ("said the cost was a problem", never "cannot afford care"), test bans descriptions of the person. coverage() reports what share of leaked referrals have any barrier, because 4 bars over 6% of cases is not a finding. Verify: 17 no-inference tests |
| W95 | done | builder-B | 2026-08-10T07:36Z | cdc8175 | src/referrals/outreach.ts + app/console/outreach + e2e. THE NUDGE IS THE ORDINARY INVITATION, BYTE FOR BYTE — this module writes NO COPY; `planOutreach` calls W6's `renderCompliant`, so a leakage nudge passes the same gate as every other message because it IS the same message, and the lead test asserts byte-identity with `renderAvailabilityInvitation` (anyone appending "(about your referral)" breaks it). The referral is WHY THE PRACTICE IS LOOKING, never what the patient is told. Three reasons, any one sufficient: an SMS is read by whoever holds the phone, so "about your cardiology referral" discloses an investigation to a partner or a bus passenger — W92 keeps the specialty opaque precisely so it never reaches a surface and a text is the surface furthest outside the practice's control; the record CANNOT SUPPORT THE SENTENCE, since W92/W93 refuse to distinguish "the letter never arrived" from "the appointment was never made" and "you haven't been" asserts that guess to the one person who knows it is wrong; and referral framing would fail the linter anyway. Input is `LeakageSummary` NOT `Referral[]` — handed the shape carrying no specialty, so there is nothing here to leak; `contextFor()` takes the input and the recipient and nothing referral-shaped, so the guarantee is the ARGUMENT LIST rather than a test. `attended_no_completion` is NEVER nudged: it is a correspondence gap between the service and the practice, and texting the patient asks them to chase the practice's admin — W93 built the stage distinction so the two stopping points get different responses, and this is that. Two refusals that cost us sends, both deliberate: ANY open recall suppresses because a condition-free message cannot do W71's condition comparison (fail-safe direction forced, not chosen), and a patient who already told the practice why (W94) gets a conversation not a form letter. Bad practice-supplied wording THROWS the whole batch rather than withholding per-patient — one config error affecting every message, and silently withholding all of them would read as "nobody was eligible". All four inputs scoped to the practice AT WRITE TIME (W91's fourth-instance class, checked not trusted); W74's `ContactRefusal` unioned into `NudgeRefusal` so a new refusal there is a compile error here. Console names recipients in referral-record order and says on the page that the order carries no priority — naming a recipient is unavoidable when sending, ordering them by need is not (the W96/G7 line). NOTE FOR W101: `/console/outreach` is a new surface added after that claim was taken and is not yet in the axe sweep. Verify: 26 unit tests (byte-identity + zero-violation `lintMessage`) + e2e/outreach.spec.ts 6/6; pnpm verify green, 1054 tests / 94 files; full e2e 67/67 |
| W96 | done | builder-B | 2026-08-10T07:33Z | 57aebc3 | src/referrals/report.ts. The number is uncomfortable by nature (how many of your own referrals went nowhere), so framing is the work: (1) REPORTS THE RECORD — every stage line describes what is/is not on file, and the report states outright that a letter that never arrived and an appointment never made look identical; (2) STATES WHAT IT DOES NOT KNOW — barrier coverage sits NEXT TO the chart ("3 of 4, 75%; the rest is unknown, not 'no reason'") because 4 bars over 6% of cases is not a finding and the bars alone cannot say so; when nothing is recorded it says so in words rather than rendering an empty chart; (3) NEVER RANKS PATIENTS — no at-risk list, no priority, no per-patient row, because a ranked list derived from referral records is clinical triage by another name (G7); tests assert no patient identifier and no ranking language appear. Structured object rendered from (W20 pattern). Verify: golden asserted as ONE exact string, fixture covers all four stopping points + a completed chain |
| W97 | done | builder-A | 2026-08-10T14:05Z | d811f43 | src/tenancy/multisite.ts + 16 tests. The design refuses the obvious modelling: a group is NOT a big practice, because that quietly destroys the property W18 exists to hold. Group membership is a SEPARATE grant that RESOLVES DOWN to per-site decisions — a group role is turned into a synthetic site membership and run through the same authorize(), so a group role can never grant more than the same role grants at the site and there is NO group-only permission, by construction rather than by discipline. Tested: group-1 owner gets nothing at group-2's site and nothing at an ungrouped site; a group CLINICIAN cannot edit rules anywhere because a site clinician cannot. Cross-site reporting is the one place group scope is wider, and it is AGGREGATE-ONLY (SiteTotals has no patient/clinician/appointment field, asserted by enumerating its keys — nothing to leak even if a caller forgets to scope) and it NAMES the sites it withheld, because a total that quietly excludes sites is worse than no total: it looks complete. addSiteToGroup returns WHOSE access it newly grants, since extending a group is a privilege change and the audit should say who gained, not just that a row moved. Written against the failure where a group hierarchy starts as convenience and ends as an ambient superuser role nobody remembers granting. Verify: isolation at group scale; pnpm verify green, 955 tests / 88 files |
| W98 | done | builder-A | 2026-08-10T14:50Z | c8a9d52 | src/tenancy/rollout.ts + 15 tests. The design question is not "how do we onboard N sites" but "what happens when one FAILS", answered before anything is written: VALIDATE EVERYTHING, THEN APPLY. planRollout checks every site against every rule; applyRollout refuses any plan with an error and writes NOTHING. No partial-apply path exists, because the recovery story for a half-configured group (six done, one broken, five untouched, operator reconciling by hand) is worse than the failure it avoids — pinned by a test that puts one bad site among 50 and asserts zero commits. Reports EVERY problem at once, not the first: fixing a twelve-site config one error per run is a bad afternoon. The dry run is the SAME code path — planRollout is what the operator previews AND what applyRollout consumes, not a second implementation that can drift (the W40 playbook's shadow-mode habit, in code). Group defaults resolve only for name/timezone/holdout and the plan SAYS which fields it defaulted; eligibility rules, participating clinicians and protected capacity are deliberately NOT settable here, because a group default silently governing who gets contacted at twelve practices is exactly the ambient authority W97 refuses. **GATE DEVIATION, stated not hidden:** the plan says "timed e2e"; timing is asserted at unit level instead because onboarding is create-only by design (W26 closed a privilege-escalation bypass), so the console holds ONE practice and there is no browser surface to drive N sites through — a group console is its own unit. The timed tests guard against accidental O(n^2) (50 sites under 1s; per-site cost at 200 vs 50). Verify: pnpm verify green, 1043 tests / 94 files |
| W99 | done | builder-A | 2026-08-10T14:20Z | 6d41a3e | src/sim/scale.ts + 12 tests. Deliberately does NOT run 500 sims: that would measure how fast ONE machine runs 500 sims back to back, which is not the production question — in production the 500 nightly cycles are independent jobs. Projects from a measured sample and is explicit about WHICH quantities scale, which is the whole unit: LINEAR = sends, SMS spend, total compute (each practice's work is independent). INVARIANT = per-practice cycle and backfill latency, carried through UNCHANGED rather than multiplied, because a practice's cycle does not slow down because another practice exists — and if those ever DID rise with fleet size that would mean shared contention and the projection would be wrong in the direction that matters. Both properties are tested by projecting to 500 and 1000 and asserting one doubles while the other is identical. Assumptions live IN THE OUTPUT, not in a comment, because a projection whose assumptions are invisible gets quoted as a measurement — and the report's first body line says "This is a projection, not a measurement". Names the three things that would break linearity (shared lock, single job queue, one provider rate limit across the fleet), none of which is modelled. Budget breaches render as **BREACH**, not buried in a table. Verify: latency/cost budgets; pnpm verify green, 984 tests / 90 files |
| W100 | done | builder-A | 2026-08-10T15:05Z | 4266272 | src/pilot/casestudy.ts + 7 tests. Y2 register section, and the load-bearing choice is an ABSENCE: RegisterCaseStudyInput has NO condition-name field, so a Y2 case study cannot name what a register was about. Two reasons and the second is stronger — the W23 linter bans condition vocabulary, but DE-IDENTIFICATION is the real argument: "a 10-GP metropolitan practice" is not identifying, and the same practice plus a named register is a much smaller haystack. Registers are described by what they DID, never what they were about. Reports the DIFFERENCE between arms and never the messaged rate alone (W64's rule carried through: a closure rate without a comparison is just "patients attended appointments", which happens anyway), and claims NOTHING when the register had no holdout. Section omitted entirely when the pilot ran no registers, so the W45 golden is byte-unchanged. All three W45 laws still run over the longer document. NOTE for the compliance owner (recorded in the dossier, NOT fixed here): the no-ratings rule has now blocked correct copy 3x on the word "review" — W45, W65, W100. In healthcare "review" means a clinical review, not a customer rating. Copy reworded each time per the W45 power balance, but 3 hits is evidence the RULE is miscalibrated; loosening a compliance rule inside an unrelated unit is how such rules erode. Verify: golden output; 1050 tests / 94 files |
| W101 | done | builder-A | 2026-08-10T14:35Z | 0e50d86 | docs/A11Y-W101.md + e2e/a11y.spec.ts. Zero violations across every surface — but what the re-run FOUND was a COVERAGE failure, not a contrast one, which is the more dangerous kind because the suite was green throughout. The test named "sign-in and onboarding pass WCAG A/AA" had, since W49, only ever scanned SIGN-IN; onboarding was in the title and not in the assertions. It is the first authenticated form a practice ever meets and it needs a state no other test sits in (signed in, NO practice yet), so it fell through the gap between the signed-out test and the signed-in one. It passes — which is why nobody noticed. A green suite that does not test what its own test name claims is worse than a red one. Coverage re-derived from the FILESYSTEM rather than from memory: all 12 Y2 surfaces + onboarding now scanned; app/api/** and the non-practice setup steps excluded with stated reasons. Restated the two W49-era sampling fixes (reduced-motion before scanning, title asserted first) since both were originally misdiagnosed as product bugs. Post-G2 re-run requirement stands. Verify: axe zero violations; 1002 tests / 91 files, 61 e2e |
| W102 | done | builder-B | 2026-08-10T08:05Z | 904e022 | docs/COMPLIANCE-DOSSIER.md + src/compliance/surfaces.ts + ADM statement rewrite. COMPLETENESS STOPS BEING A CLAIM: the dossier has asserted "zero unmapped surfaces in app/" twice and been wrong within a day both times, corrected each time by whoever happened to look — that is the control failing, not working. It now carries a machine-checked census (34 routes, 34 lines, each naming its governing row) tested in BOTH directions: a route with no line is unmapped, and a LINE WITH NO ROUTE is a stale row describing controls over something that has moved, which reads as coverage and is the failure nobody catches. Proven to fail on a planted route before being trusted. Five surfaces were genuinely unmapped: /console/capability, /console/case-mix, /console/outreach, /api/interest/export, /api/mock/*. FINDING 1 (fixed): the PUBLISHED ADM statement had become UNTRUE — it said "we do not process diagnoses" while W57 reads condition flags and W92–W95 read referrals; the no-inference boundary held, but "we do not process them" is a claim about processing, and a privacy statement wrong about what data you hold is the document a regulator reads first. FINDING 2 (fixed): five Y2 automated decisions were undisclosed — register membership, review timing, the W85 cap, the decisions NOT to contact, and above all W84 ROUTING, which can put a different GP's name on the offer than the patient's usual one; the decision most likely to count as significantly affecting someone was the one most conspicuously missing. FINDING 3 (filed PRIV-1): /api/interest/export streams the whole community register as CSV behind `verifySession` alone — no practice scoping, no role check, no practice id in the store; correct control is "Meherr staff only" and NO SUCH ROLE EXISTS, so the right answer cannot currently be expressed — a product decision, not a patch to make inside a compliance review. FINDING 4 (filed PRIV-2): APP 12 export and APP 11 retention name NONE of the Y2 record classes (register membership, referrals, barriers, capability incl. W80's derived case mix); harmless only because those stores are in-memory while the migrations already exist. FINDING 5 (fixed): e2e/a11y.spec.ts had ALREADY DRIFTED — /console/outreach missing one day after W101 declared zero violations across every surface; third enumerated-coverage list to fall behind its routes, and the axe list is still enumerated (left to its owning unit to derive). Also: the plan says ADM transparency is "now in force" — it is not, APP 1.7 commences 10 Dec 2026, and those four months are the window to close PRIV-1/PRIV-2 rather than carry them. Verify: every surface mapped mechanically (8 census tests); pnpm verify green, 1084 tests / 96 files; a11y 4/4 zero violations; privacy e2e 3/3 |
| W103 | done | builder-B | 2026-08-10T08:25Z | f20a0e6 | docs/AUDIT-Y2.md + fix in src/registers/membership.ts. WHOLE TREE, NOT A DIFF — and that mattered: a Y2-diff audit would have missed both findings, since one lives in W57 code Y2 never touched and the other is a property of how the console has been assembled since W22. **B1, HIGH at G2, FIXED: `reconcileMemberships` could CLOSE another practice's register row.** It keyed on `patientId::conditionCode` while `RegisterMembership` carries a practiceId that `0004_registers.sql` makes NOT NULL; with `existing` spanning two practices (what a store query naturally returns) a shared patient id collided, and the consequence was a WRITE not a stale read — practice A's derivation does not contain B's patient, so B's open row took the "basis gone — close" branch and was stamped `removedAt`. A patient drops off a register belonging to a practice A cannot see, and removal is BY DESIGN indistinguishable from a legitimate departure, so nothing downstream flags it. FIFTH instance of the W91 class and the FIRST THAT MUTATES another practice's data. Three-part fix, because the first alone was not enough — proven by writing the regression test first and watching it still fail: key includes the practice; `reconcileMemberships` takes an EXPLICIT practiceId rather than deriving it from `derived[0]` the way W71 does, because `derived` is EMPTY in exactly the case that closes rows so an inferred scope would be unknown at the only moment it matters; and foreign rows PASS THROUGH UNCHANGED rather than being dropped, since "a foreign record is absent" is right for reads and wrong here — with `store = reconcile(store, …)`, absent means deleted. Also fixed: `currentMembers` had NO PARAMETER TO SCOPE BY, so it could not be called correctly — a footgun with no caller yet. **B2, filed PRIV-3:** the console cannot represent two practices (`"prac-console"` hardcoded in three modules), so `openComplaintCount()` filters by no practice and `tallyOutcomes` takes none — correct ONLY because the store holds one, which makes every console-side scoping bug structurally unobservable rather than absent; W97 makes the multi-practice day foreseeable. Confirmed clean tree-wide: ONE `fetch(` in existence and it sits behind the G3 host refusal; both `.send()` sites preceded by `renderCompliant`; `optedOut` never cleared; `Math.random` appears once, in a comment saying it is banned; all 11 server-action files gate INSIDE the action; zero type escapes, zero secrets, zero debt markers in 20.9k lines. For the plan owner: the scoping sweep that found B1 had a ~10% true-positive rate — excellent for an audit, suppressed within a week as a CI lint, so KEEP IT AN AUDIT STEP rather than pretending it automates. Scheduled failures recorded: audit allowlist expires 2026-11-09, APP 1.7 commences 2026-12-10. Verify: audit report + green suite — 1098 tests / 98 files, typecheck, build, audit:gate PASS, full e2e 67/67 |
| W104 | done | builder-B | 2026-08-10T08:40Z | fd683e8 | **YEAR 2 COMPLETE.** §5c + 52 rows: Q9 credential registry W105–W117, Q10 pathway engine W118–W130, Q11 GP-to-GP referral rails W131–W143, Q12 education engine W144–W156. Ledger runs W1–W156, no gaps, no duplicates. DERIVED FROM WHAT WAS BUILT, not only the theme — three Y2 residues scheduled rather than hoped for: (1) **G5 stops being deferrable in Q10** — W56's intervals and W69's workspace have been blocked since Q5 and the pathway engine is the same question at 10× the scale; the engine ships empty without the ruling (W68/W69 pattern) but delivers an empty PRODUCT, so the ruling is a dependency; (2) **the first live quarter will be integration, not features** — the send path is still unwired so W74's preferences and W95's outreach are captured and inert (W103 obs. 4), Q9 therefore opens with deferred privacy work and nothing in Y3 assumes a send path until G3; (3) **two dated failures fall inside Q9 and are units, not surprises** — allowlist expiry 2026-11-09 (W107) and APP 1.7 commencement 2026-12-10, the deadline on W105/W106; PRIV-1/2/3 now name their owning units instead of floating. **PROPOSED GATE G8 — third-party model processing — added to plan §4 for FOUNDER RATIFICATION, not decided here**: Q12 sends content to the Claude API, the first time anything leaves this tree to a third party, and no existing gate covers it (G2 = holding real patient data, G5 = clinical content, G7 = CDSS; none says whether de-identified case context may be TRANSMITTED to a model vendor at all). W146/W147 written to be buildable behind it and marked blocked on it. Q12 drafted defensively throughout because case-triggered clinical education from a model is the closest this product has come to CDSS: W144 writes the boundary BEFORE any code (W89 pattern) and names the incremental path to a regulated clinical service so a later unit cannot arrive there by extension; W145 gives the engine no generation entry point at all; W152 makes a source-less item unrenderable by type. Verify: 52 units appended with verify gates, gates inherited intact, ledger integrity W1–W156 checked; pnpm verify green, 1098 tests / 98 files, audit:gate PASS |

> **Year 3 — Scope Yield foundations** (expanded by W104, 2026-08-10; definitions in docs/FIVE-YEAR-PLAN.md §5c). Q9 W105–W117 · Q10 W118–W130 (**G5 load-bearing throughout**) · Q11 W131–W143 · Q12 W144–W156 (**behind proposed gate G8 — third-party model processing, awaiting founder ratification; the loop must not decide this itself**).

| W105 | done | builder-B | 2026-08-10T09:00Z | 46f6e00 | src/tenancy/staff.ts + gates on /console/interest and /api/interest/export. Closes PRIV-1 four months ahead of the APP 1.7 deadline. **DELIBERATELY NOT A FOURTH ROLE**: adding `meherr_staff` to `Role` would route it through `authorize()`, and a practice owner holding `manage_members` could then grant THEMSELVES access to the contact details of people who are not their patients — W97's lesson run BACKWARDS (W97 resolved group roles DOWN to site memberships precisely so a group role could never grant more than a site role; this requirement runs the other way, so it must not touch the practice authorization path at all). A separate list no practice can write is a structural guarantee; a fourth role would be a convention — tested by asserting NO role at ANY practice makes anyone staff. **SHIPS EMPTY** (W68/W69 posture): nobody is Meherr staff until the founder says who, so today the answer is "nobody can read it" — strictly safer than yesterday's "everybody signed in can" — and the register is closed, not deleted; a test pins the list at zero so a later unit cannot quietly add one. Grants live in CODE not an env var: a handful of people at this scale, and a commit is an auditable record of who was added and by whom. **The signup list is not fetched AT ALL for a non-staff visitor** — conditional rendering satisfies the eye and not the wire, since in a server component the data still reaches the RSC payload, so the gate sits ABOVE the read and the e2e asserts the count, the download link and every mailto are absent from the response body. Gated at the ROUTE as well as the page (W13 — a page check protects the link and nothing else, and the route streams the whole register in one request); 403 not 404, because the register's existence is already public and what is withheld is the contents. Coverage note: /console/interest's axe pass now exercises the refusal panel, so the register itself needs an a11y pass when the first staff account is granted. Verify: 4 e2e + 5 unit tests; pnpm verify green, 1108 tests / 99 files; full e2e 71/71 |
| W106 | done | builder-A | 2026-08-10T15:40Z | 57638ed | src/privacy/record-classes.ts + src/interest/store.ts + 13 tests. RECORD_CLASSES enumerates all 15 modules that could hold patient identity, and the test CHECKS THE REGISTRY AGAINST THE SOURCE TREE — a module with a globalThis store that is not declared FAILS THE SUITE. That closes the actual W51 lesson: the critical finding was never bad erasure code, it was that erasure covered the stores existing when it was written and the next store joined the tree without joining the erasure path; enumerating by hand would reset the same trap. `derived` is a REVIEWED answer not an exemption (membership and gaps are recomputed from PMS flags and never persisted, so erasing the source erases them — a test forces each derived class to explain why). Every class needs a rationale, because an undocumented classification is a guess. Functional half: the community interest register — the class the Y2 dossier flagged, holding contact details for people who are NOT patients of a subscribing practice — now has APP-12 access, erasure and APP-11 retention; erasure rewrites the FILE (asserted, not filtered on read), lookup is case/whitespace insensitive so neither becomes a way to miss someone's record, and an unreadable date is KEPT not pruned (losing data to a parsing bug is worse). Pins that the suppression list is deliberately NOT erasable: erasing it would re-open contact. |
| W107 | done | builder-B | 2026-08-10T09:25Z | e6fce9a | src/security/reachability.ts + review history on AllowlistEntry. Meant to be a date check three months early; it found something. **The image-size acceptance rests on ONE claim — pptxgenjs is build-time only and "absent from the deployed app" — and nothing checked it, while the sibling case was ALREADY BROKEN**: `app/console/results/page.tsx` imported `DEFAULT_REPORT_OPTIONS` (three plain numbers) from `src/report/weekly.ts`, which imports `docx` at module scope, so a server component loaded a document library to read a constant. Benign in itself (docx has no advisory), NOT benign as a pattern — it is precisely the reasoning the acceptance depends on, applied to the package next door. **What did not catch it: `next build` succeeds either way**, because a devDependency is installed in the build environment — a green build is not evidence a build-time package stayed out of the bundle. Fixed by splitting `src/report/options.ts`, and made STRUCTURAL by a static import-graph walk from every file under `app/` asserting no request-serving path reaches pptxgenjs/docx/adm-zip and no allowlisted module is reachable at all; proven non-vacuous by restoring the old import and watching the docx case fail with an actionable message. **Second change: an acceptance could be renewed by moving `reviewBy` forward on its own** — the cheapest response to a failing gate and indistinguishable in a diff from one genuinely re-argued. `AllowlistEntry` now requires a NON-EMPTY review history (tuple type, so the compiler asks), each review records the date it set, and the gate requires `reviewBy` to equal the newest review's `extendedTo` — move the date without writing down what you checked and the two fall out of step and the acceptance stops working. Both advisories re-reviewed against live data (advisory updated 2026-08-07): still no patched release, patched range `<0.0.0`, recommendation None. **DELIBERATELY NOT EXTENDED** — the November date exists to force a look NEAR the deadline, and moving it in August because the answer has not changed would spend the forcing function for nothing; the first recorded review is one that extends nothing, which is the case the mechanism most needs to support. Verify: audit:gate green, no acceptance past its date, none extendable without a fresh rationale; pnpm verify green, 1116 tests / 100 files; results/dashboard e2e 9/9 after the import split |
| W108 | done | builder-A | 2026-08-10T16:10Z | f3fd73e | src/credentials/model.ts + 21 tests. The unit's gate — "no credential is representable without a verifier and a date" — is met by the TYPE, not by validation: `verifiedBy` and `verifiedOn` are non-optional on `CredentialRecord`, so there is no path to an unverified credential at any call site, and the test pins that half with `@ts-expect-error` on both omissions. That is the W55/W79/W69 pattern for the same reason each time — a rule the compiler holds cannot be forgotten under deadline, and a rule that lives in a validator only protects the callers who remember to call it. **THE DISTINCTION THIS PROTECTS IS W79's**: interest is what a clinician says, experience is what the system derived, competence is what someone EXTERNAL verified — and the third is worth nothing if "verified" can mean "typed in by the person it describes". So `verifiedBy` is refused when it equals `clinicianId` (self-verification, the W69 rule applied to credentials): a workflow that permits it produces the paper trail of a check that never happened, which is worse than no check at all because it LOOKS like one. **EVIDENCE IS A REFERENCE, NEVER EMBEDDED** — `documentRef` points into the W109 vault, so a credential record never becomes a place documents accidentally live, and a credential can be read by code that has no authorization to read evidence; `no_evidence` is its own refusal because a credential with nothing behind it is a rumour with a date on it. Eight refusals total (issuer, verifier, unreadable date, future date, self-verified, no evidence, evidence without a reference, expiry before verification), ALL returned at once rather than first-wins — a reviewer fixing a submission one error per round is a reviewer who stops reviewing carefully — and each carries a plain-English `explainRejection`. `expiresOn: null` means NO STATED EXPIRY, not "never expires"; W88 already drew that line and charged it a staleness cost, and the comment says so where the next reader will re-decide it. **DELIBERATELY DOES NOT DECIDE SUFFICIENCY** — whether a credential is enough for anything is W82's competence floor and W111's Ahpra check; this is the record. G5 untouched: the module ships no credential values and no clinical judgement. Verify: pnpm verify green, 1157 tests / 103 files (rebased onto builder-B's W111 and re-run) |
| W109 | done | builder-A | 2026-08-10T17:05Z | dd9d579 | src/credentials/vault.ts + 22 tests. The gate — "no route serves an evidence document without authorization" — is a claim about every route that will EVER be written, and no runtime test can make a claim about code that does not exist yet. So the API makes it: `readEvidence` takes a `VaultGrant`, the grant is branded with a `unique symbol` this module never exports, and `openVault` is the only thing that mints one — through `authorize()`. **A ROUTE THAT SKIPS THE CHECK DOES NOT FAIL REVIEW, IT FAILS TO COMPILE** (W69's shape, different question). Three `@ts-expect-error` cases pin it, and TypeScript errors on an UNUSED expect-error, so they cannot rot into vacuity the way W65's tautological guard did. **ISOLATION IS STRUCTURAL, NOT CHECKED**: the lookup matches ref AND practice in ONE step, so there is no branch in which the code knows a ref exists elsewhere — a miss and a neighbour's document are literally the same answer, leaving nothing for a refactor to start reporting. That is the OPPOSITE of W105's 403-not-404 and the reason is written down: there the register's existence was already public and only contents were withheld; here the existence of a credentialing document for a NAMED clinician at another practice IS the disclosure. `storeEvidence` takes practiceId and uploadedBy FROM THE GRANT rather than from the input, so a cross-tenant write is unrepresentable rather than validated (W108's move) and the uploader is stamped by us, not trusted from the caller (W111's rule). **A SUMMARY IS NOT A DOCUMENT**: listings return a type with no `content` field and the test asserts it on the SERIALISED payload, not the type — a field dropped in the type and re-added by a spread passes tsc and fails this (W83's coarseness check). **CLINICIANS DELIBERATELY NOT GRANTED** `read_credential_evidence`: it is the role every clinical user holds, so granting it there grants every clinician every colleague's file. Subject access to one's own record is a real right and is W113's — it must be scoped to the SUBJECT, not to the role, and refs are content-derived hex carrying no practice/clinician/credential identifier precisely so that path stays open without becoming an enumeration of everyone else. Content-type whitelist (pdf/png/jpeg) because a vault that accepts text/html plus a route that serves it back is stored XSS handed to whoever can upload. Joined both tree-checked registries (W106 record classes — declared `no_patient_identity`, with the rationale stating what that does NOT cover; W51 store resetters) — those mechanisms working, not a courtesy. One self-caught test bug: the payload assertion first banned the substring "content", which `contentType` contains; tightened to the JSON key rather than loosened. Verify: pnpm verify green, 1182 tests / 104 files |
| W110 | done | builder-A | 2026-08-10T17:45Z | 7d8ea28 | src/credentials/verification.ts + 24 tests. W108 made `verifiedBy`/`verifiedOn` required BY THE TYPE, which guarantees the fields exist and says nothing about whether they were EARNED. This is where they are earned: submitted → checked → verified as events, the history replayable, and nothing stored alongside the log that could disagree with it. **SELF-VERIFICATION IS UNREACHABLE, NOT DISCOURAGED** — refused at the TRANSITION, so no log can contain it and no replay can produce a credential verified by its own subject (W69's rule in the case it matters most). Tested by breadth-first search over every event kind from every reachable state, because the unit's claim is about ALL logs, not about one call. **EXPIRY IS COMPUTED, NEVER TRANSITIONED**: there is deliberately NO expire event, because if expiry needed a job to apply it then a credential whose job did not run would read as VERIFIED — the failure mode where the safe answer depends on a cron. `replayVerification` takes `asOf` (required, never defaulted to the clock: a replay whose answer depends on when it runs is not a replay and every golden built on one drifts silently) and derives it. An expired credential returns NO provenance rather than weaker provenance — W88's void-not-stale — and `verifiedProvenance` is the only route to W108's two fields, so they cannot be sourced from an incomplete workflow. `ALLOWED_TRANSITIONS` is a declared table and a test drives every state through every event asserting the table IS the behaviour, so it cannot drift into being mere documentation. **ONE PERSON SUBMITTING AND CHECKING IS RECORDED, NOT REFUSED** (`singleHandled`, for W115): a two-clinician practice where the owner collects the documents AND confirms them is the ordinary case, and a rule blocking it buys a second pair of eyes by inviting a second account — so the rule that stops the actual fraud stays absolute and the weaker segregation concern is made VISIBLE instead of enforced into a workaround. Deliberately its OWN log, not the W10 spine: that log's events, replay and exports are built around patients and invitations, and filing clinician credentialing history there is a privacy-shaped mistake rather than a reuse. Self-caught: the search's non-vacuity guard was first a made-up append count (`>50`, actual 22) — replaced with a coverage assertion that the search REACHED every state including `verified`, which an over-refusing machine cannot satisfy and which does not need a magic number. Verify: replay asserted on every PREFIX of the log (a fold only correct at the end is wrong the first time someone asks what the state was on Tuesday); pnpm verify green, 1224 tests / 106 files |
| W111 | done | builder-B | 2026-08-10T09:50Z | 69e0d56 | src/credentials/ahpra.ts + 13 tests. Reading the register is how a credential stops being a claim the clinician typed and becomes a fact somebody can check — and this module does the reading and NOTHING ELSE. **A LOOKUP IS NOT A CREDENTIAL**: a register hit says "this person is registered", not "this person may do the thing", and collapsing the two is how a registration becomes an implied competence; the bridge to W108's model is deliberately absent and a test asserts the export list contains nothing named credential/verify/attest/grant/approve. **NO INFERENCE FROM ABSENCE**: a miss is `not_found`, a statement about the SEARCH — the outcome union has NO "unregistered" member, so the conclusion this lookup is not entitled to draw is unrepresentable rather than discouraged (W92's shape). A thrown lookup is `lookup_unavailable` NEVER `not_found`, so an outage cannot become an absence of registration. Two matches are `ambiguous`, never the first one — practitioners share names more often than a practice notices and picking one attaches a registration to the WRONG PERSON, the hardest failure to spot afterwards because the record looks complete. An empty query is refused rather than run, since an empty search against a register of practitioners is a request for the whole register. Live register gated in the constructor (W28/W36 posture) — querying it touches real practitioners' data and has its own terms, so it is a founder decision, not something a build unit opens by wiring a URL; refusal tested against four host shapes PLUS a fixture host so the constructor is not simply throwing at everything. `checkedAt` stamped by the adapter not trusted from the client (the age of evidence is a property of when WE read it; a client supplying its own could make a stale read look fresh). Conditions carried VERBATIM — a condition is a legal statement and paraphrasing one is a claim about it. No field grades or endorses anyone, asserted by enumerating the record's keys. Taken out of order: W109/W110/W113–W115 all wait on builder-A's in-flight W108 model, and this is the Q9 unit that does not. Verify: live-host refusal, fixtures only, no network client in the module, instantiated nowhere outside tests; pnpm verify green, 1142 tests / 102 files |
| W112 | done | builder-A | 2026-08-10T18:20Z | 2243bea | src/credentials/attestation.ts + 17 tests. W110 already derived `expired` at replay time, so the STATUS was honest — and that is not enough, because a status only matters if every consumer acts on it, and "every consumer remembers to filter" is exactly the shape of promise that fails: one report shows the expired credential greyed out with a caveat, and a lapsed qualification is back in front of a decision-maker AS WEAK EVIDENCE rather than as nothing. **SO THE GATE IS MET BY THE TYPE**: `LiveCredential` is branded and only `liveCredentials()` can mint one, and there is NO variant of the output carrying an expired credential in weakened form because the output type has no room for one (W59's move — the eligibility filter has no branch producing `eligible: true` — pointed at a different question). Asserted on the serialised result, not just the length. **NO STATED EXPIRY DOES NOT MEAN NEVER EXPIRES** is the sharper half: W108 refused to let `null` mean forever and W110 carried the null through without inventing a date, which leaves a credential verified once in 2026 still presented as current in 2040. It now gets an EFFECTIVE expiry of `verifiedOn + unstatedLifetimeDays` and is treated exactly like a stated one — absent, not weak — with `effectiveExpiry` reporting `stated: false` so it is never a silent disappearance, and `reattestationDue` giving a quarter's notice ahead of it. A boundary test drives both expiry paths (W110's status and this module's effective date) across four consecutive days asserting they never disagree — two readers reaching different answers about the same credential is the failure this pair could produce. **A RENEWAL IS A NEW CREDENTIAL NAMING ITS PREDECESSOR, NOT A RE-OPENING**: the old verification really did happen, and stamping a fresh date on it would erase whatever GAP sat between the two, which is precisely what an audit asks about (needed one surgical addition to W110: optional `supersedes` on the submitted event). A renewal that was submitted but never verified does NOT silence the warning — that is exactly how a credential lapses while everyone believes it was handled. A lapsed credential keeps appearing on the re-attestation list even though it is absent from every output: absolute expiry without a work list is a support ticket. Policy note: `unstatedLifetimeDays` is practice ADMINISTRATION not clinical content, so not a G5 item, but still the founder's to set — the default errs SHORT because the asymmetry is one-sided (short costs admin time; long presents a lapsed credential as current), and a test pins that direction so the reasoning cannot be reversed silently. Self-caught, and the same trap twice: the brand-forgery test first annotated an UN-IMPORTED `LiveCredential`, so `@ts-expect-error` was satisfied by "cannot find name" and proved nothing; importing it then exposed that two fields were `string | null`, which would have satisfied the directive on NULLABILITY instead. Rewritten with plain literals so the missing brand is the ONLY thing wrong — W65's lesson, caught in my own test. Verify: pnpm verify green, 1241 tests / 107 files |
| W113 | done | builder-A | 2026-08-10T18:55Z | e1c965d | app/console/credentials + src/credentials/ledger.ts + 9 unit + 8 e2e tests. **THE SAFETY ARGUMENT IS ONE SENTENCE AND EVERY CHOICE FOLLOWS FROM IT: every action a clinician has here can only REDUCE what the system claims about them.** Withdrawing removes a claim; there is NO control that adds one — W110 refuses self-verification outright, and the page deliberately offers no add-a-credential form because credentialing starts with the practice, which holds the evidence. So the page cannot be used to manufacture a qualification, not because it validates carefully but because it exposes nothing pointing that way; an e2e enumerates every control in the record list and asserts all are withdrawals. **THE READ IS SCOPED BY SUBJECT**: `ownCredentials` takes the clinician id as its QUERY, not as a filter over a practice-wide fetch, so there is no "everything, then narrow" step for a refactor to drop — this is the answer W109 required instead of granting evidence access to the `clinician` role, and the browser half asserts a colleague's credential never reaches the page. `practiceCredentials` is a SEPARATE function rather than a flag, because a `showAll` boolean is one mistaken argument away from being the wrong call. Lapsed credentials ARE shown here although W112 makes them absent everywhere else: the person a record is about must be able to see that it lapsed (W83's "a record you cannot inspect is one you cannot correct", which applies with more force to qualifications). Evidence DOCUMENTS are not served at all — W109's grant stays unobtainable by a clinician, and subject access to the scans needs the founder's G6 position (W117). Unlinked sign-ins are REFUSED rather than shown a probable match (W81's rule unchanged; on a page about someone's qualifications the cost of guessing is higher). The withdraw action re-checks authorization, identity and ownership independently (W13: a server action is its own endpoint; W51: a session is not authorization), and answers "belongs to a colleague" and "does not exist" identically so the error cannot confirm a colleague holds that id. History is not deleted by a withdrawal — an e2e reads the log back and asserts all four events survive. Joined THREE tree-checked registries (W106 record classes, W51 store resetters, W102 surface census — the census caught both new routes, which is that mechanism working). Verify: pnpm verify green 1249 tests / 108 files; full e2e 79/79; axe zero violations with the page scanned POPULATED (seeding in the a11y beforeEach, because scanning the unlinked refusal would pass while leaving the list, status chips and forms untested) |
| W114 | done | builder-A | 2026-08-10T19:35Z | 489797c | src/credentials/scope.ts + 17 tests. A credential without a scope is a claim with no edges — "verified" says someone checked something and says NOTHING about what the practice may now do differently. **A PERMISSION HERE IS NEVER A CLINICAL ONE**: nothing in this system authorises anybody to do anything clinical (that is registration, scope of practice and the practice's governance, none of which a scheduling product is entitled to encode), so `ScopePermission` is a CLOSED two-value vocabulary and a test asserts no member matches perform/prescribe/diagnose/treat/administer/certify — W111's move from the other side, where the conclusion the product may not draw is unrepresentable rather than discouraged. **UNLINTED IS UNREPRESENTABLE**: a scope label is free text a practice writes and it lands NEXT TO A CLINICIAN'S NAME, which is exactly where regulated advertising language does its damage, so `ScopeStatement` is branded and `scopeStatement()` — which runs both linters — is the only producer. **REACH IS STRUCTURAL, NOT REMEMBERED**: rather than copying the patterns, this extracts `lintMessageText` from W6 (the banned-language half, WITHOUT the identifies-practice/opt-out/booking-link checks, which would report three meaningless violations on a label) and both linters now export their rule-name lists, so the test asserts NO rule from either fails to reach a scope label and a rule added to either arrives here the same day. Proven non-vacuous from both sides: a W23-only rule (`no-testimonials`) and a W6-only rule (`no-overdue-framing`) each refuse a label. **s 133 IS RELATIONAL, AS THE GATE WORDS IT**: "specialist" beside a NICHE scope is its own refusal naming the section, because "specialist in <condition>" is the reading s 133 exists to stop; the W23 blanket ban then also refuses it beside a general scope, which is STRICTER THAN THE LAW and deliberately so — whether a clinician may use a protected title is a fact on the Ahpra specialist register, which W111 can read and this module cannot rule on, and Meherr carries no specialist titles at all. Safe under any founder ruling, and the refusal copy says so rather than looking like an oversight. Ships ZERO scope statements (W68/W69/W105 posture, pinned): a statement of what a credential covers for a named condition is clinical content and the founder signs it off (G5). Verify: pnpm verify green, 1266 tests / 109 files |
| W115 | done | builder-A | 2026-08-10T20:05Z | b747f4a | src/credentials/provenance-report.ts + 13 tests. **THE REPORT'S MOST USEFUL NUMBER IS WHAT IT DOES NOT KNOW.** The temptation is to list the credentials on file and call that credentialing, and that is the QUIET failure: a report showing only what EXISTS implies everything absent is fine, so a clinician nobody ever credentialed appears nowhere and reads as no problem. Coverage is therefore not an appendix — clinicians with NO record are named, with the line that this is a gap in the practice's RECORDS rather than a statement about them, and credential subjects who are no longer on the roster are flagged the other way. **RANKS NO CLINICIAN**, and not by avoiding ranking language: there is NO per-clinician tally at all, because a count of credentials per person is a league table however it is captioned (W83 already refused to let a capability graph become a productivity board). Rows order by credential id — arbitrary on purpose and SAID SO IN THE OUTPUT — and a test asserts one clinician's two credentials are not gathered into a block, that the report object has no score/rank key, and that rendering is stable under reversed input order. **STATES ITS OWN COVERAGE EVERY TIME, INCLUDING WHEN EMPTY** — the case where a report is most likely to be quoted as "all clear" (W99's assumptions-in-the-output, which matters harder here: a credentialing report quoted without its coverage is evidence of diligence that was never done). Explicit about what it is not: an assessment of competence, a register check (W111 is its own act, and a credential verified here may still need that lookup), or a way to read evidence — documents are COUNTED, never included, because they stay behind W109's grant which this report never holds. A derived review date is labelled as one, so a practice policy date is never read as an issuer's expiry. W110's `singleHandled` surfaces here as promised, naming the CREDENTIAL not the handler and repeating why it is recorded rather than forbidden. Golden covers the WHOLE rendered document, caveats included, so the wording cannot drift without a deliberate edit. Verify: pnpm verify green, 1279 tests / 110 files |
| W116 | done | builder-A | 2026-08-10T20:35Z | 078fd3d | docs/HARDENING-Q9.md + src/lib/source-hygiene.test.ts + 5 fixes. **SIX FINDINGS, SIX FIXED, ZERO CRITICALS** — and the honest reading of that gate is not "nothing found" but "nothing found was critical": THREE OF THE SIX WERE MINE, from units that shipped `pnpm verify` GREEN in this same session, and the worst sat in main for two units. **HIGH — a source file review could not read**: `vault.ts` shipped in W109 with three NUL bytes inside a template literal, so git classifies it BINARY and `git diff`/`git show` print "Binary files differ" and nothing else while grep/ripgrep skip it. It compiled, its tests passed, and the tree-walking registries still matched their regexes (Node reads NUL as an ordinary character), so every existing control was satisfied. What it broke was REVIEW — for a repo whose whole discipline is that the reasoning lives in the diff, a file with no diff is a hole in the PROCESS, not a bug in the code. Surfaced only because a grep in this sweep answered "binary file matches" instead of a line number. Fixed and made structural: a tree walk failing on any NUL in a text source, PROVEN NON-VACUOUS by reintroducing the byte and watching it name the path (W107 discipline). **HIGH — a byte limit that did not measure bytes**: `MAX_EVIDENCE_BYTES` was compared against `content.length`, which counts UTF-16 CODE UNITS, so an "8 MB" limit admitted up to 4x that and `byteLength` recorded a number that was not bytes; not exploitable (nothing uploads yet) but a resource limit measuring something other than what it says is one nobody downstream can reason about, and that field would have become the number a storage bill was estimated from. **MEDIUM — a duplicate submission could erase a verification**: `replayVerification` reset the record on a second `submitted`, silently discarding provenance; unreachable through `appendVerification` today, but replay is exported and TOTAL over any log including one rehydrated later, and the question a hardening pass asks is how a module degrades when the guarantee upstream stops holding. **MEDIUM (builder-B W81) — authorization by coincidence**: case-mix was the ONLY console action with no membership check; nothing was exploitable (the actor is resolved from the session and can only act on itself, so W51's failure is absent) but the DECISION was missing — being on the clinician roster is not being a member of the practice, and "authorized because the roster happens to contain your address" is not a rule anybody chose. Two LOW (duplicated rule name in the scope-label union; duplicate import). **W103 SCOPING SWEEP**: four Q9 capabilities are built ahead of their consumers (vault reads, `liveCredentials`, scope statements, provenance report) — deliberate, since `liveCredentials` exists precisely so the first reader of a credential CANNOT read an expired one, but four in one quarter is the pattern W103 flagged, and the honest statement is that Q9 shipped INFRASTRUCTURE and the quarter that consumes it is where the design gets tested. Method note carried into the doc: findings 1 and 2 were invisible to every test in the suite because they were about the FILE and the UNITS OF A NUMBER, not about behaviour — both are now covered by checks that read the tree, the third time this quarter a tree-reading check caught what a behavioural test could not. Verify: pnpm verify green 1286 tests / 111 files; full e2e 79/79 |
| W117 | done | builder-A | 2026-08-10T21:20Z | d79a99b | docs/GATE-DOSSIER-Q9.md. **THE DECISION IS FREE TO MAKE NOW AND STOPS BEING FREE THE MOMENT Q11 BEGINS**: W131's referral document model determines what a referral can CARRY, so a credential field added there before anybody rules makes the ruling BY DEFAULT — and reversing it then means migrating documents rather than deleting a line. W133's own stated verification is "never crosses G6", and a unit cannot verify it never crossed a line nobody drew. Deliberately NARROW: it does not refresh G0–G5 or G7 (Q9 changed none of them, and a dossier restating unchanged gates trains its reader to skim). **THE SUBSTANTIVE OBSERVATION IS THAT TWO SEPARATE FACTS ARE ONE RISK** — an unassessed patient-facing directory exists at `/` and `/finder` (carried from the Y2 dossier UNCHANGED, now open a full year), and a credential registry now exists; neither is connected, and the connection is the single change that would turn a prototype directory into a regulated advertising surface displaying practitioners' QUALIFICATIONS, the most heavily governed form there is. No unit is scheduled to make it, which is precisely why it is written down: unplanned connections are how the Y2 dossier's "status changed materially without a decision" happened the first time. Two options presented with their regulatory footing and NOT chosen between — (A) capability presence only, where W133's gate is trivially satisfied but a referring GP cannot see WHY the receiving GP is suitable, which may just push the conversation to the phone where it is unrecorded; (B) credential detail practice-to-practice inside a consented network, where practitioner-to-practitioner communication about a patient's care is not public advertising PROVIDED four conditions hold as ENGINEERING requirements not aspirations, and where the boundary is behavioural ("a network any practice can join on request is a directory with a sign-up form"). Public directory recorded as ALREADY CLOSED by CLAUDE.md law 4 rather than presented as live. Choosing is a product judgement about where the referral rail's value sits, and a build unit pre-empting it is the failure this unit exists to prevent. Three positions Q9 took IN CODE are listed for ratification rather than left as inheritance: no specialist titles at all (W114, stricter than s 133 requires), no clinician access to evidence documents INCLUDING THEIR OWN (W109/W113, deferred deliberately not overlooked — and if granted it must be scoped to the SUBJECT, never to the `clinician` role), and the three-year unstated-expiry default (W112). **SIX FOUNDER ACTIONS, ordered by what they block**, each naming its blocked units; action 1 (rule A or B before W131) is the only time-critical one. Verify: dossier complete — G6 status, code-level enforcement, what Q9 changed (nothing, stated as a FINDING rather than an absence), the Q11 decision, the option set, and named actions; cross-checked against the Y2 dossier, plan §4, Q11's unit list, BUILD-STATE blocked rows (all G5, none G6) and HARDENING-Q9's scoping sweep. pnpm verify green, 1306 tests / 113 files |
| W118 | done | builder-B | 2026-08-10T10:10Z | c8642a3 | src/pathways/versioning.ts + 18 tests. **G5: catalogue ships EMPTY**, pinned by a test — mechanism now, content after the ruling (W55/W56/W60/W68 pattern). Immutability, edit-makes-a-new-version and replay are ONE DECISION: versions are not stored, they are DERIVED from an append-only event log. That matters more here than anywhere else in the tree because of what a pathway is FOR — if a clinician acted under version 3 in March, the question an audit asks in September is what version 3 SAID, and a mutable row can only answer what it says NOW; a store that lets you edit history cannot reconstruct a clinical decision, and reconstructing clinical decisions is why this content is versioned rather than simply edited. So immutability is a PROPERTY OF THE REPRESENTATION, not an enforced rule: no mutation function exists (asserted against the export list), a version's identity is its CONTENT HASH so an edit is a different version (W67's lesson, not a counter someone forgets to bump), and events that cannot apply are rejected WITH A REASON rather than skipped (W93). The hash is RECOMPUTED on replay, never trusted from the event — otherwise a version could be published under an identity it does not have and the audit trail would name the wrong document. Criteria are carried OPAQUELY: this module hashes and versions them and never looks inside (evaluation is W120, escalation semantics W121, withdrawal W128), which is what lets the content stay gated while the machinery ships; the criterion shape gives symptom inference nowhere to hide — no free-text expression, no threshold, just a recorded fact code and present/absent. **A MUTATION CHECK EARNED ITS KEEP**: removing the supersession guard from `pathwayAt` changed NO test, because the guard was compensating for a wrong selection rather than expressing the rule — with two versions published at the SAME INSTANT it returned null, reporting no pathway in force when one plainly was. `pathwayAt` now asks the replay's own state using the same predicate the invariant test asserts, so implementation and test cannot drift, and the new case fails against the old code. Taken out of order: Q9's W109–W117 all wait on builder-A's in-flight W108 model; Q10's head depends on nothing in Q9. Verify: published version immutable, edits create a new version, replay reproduces any version — 18 tests incl. ingest-order independence and cross-pathway isolation; pnpm verify green, 1160 tests / 103 files |
| W119 | done | builder-B | 2026-08-10T10:40Z | 91ed88f | src/pathways/approval.ts + 20 tests + 2 compile-time assertions. W118 built the container; this decides which versions may be USED, on W69's rule: an unsigned pathway is UNUSABLE — not flagged, not warned about — and the TYPE SYSTEM enforces it, because a runtime check can be forgotten at a call site and a type cannot. `UsablePathway` is branded with a unique symbol, `usablePathway()` is the only source, and W120's evaluation and W123's binding must take THAT type, never `PathwayVersion`. Three stages not two (W69's reason): a specialist answers "is this clinically correct?", the founder answers "do we accept the liability of shipping it?" — so one person doing both is refused, as is the author reviewing their own work and a sign-off dated BEFORE the review it depends on. **ONE PROPERTY COMES FREE AND IT IS THE IMPORTANT ONE**: attestations are keyed by version hash and W118 made identity be CONTENT, so editing a pathway does not invalidate its sign-off — it produces a different version that was never signed at all. W67 had to BUILD hash-keyed revocation; here no code path could get it wrong. Sign-off kept separate from being in force: a draft or superseded version is unusable however well attested, since conflating them would let a retired version keep working because it still had signatures. **THE TYPE CLAIM IS VERIFIED IN `tsc`, NOT VITEST** — `approval.types.test.ts` holds `@ts-expect-error` assertions inside a function that is NEVER CALLED (the runtime would only prove that reading fields off the wrong shape throws, which is not the claim); confirmed non-vacuous by deleting the brand and watching `pnpm typecheck` fail with TS2578. That check also CORRECTED AN OVERSTATEMENT: a raw `PathwayVersion` is rejected on SHAPE even with no brand, so only the hand-assembled-with-every-field case actually tests the brand — the comments now say which line proves what instead of implying both do. Ships with ZERO attestations, pinned by a test, so nothing is usable today. Verify: unapproved pathway unusable by type, zero signed at ship; pnpm verify green, 1299 tests / 112 files |
| W120 | done | builder-A | 2026-08-10T21:55Z | 6a06826 | src/pathways/evaluation.ts + 17 tests. **THE G7 BOUNDARY IS A BOUNDARY ABOUT DIRECTION**, and that is the whole design: a function taking facts about a patient and RETURNING a pathway is software deciding what care someone should have (regulated CDSS, outside what this product is); a function taking a pathway you have ALREADY CHOSEN and reporting whether its recorded criteria are met is bookkeeping about a record. `evaluatePathway` takes the pathway as its FIRST ARGUMENT and never returns one — which is why the unit's export-list test is sufficient: the only way to smuggle selection in is to add such a function. Backed by two things a naming rule alone would miss (it would pass `computeCarePlan(facts)`): a compile-time assertion that no export returns a pathway, and a fact vocabulary with NO member a symptom could be recorded under (W57's closed union, same reasoning — an inferential member is not a slippery slope, it is the thing itself). **THE PROPERTY DOING THE REAL WORK: "NOTHING RECORDED" IS NOT "RECORDED ABSENT."** A practice's record is authoritative about what the PRACTICE HAS RECORDED, not about the patient, so `recorded_absent` (somebody looked and wrote it down) is a different state from having no fact at all. Collapsing them would let SILENCE BECOME A CLINICAL CONCLUSION — a patient nobody has assessed would satisfy every "absent" criterion and quietly clear an exclusion designed to keep them out. That is W111's no-inference-from-absence applied where the cost is higher. So the verdict has THREE outcomes and NO BOOLEAN (a two-valued answer must fold `unknown` into one side, and whichever side it lands on is a conclusion drawn from silence), and `cannot_determine` NAMES the missing fact codes, turning an unanswerable evaluation into a list of things to go and record. Decision order is argued, not incidental: a definite EXCLUSION ends it even with unknowns elsewhere (the unknowns could only ever have INCLUDED them), but an unknown exclusion does NOT — not knowing whether someone should be kept out is not permission. Escalation is reported as a flag on the PATHWAY, never as advice, and an unknown escalation blocks a determination like any other. Verdicts carry `notAClinicalRecommendation: true` as a LITERAL type (W58: the disclaimer travels with the value) and name the exact version by HASH not ordinal. Takes W119's `UsablePathway`, so G5 is enforced by the ARGUMENT TYPE rather than re-checked. Self-caught and worth recording: I wrote the fixture's exclusion backwards (`requires: "absent"` for a rule meant to fire on presence) — three tests failed loudly, but had that fixture been the only reader every one would have PASSED with the criteria behaving exactly inverted; the module now spells the satisfied-exclusion-excludes rule out where an author will hit it. Verify: pnpm verify green, 1323 tests / 114 files |
| W121 | done | builder-A | 2026-08-10T22:30Z | 5ff0819 | src/pathways/escalation.ts + 13 tests. W120 says an escalation criterion is satisfied by the record and deliberately STOPS there, because "and therefore do X" is a different kind of statement. Deciding what an escalation MEANS — who needs to see it, how soon — is clinical authorship, so the mechanism ships and `SHIPPED_ESCALATION_RULES` is empty, pinned by a test (W55/W56/W60/W68/W114 pattern). **THE HALF WORTH ARGUING IS WHAT ZERO RULES MEANS.** W68 established that a safety rail producing SILENCE is worse than no rail, and an empty rule set is exactly where that trap springs: if an unmatched escalation simply produced nothing, this whole module would be A NO-OP THAT LOOKS LIKE IT IS WORKING, and nobody would find out until the rules landed. So an escalation with no rule comes back `unrouted`, naming the pathway and the trigger — "this fired and nobody has said what to do about it" — which is the honest state while G5 is pending and precisely what a practice should see. A test asserts that with the SHIPPED (empty) set every fired escalation is unrouted, so the emptiness gate and the no-silence property are checked together rather than separately. **THE ACTION VOCABULARY IS OPERATIONAL, NEVER CLINICAL**: it says who gets told and what gets flagged, never how sick anybody is or how fast they should be seen; closed two-value union, and tests assert neither the vocabulary NOR the operator-facing copy contains urgency language. **NO TIMEFRAME FIELD, DELIBERATELY** — a timeframe attached to a clinical escalation is a judgement about URGENCY, and that judgement belongs in content the founder signs off rather than in a shape this module invents (W114 drew the same line for what a credential permits); a test enumerates the rule's keys so one cannot be added quietly. Rules are pinned to a version HASH, so editing a pathway invalidates its escalation rules exactly as it invalidates its attestations (W118/W119) — a rule written against criteria that have since changed is a rule NOBODY REVIEWED — and that case is distinguished from "no rule at all" because the fixes differ: one needs a rule written, the other needs re-review. That test is itself guarded by asserting an edit really does change the hash, so it cannot pass on a hand-written wrong value instead of on the mechanism. Takes W120's VERDICT rather than facts, so it cannot become a second evaluator. Verify: pnpm verify green, 1336 tests / 115 files |
| W122 | done | builder-A | 2026-08-10T23:00Z | d534606 | src/pathways/diff.ts + 12 tests. A versioned clinical document that cannot be diffed is a document NOBODY RE-REVIEWS: the specialist who signed off version 3 will not read version 4 end to end, they will ask what moved, and if the answer is "here is the whole thing again" then the second review is shallower than the first and the sign-off records a check that did not really happen. **THE GATE IS STRONGER THAN IT SOUNDS** — a diff is the artefact a reviewer forms an opinion FROM, and an unattributed change is one nobody can question ("who decided this and when" is the first thing a clinical reviewer asks and the last thing a diff format usually carries). Provenance is REQUIRED BY THE TYPE and taken from the after-version's OWN draft record rather than passed in, so a diff cannot be attributed to somebody who did not draft it; `renderDiff` THROWS rather than emitting a document with a gap in it (W67: a boolean nobody reads is not a gate, and neither is a placeholder — an "unknown author" line would still be read). Tested from both directions: a compile-time assertion that the diff cannot be constructed without provenance, and a runtime refusal for the case that actually matters, a diff REHYDRATED FROM STORAGE with no compiler standing over it. **TWO JUDGEMENTS ABOUT WHAT COUNTS AS A CHANGE.** A rationale-only edit is a REAL change, rendered with both texts — easy to show as "no functional change" and wrong, because the rationale is the part a clinical reviewer actually reviews and W118 hashes it, so it genuinely is a new version. A criterion MOVED BETWEEN LISTS appears TWICE, as a removal and an addition, because moving a rule from inclusion to exclusion INVERTS what it does to a patient and one line would invite that to be skimmed as a tidy-up. When nothing changed it SAYS SO rather than rendering an empty section: those are different claims and the second reads as "nothing important". Golden covers the whole rendered document, so the wording a clinical reviewer reads cannot drift without a deliberate edit. Verify: pnpm verify green, 1348 tests / 116 files |
| W123 | done | builder-A | 2026-08-10T23:35Z | 5914b37 | src/pathways/binding.ts + 15 tests. W119 says a pathway may be USED at all; W120 says whether a record MEETS its criteria. Neither says WHO MAY DELIVER IT, and that question has a different failure mode: offering a pathway under a clinician who was never meant to carry it is not a bookkeeping error, it is the product routing care to the wrong person. **NO BINDING MEANS NOBODY, NOT EVERYONE** — a pathway with no binding returns `no_binding`, NOT an empty eligible list and certainly not the whole panel. Those are THREE different claims and only one is true; an empty list would read as "we checked and nobody qualifies", a statement about the CLINICIANS rather than about the absent DECISION. `authorize()`'s deny-by-default and W120's `cannot_determine`, applied where the cost of guessing is highest — and shipping zero bindings (G5: what experience or credential a pathway requires is clinical governance) is only SAFE because absence fails closed, making pathways undeliverable rather than universally deliverable, which is the right way round for an undecided question. A stale binding (written against another version hash) is distinguished from no binding, because the fixes differ. **IN-PANEL ONLY, BY CONSTRUCTION**: candidates are assembled from rows ALREADY filtered to the practice and `clearsFloor` is passed the practiceId as well, so a foreign record is ABSENT FROM THE COMPUTATION rather than filtered out of the answer — W91/W103 found that distinction the hard way, since a filter is a line somebody can delete and the deletion looks like a simplification. A clinician known only at another practice appears in NEITHER list: being listed as "excluded" would still disclose that someone by that id exists elsewhere. **THE RESULT IS A SET, NOT A RANKING** — W82 owns ordering by capability and is deliberately unused, because an ordered list of clinicians for a clinical pathway is a recommendation about who is BETTER, which is not this product's to make; output is id-ordered, the copy says so, and a test asserts no rank/score/order key. Interest alone never clears the floor (W79: what a clinician SAYS is not what someone verified). Self-caught: the seed helper called `putExperience` per clinician, which is a bulk REPLACE not an insert, so only the last seed survived — the isolation test was passing because practice A had NO ROWS AT ALL, i.e. trivially no leak. Fixed to append, and the helper now explains why. Verify: pnpm verify green, 1363 tests / 117 files |
| W124 | done | builder-A | 2026-08-11T00:10Z | 0d56605 | src/pathways/simulation.ts + 15 tests. **THIS CANNOT ANSWER WHETHER A PATHWAY IS ANY GOOD** — the cohort is INVENTED, and a distribution of verdicts over invented patients says nothing about medicine. What it answers is a question nothing else in the stack can: **HOW OFTEN CAN THE PATHWAY BE DECIDED AT ALL?** W120 returns `cannot_determine` when the record says nothing either way, so a pathway leaning on facts practices do not routinely record will mostly return "we do not know" — a DATA AVAILABILITY finding, not a clinical one, invisible until you run it over a population, and worth knowing BEFORE a specialist reviewer spends an afternoon on the content. **THE COHORT'S RATES ARE ASSUMPTIONS, NOT PREVALENCE**: writing "30% of patients have fact X" into a generator would be a clinical claim about how common something is, made by a build unit — so rates are caller-supplied, defaulted to an OBVIOUSLY arbitrary 0.5 rather than a plausible-looking number (a number that looks like a real rate invites somebody to quote it as one), and carried into the output labelled as what they are (W99's rule, where the misreading would be clinical rather than commercial). **DISTRIBUTIONAL ONLY**: no per-patient output and no type that could hold one — a simulated patient's verdict is meaningless and a real one's would be a clinical statement about a person; asserted on the report's key list and on the serialised payload, W115's move against per-clinician tallies. Determinism tested THREE ways, because same-seed-same-answer alone would hold for a function that ignored its input: byte-identical on repeat, genuinely DIFFERENT on a different seed, and independent of how many simulations ran before it (a module-level RNG would make the first run of a session differ from the fifth). Self-corrected: the no-clinical-claim test first banned BARE WORDS including "safe", and the caveats legitimately contain it in the sentence saying the report does NOT speak to safety — a word list is a PROXY for "makes no claim" and the proxy was wrong, so it now checks ASSERTIVE PHRASINGS (which also catches "is unsafe", a positives-only list would miss it), scoped to the findings with the caveats block removed because a disclaimer must NAME what it disclaims; the caveats are pinned verbatim by the adjacent test, so every part of the document is covered by one check or the other. Verify: pnpm verify green, 1378 tests / 118 files |
| W125 | done | builder-A | 2026-08-11T00:45Z | 58ddcbc | src/pathways/consent.ts + 28 tests. Everything else in this stack decides what the PRACTICE may do; this is the one place the PATIENT's decision lives, and where a convenient default does the most damage — **a system that treats silence as agreement has not obtained consent, it has recorded that nobody said no, which is a different fact wearing the same word.** **THERE IS NO BOOLEAN**: a `consented` flag has a default and whichever it is, it is wrong (false quietly blocks care that WAS agreed to; true is the failure this unit exists to prevent), so a record exists or it does not and the query returns given/refused/withdrawn/not_recorded. A test asserts no field on the record is a boolean at all. **`not_recorded` IS NOT `refused`** — a refusal is a decision the patient MADE, to be honoured and visible rather than re-asked as though nothing happened; nothing recorded is the ABSENCE of a decision, and collapsing them loses the difference between "they said no" and "nobody asked", which are OPPOSITE instructions to whoever picks the record up next. The two absences are themselves distinguished: consent on file for an earlier version means "re-consent to the change", nothing at all means "consent from scratch". **CONSENT IS TO A VERSION HASH, NOT TO A NAME** (same rule as W119 attestations, W121 escalation rules, W123 bindings): consent that survives an edit is consent to something THE PATIENT NEVER SAW. `statement` — what was actually put to the patient — is required and non-empty, because a consent record that cannot say what was agreed to is a signature on a blank page; `recordedBy` required, because a decision with no recorder is hearsay. `assertConsented` THROWS (W67: a boolean nobody reads is not a gate) and EVERY status other than `given` throws, including `withdrawn` — no "close enough". Withdrawal produces a NEW record rather than mutating, so the period the consent stood is not erased, and re-consent is a fresh act. `ConsentRecord` is branded so `recordConsent` is the only producer, and an export-list test bans any name suggesting default/implied/assumed/inferred/presumed. **"No default consent" tested as an EXHAUSTIVE PROPERTY over all 64 subsets of the fixture set**, not by example, with a loop-count guard so an empty search cannot pass silently. Self-caught: the positive control had to be dated LAST to mean anything — the fixtures include a withdrawal and latest-wins correctly reports that as `withdrawn`, so an earlier-dated control would have failed for the RIGHT reason while looking like the property was broken. Verify: pnpm verify green, 1406 tests / 119 files |
| W126 | done | builder-A | 2026-08-11T01:20Z | 04d65b6 | src/pathways/audit.ts + 19 tests. Q10 built a governance chain across FIVE modules, each holding the piece it needs — right for the code and wrong for an AUDITOR, whose question is never "what does approval.ts think" but "what happened to this pathway, in what order, and who did each part". **USES W10'S SPINE RATHER THAN IMITATING IT**: `verifySequence`/`verifySequenceAgainst` were generalised over anything carrying a `seq`, so the pathway trail is checked BY W10's code — an integrity check that exists twice gets fixed ONCE and the copy then quietly guarantees less (W114's reasoning, reaching the real rules instead of duplicating the patterns), and a test asserts both paths return the SAME verdict so they cannot drift. Truncation is caught only against an ANCHOR, exactly as W10 documents: a test shows a tail-truncated trail passing the contiguity check and failing the anchored one. **REPLAY ASSERTED OVER EVERY PREFIX**, walked one event at a time, so a transition replay silently DROPS shows up as a state that never changed (W110's discipline: a fold only correct at the end is wrong the first time an auditor asks what the state was in March). **REVOCATION AND WITHDRAWAL MARK RATHER THAN DELETE** — the review really did happen, and an audit that cannot see a WITHDRAWN sign-off cannot answer why a pathway stopped being usable; revocation targets the LIVE attestation of that kind, so a re-sign-then-revoke sequence records both. An event about a version never seen drafted still CREATES A ROW: a trail that silently ignored an orphan would hide exactly the anomaly an auditor is looking for. Every event requires `at` and `by`, refused otherwise (W122: a change nobody can attribute is a change nobody can question), plus an out-of-order date refusal because a trail whose order does not match its dates cannot answer "in what order". **CARRIES NO PATIENT IDENTITY**: W125's consent records name a patient and this is a CONTENT governance document a specialist reviewer, regulator or indemnifier may read — patient ids here would make every reader of a pathway's history a reader of WHO AGREED TO IT, the privacy-shaped mistake W110 refused when it declined to file credentialing history on the invitation spine. Consent keeps its own record attached to the patient, which is where a consent question is actually asked. Verify: pnpm verify green, 1425 tests / 120 files |
| W127 | done | builder-A | 2026-08-11T01:55Z | 520655e | app/console/pathways + src/pathways/registry.ts + 7 e2e tests. **THE GATE'S SECOND HALF IS THE WHOLE PAGE**: "zero signed at ship, and the dashboard SAYS SO rather than rendering empty". An empty table reads as a LOADING FAILURE, or as a feature nobody has used yet — neither is true, and those readings lead a practice to OPPOSITE next actions. Nothing is signed off because signing clinical content off is a founder act that has not happened, so the zero state names both misreadings explicitly ("not a loading error", "not a page waiting for you to fill it in") and explains what would have to happen: TWO PEOPLE, TWO SEPARATE STEPS. **SHOWS GOVERNANCE, NEVER CONTENT** — criteria are summarised as COUNTS and never listed, because a pathway's inclusion/exclusion rules ARE clinical content and rendering unsigned clinical material in a console with the product's name on it is precisely what G5 exists to prevent; an e2e asserts no seeded fact code reaches the page. Each refusal renders W119's OWN sentence rather than a generic "not available", so "reviewed and waiting on founder sign-off" is visibly a different situation from "nobody has looked" — tested as its own state. The registry seeds from `SHIPPED_PATHWAYS`/`SHIPPED_ATTESTATIONS`, both empty and pinned by their own units, which is the load-bearing fact: the running product's catalogue starts at zero BECAUSE the shipped one is zero, so the dashboard's real state and its nothing-signed-off state are THE SAME STATE rather than a placeholder standing in for one — an e2e reads the registry in its SHIPPED condition (not the seeded-empty one) and asserts both lists are empty. Deliberately NOT practice-scoped: a pathway is a document, not practice data, and per-practice copies would mean N versions of a clinical document with no single answer to "what was in force in March"; which clinicians may be offered one is W123's question and is scoped there. Mock seed uses obviously-placeholder fact codes and rationales that say so, since seeding a real pathway would put clinical content in the tree through a side door. Joined the three tree-checked registries (W51 resetters, W106 record classes, W102 census). Verify: pnpm verify green 1425 tests / 120 files; full e2e 86/86; axe zero violations with the page scanned POPULATED |
| W128 | done | builder-B | 2026-08-10T11:10Z | 73f899e | Withdrawal in src/pathways/versioning.ts + the W119 half in approval.ts + 17 tests. W118 left this out deliberately; adding it forced a shape worth naming — **PUBLICATION IS AN EPISODE, NOT A FACT**. W67's withdrawal is terminal for that approval RECORD and re-approving "opens a new record, the withdrawal stays as history", so here withdrawal ends the current episode and publishing again starts another; the content stays immutable and hash-identified, and what has a lifecycle is its TIME IN FORCE. Episodes are not decoration — `pathwayAt` reads them: a version withdrawn in May and republished in August WAS in force in April, was NOT in June, and IS again in September, which a single publishedAt/supersededAt pair cannot represent (all three instants asserted). Immediate (leaves force at the recorded instant, and withdrawing today does not make March look empty); terminal for the episode (withdrawing twice refused; the closed episode never written to again, asserted by republishing AND superseding then checking the first episode still reads "withdrawn" with its original reason). A REASON IS REQUIRED — without one the next person cannot tell a safety withdrawal from a tidy-up, and they need to know before republishing. One arbitrary special case removed: a version is republishable whenever it is not currently in force, by withdrawal OR supersession, since reverting is a real need and allowing one but not the other would be a rule with no argument. **THE W119 HALF IS WHAT MAKES "A FRESH ACT" REAL RATHER THAN NOMINAL**: attestations predating a withdrawal no longer count, so a pathway withdrawn for safety cannot be put back in force by one person and be instantly usable on signatures given before anyone knew there was a problem; both stages must be retaken (a half-fresh pair is refused). Deliberately NOT "withdrawal revokes the attestations" — revocation is somebody's act with their name on it, and inventing one would put a revocation in the record that nobody performed. Writing it surfaced a REAL ORDERING BUG: the picker took the earliest live attestation and checked the boundary AFTERWARDS, so a stale signature masked a genuinely fresh one and a re-attested pathway reported unusable; the filter now runs BEFORE the choice, mutation-checked (removing it fails two tests). Also caught in my own test: passing `undefined` to a parameter with a DEFAULT silently supplies the default, so the missing-reason case was asserting the opposite of what it claimed — built inline now. Sibling fixtures (escalation/evaluation/simulation/diff/binding) updated mechanically for the four new fields. Verify: withdrawal immediate and terminal, re-publication a fresh act; pnpm verify green on the merged tree, 1442 tests / 121 files; axe 4/4 incl. builder-A's new /console/pathways |
| W129 | done | builder-A | 2026-08-11T03:50Z | bb06c0a | docs/HARDENING-Q10.md + 2 fixes + 8 tests. **TWO FINDINGS, BOTH MINE, ONE CRITICAL AS DESIGNED.** (1) **A WITHDRAWN CONSENT COULD READ AS `given`, DEPENDING ON ARRAY ORDER.** `consentFor` took the record with the greatest `decidedAt` and checked THAT ROW's `withdrawnAt` — but `withdrawConsent` returns a NEW record with the SAME `decidedAt`, which is not a misuse but the DOCUMENTED way to use it (chosen so withdrawal does not erase the period the consent stood). A set holding both copies — the natural contents of an append-only consent store — therefore resolved BY ARRAY ORDER. Reachable through the module's own documented usage, and the failure direction was the worst available: W125's entire argument is that consent is never inferred and absence is not permission, and a withdrawal silently reading as AGREEMENT is the exact inverse of that property, shipped inside the unit that argues for it most loudly. Not live (no store, no consumer) — timing, not design, which is the only reason it is written up as "critical as designed" rather than as an incident. Fixed by stating the rule over the WHOLE SET rather than one row: **a withdrawal suppresses every decision made at or before it** — order-independent, no deduplication, and re-consent still works because a later decision is not suppressed; same-day re-consent now reads as withdrawn, since day-granularity dates cannot order two events within a day and refusing to guess is the safe direction. (2) **THE AUDIT TRAIL DISAGREED WITH THE THING IT AUDITS**: W126's state model omitted `superseded` (three members against W118's four), so publishing v2 left v1 reading as IN FORCE in the very document written to be the authoritative account — an audit trail that disagrees with the system it audits is worse than no audit trail. Fixed by DERIVING supersession during replay rather than adding an event kind, so nobody has to remember to record it (W112's reasoning for expiry), with a test that withdrawing the current version does NOT resurrect the previous one. Checked clean: W128's withdrawal against the whole chain, version-hash pinning across W119/W121/W123/W125 as a SET (the value is in their agreeing), fail-closed-on-absence re-read in all four modules, the G7 direction rule across every Q10 module, no patient identity outside consent, determinism in W122/W124. **METHOD NOTE — Q9's lesson in a new shape**: both findings were invisible because EVERY TEST EXERCISED A SINGLE INSTANCE of the thing (one consent record, so ordering never arose; one version, so supersession never arose). Neither test was careless; the gap was that none asked what happens when there are TWO. New habit named: for any module folding a collection into one answer, TEST TWO, IN BOTH ORDERS — four of the six Q9/Q10 findings would have been caught by that rule alone. Verify: pnpm verify green 1476 tests / 122 files; full e2e 86/86 |
| W130 | done | builder-A | 2026-08-11T02:35Z | 9c997bd | docs/GATE-DOSSIER-Y3.md. In the Y2 dossier G5 was one ask among three; Y3 built TWO QUARTERS of machinery on top of it, all shipping empty by design, so this traces what the ruling RELEASES and in what order. Deliberately narrow again — does not restate G0–G4/G7 (Y2 dossier) or G6/credentials (Q9 dossier), because a document repeating unchanged gates trains its reader to skim. **EIGHT constants are empty FOR G5** and pinned at zero by their own tests; **one is NOT empty and is called out rather than left for someone to find** — `SHIPPED_TRIGGERS` carries a single rule routing a free-text patient reply to their usual GP, which is a routing rule about a MESSAGE not clinical content about a CONDITION, and it is listed so the founder can disagree while it is one line. Inventory READ FROM THE TREE not from memory, which caught my own count being off by one (MEHERR_STAFF is empty for a different reason and is marked as such). **THE SECTION A GATE DOSSIER USUALLY OMITS IS WHAT HAPPENS IF THE ANSWER IS NO**, and it is the part that matters for planning: nothing breaks, because several mechanisms were designed so that EMPTY FAILS CLOSED (W123 returns `no_binding` rather than an empty eligible list; W121 returns every escalation as `unrouted` rather than doing nothing silently) — the product today is not a broken version of the product with content, it is a COHERENT PRODUCT THAT DOES LESS. Q11 does not depend on G5 at all and can proceed in full. What degrades is CONFIDENCE in machinery verified only against synthetic content, and the longer the ruling waits the more accumulates behind an untested assumption. **TWO THINGS SEPARATED THAT ARE EASY TO CONFLATE**: SUP-2 needs G5 **plus** a company-direction decision (whether Meherr is willing to be a party to individual clinical care), and ruling on G5 must NOT be read as answering it — doing so would answer the harder question by accident; and the Ahpra advertising review of `/` and `/finder` has now been asked in THREE CONSECUTIVE DOSSIERS (Q3, Y2, Q9) without being actioned or declined, which is a fact about a control everyone assumes someone else is tracking — "prototype only" is a complete answer and one ledger line closes it permanently. Also records that W131 can be built to the SAFE INTERSECTION of the pending A/B credential-visibility ruling (a referral document with no credential field is valid under either), which moves that deadline to W133 — a reprieve, not a resolution. Six founder actions, each with what it blocks. Verify: pnpm verify green, 1425 tests / 120 files |
| W131 | done | builder-A | 2026-08-11T03:10Z | 94d4e4f | src/referrals/document.ts + 26 tests. **THE GATE IS NOT "NO FREE TEXT", IT IS "NO CLINICAL TEXT MEHERR WROTE."** G5 governs content the PRODUCT publishes — pathway criteria, recall intervals, safety rules — because that carries Meherr's implicit endorsement and nobody here is qualified to write it. A GP writing about their OWN patient is not that: it is practitioner-to-practitioner communication they are professionally responsible for, and a product refusing to let a doctor write a sentence about their patient would be wrong in the OTHER direction. So exactly ONE field carries prose, it must be ATTRIBUTED to a named clinician at a stated time (W122: the attribution is what makes an ungoverned field acceptable at all), and the module contains NO generator and NO template a system could fill in — asserted on the export list and on the set of exported string tables. That is the bypass the gate closes: system-generated clinical wording travelling inside a referral would be Meherr content that never passed W119's sign-off, WEARING A GP'S NAME. The module deliberately does NOT edit or validate the clinician's wording (tested with deliberately odd text), because rewriting a doctor's note is the opposite failure. **FACTS ARE REFERENCED BY CODE, NEVER RESTATED** — restating a recorded fact as prose turns a bookkeeping entry into a clinical assertion somebody must stand behind, and loses the link to the record it came from. The `ReferralReason` vocabulary is OPERATIONAL (extended_scope/capacity/continuity/patient_preference — why a PRACTICE is asking) and a test asserts no member mentions diagnosis, symptom, severity or urgency. Self-referral refused: a referral to yourself is not a referral, and recording one puts a second copy of the patient's care in a rail designed to hand it over. **BUILT TO THE SAFE INTERSECTION OF THE PENDING A/B RULING** (docs/GATE-DOSSIER-Q9 action 1, escalated in W130): the document carries NO credential/capability field and names NO receiving clinician, only a receiving PRACTICE — this shape IS what a referral can carry, so a credential field added before the ruling would MAKE that ruling by default, while a document without one is valid under either answer. A source-reading test asserts the omission is DOCUMENTED, because a missing field is indistinguishable from an oversight unless somebody wrote down that it is deliberate. The decision moves to W133, where choosing a clinician at the far end actually happens. Verify: pnpm verify green, 1451 tests / 121 files |
| W132 | done | builder-A | 2026-08-11T04:25Z | 19f0249 | src/referrals/return-report.ts + 25 tests. W93's worst leakage stage is `attended_no_completion` — seen, and nothing came back. This is the thing that comes back. **A RETURN REPORT IS NOT A LETTER**: a referral loop where BOTH practices believe the other is following up is the classic harm in GP-to-GP care, and it is a harm of AMBIGUITY rather than of error — nobody did anything wrong and nobody is watching the patient. So the safety-critical field is not the narrative, it is WHO HOLDS ONGOING CARE, and it is **DERIVED from the outcome rather than recorded beside it**: two fields that can disagree eventually do, and "seen, care returned to you" next to "receiving practice retains" is worse than either alone because a reader has to GUESS and the guess decides whether anybody follows the patient up. One field is recorded; the other is a function of it, so the contradiction is UNREPRESENTABLE rather than validated — a test asserts the report carries no field that could disagree, and that every outcome has an answer so no default is needed. **THE SHARPEST EDGE IS `not_seen`: IT PRODUCES NO COMPLETION EVENT.** A report saying the patient never attended is a REAL return report — the receiving practice did their part by telling somebody — but the loop is the patient being SEEN, and emitting a completion would move the referral to `completed` and DELETE IT from the exact report a practice uses to find people who fell through. `not_seen` also returns care to the referring practice, since an appointment that did not happen transfers nothing. The gate is tested THROUGH W93's own replay rather than by asserting on the event shape, which would pass even if the two modules disagreed about what a completion looks like; and the completion is filed under the REFERRING practice, since W93 replays per practice and an event under the receiving one would close nothing in the report that exists. Per W129's method note the fold is tested with TWO returns in BOTH orders. Date coherence refused both ways (seen-without-date, not-seen-with-date, seen-before-referral). Narrative follows W131 unchanged: one prose field, attributed, and nothing here generates or templates clinical correspondence — a report with no narrative at all is valid, because the STRUCTURE carries the safety-critical part. Verify: pnpm verify green, 1501 tests / 123 files |
| W133 | blocked | — | — | — | **FOUNDER DECISION — Q9 action 1, recorded in docs/GATE-DOSSIER-Q9.md — not buildable by any builder. W195 CORRECTED THIS ATTRIBUTION FROM FOUNDER GATE G6**, which does not block it: G6 is the public directory launch (Ahpra advertising review of profile copy), and this unit routes IN-NETWORK ONLY and renders nothing public. `docs/GATE-DOSSIER-Q9.md` is explicit — action 1 *"Blocks: W131, W133, W137"*, action 5 *"Blocks: G6 itself"*. The unit stays blocked either way; what changes is that **the tree's most time-critical open decision is no longer hidden behind a gate nobody is scheduling.** [P] Referral routing to an extended-scope GP, IN-NETWORK ONLY. Verify: never crosses G6; W82's floor honoured; foreign records absent | **Why blocked:** the unit routes a referral to a named clinician at ANOTHER practice, which is precisely the question docs/GATE-DOSSIER-Q9.md action 1 puts to the founder — may credential detail cross a practice boundary (option A: capability presence only; option B: detail inside a consented network)? W131 was deliberately built to the SAFE INTERSECTION (no credential field, no receiving clinician named) so the decision stayed open, and W130 escalated it as the closest thing to time-critical in the plan. Building W133 either way DECIDES the gate: option A makes the routing near-useless if the founder wanted B, and option B ships cross-boundary credential visibility before it was authorised. Unlike W56 there is no gate-safe intersection here — the unit IS the decision. Needs: the A-or-B ruling. |
| W134 | done | builder-A | 2026-08-11T05:00Z | 213cd3c | src/referrals/acceptance.ts + 15 tests. W89's hook-3 conclusion for GP-to-GP work was that a referral must TRANSFER care explicitly rather than blurring it, and **the blur has a specific shape**: a referral is sent, the receiving practice has not looked at it, and the referring GP has already stopped watching because they "referred it on" — nobody declined anything, and nobody is following the patient. **THE GATE IS ENFORCED BY THE TYPE**: `obligationsFor` takes an `AcceptedReferral`, branded and producible ONLY from a status that is actually `accepted`, so a caller who BELIEVES a referral was accepted cannot construct one and no surface can put a patient on a receiving practice's worklist on the strength of having SENT something. Checked exhaustively over every reachable state (only `accepted` yields obligations) and at compile time that there is no overload taking a status or an id. **SILENCE IS NOT ACCEPTANCE** — deliberately no timeout converting an unanswered referral into an accepted one; it stays `awaiting_acceptance` however old it is, which is uncomfortable and correct, because the discomfort IS the signal and W93's leakage report is where it surfaces. **DECLINING IS AN ACT, NOT AN ABSENCE**: recorded, attributed and reasoned, and a different status from never having answered — W125's `refused`-vs-`not_recorded` distinction, because they are opposite instructions to the referring practice. `careHolderWhile` answers for EVERY state, never returns "receiving practice alone" before acceptance, and there is no state in which the answer is nobody. Obligations are a closed OPERATIONAL vocabulary about what the software does (worklist, outstanding return report) with a test that no member reads as clinical — the word "obligation" invites the other reading, so it is checked rather than assumed. **THE FOLD IS ORDER-INDEPENDENT BY CONSTRUCTION** (acts sorted by date; terminal acts checked against the whole set), tested over four orderings — W129's finding applied BEFORE it could recur, since a consent withdrawal was lost to array order there and an acceptance is the same shape of fact. Verify: pnpm verify green, 1516 tests / 124 files |
| W135 | done | builder-A | 2026-08-11T05:35Z | bd60d6f | src/referrals/tracking.ts + 16 tests. W93 replays the CLINICAL chain and W134 the PROFESSIONAL one, and a practice asking "where is this referral" wants both at once. **COMBINING TWO STATE MACHINES IS EXACTLY WHERE INFERENCE CREEPS IN**, because a combiner is under constant pressure to produce ONE TIDY ANSWER — three temptations, all refused. **(1) FILLING A GAP**: with no acceptance acts the referral is NOT assumed accepted because the clinical chain shows an appointment — it is `unknown`, and `unknown` is a REPORTED value not a missing one (and a different claim from `not_sent`: one says nobody recorded anything, the other says a decision was recorded). An appointment is evidence somebody BOOKED something, not that a practice TOOK THE PATIENT ON — so care stays with the referrer, because an unrecorded handover is not a handover. **(2) RESOLVING A DISAGREEMENT**: when a return report closes a referral nobody recorded accepting, this reports the CONTRADICTION and keeps BOTH machines' answers visible rather than picking a winner — a combiner that silently prefers one machine hides the one thing worth looking at, and every disagreement here is a real workflow fault somewhere. Also flags a patient seen on a referral the receiving practice DECLINED. **(3) TURNING WAITING INTO A CONCLUSION**: silence becomes an outstanding item naming the PARTY and the DATE, never a status — "waiting on the other practice since February" is actionable, "stalled" is a verdict nobody recorded; and when nothing is outstanding it produces NOTHING rather than "on track", which would be a conclusion too. A cancelled appointment puts the rebook back on the REFERRING practice, since the receiving one has nothing booked and no obligation to chase. The no-inference property is tested over the CROSS PRODUCT of both machines (20 combinations, with a count guard so an empty loop cannot pass) rather than on examples, and order-independence is checked on BOTH input sets (W129's rule). Verify: pnpm verify green, 1532 tests / 125 files |
| W136 | done | builder-A | 2026-08-11T06:10Z | 09cd404 | src/referrals/outreach.ts (new refusal) + src/referrals/loop-closure.test.ts, 8 tests. **W95 ALREADY WITHHELD A COMPLETED REFERRAL — AS `stage_not_actionable`**, alongside `not_written` and `referral_cancelled`. True, and MISLEADING: it filed the outcome the whole rail exists to produce under the same reason as a chain nobody can act on, so a practice reading the withheld counts could not tell SUCCESS from a GAP. This unit is that distinction, and the gate's emphasis is on the REASON rather than on the silence. `loop_closed_by_return` is checked BEFORE every other rule, so the reason a practice sees IS the completion, and it counts in its OWN bucket — a reason that shares a bucket with another is a reason nobody can act on differently (asserted: with a completed chain, `loop_closed_by_return` is 1 and `stage_not_actionable` is 0, and it is the only non-zero count). **CLOSURE IS CHECKED AGAINST THE RETURN REPORT AS WELL AS THE DERIVED STAGE**, and the difference is the plumbing case that matters: `completed` is DERIVED from a `completion_recorded` event, so a return report written but not yet folded into the chain would leave it looking OPEN and the patient would be chased about something finished — whichever arrives first stops the nudge, tested by planning against a chain the leakage replay still reports as `written_no_appointment`. W132's sharpest edge carries through: a `not_seen` return produces NO completion event and does NOT close the loop, so the patient is still chased — correctly, because the loop is the patient being SEEN. Closes only the referral that came back, not the patient's others. W95's own test asserted the old reason; UPDATED rather than silently flipped, with a comment saying why the expectation changed. Self-caught: I invented a `ContactPreferences` shape instead of reading the real one and three send-path tests failed — the CONTROL test ("still sends when the chain is genuinely open") is what made it visible, since a module that withheld everything would have passed every other test in the file. Verify: pnpm verify green, 1540 tests / 126 files |
| W137 | done | builder-A | 2026-08-11T06:45Z | d5bdf06 | app/console/referrals + src/referrals/store.ts + 11 unit + 12 e2e tests. A referral has TWO practices with DIFFERENT JOBS — the sender chases an outcome and can act on almost nothing; the receiver holds the one decision that MOVES it — so this is two lists fed by two SEPARATE reads with the controls only on the receiving side. Not one read with a flag: a `bothSides` boolean is one mistaken argument from showing a practice a list it should not act on (W113's reasoning). **CROSS-PRACTICE ISOLATION BY CONSTRUCTION** (the practice id is the QUERY, not a later filter): a third practice's referral appears on NEITHER list, asserted in the browser, while the two-practice case is unit-tested because a second practice is not reachable from one browser session and driving it there would be theatre (W83). **BUILT TO THE SAFE INTERSECTION of the pending A-or-B ruling**, like W131: no credential or capability detail about anyone, and the page says so — adding it before the ruling would make the ruling by default. **THE PART WORTH RECORDING IS WHAT W106'S REGISTRY DID**: declaring this store as holding patient identity FAILED its test, because the pinned list of classes erasure must reach had not grown — and it had not grown because **erasure did not reach the referral rail at all**. That is W51's original finding working AS DESIGNED rather than as a formality: I could not declare the obligation without meeting it. `deletePatientEverywhere` now composes a rail scrub, the deletion record counts it, and referral ROWS are DROPPED rather than scrubbed — an appointment without a patient is still slot history, but a referral without a patient is only a note that one practice once asked another about somebody, and its acts/events/return go with it because a chain about a deleted referral is a dangling obligation nobody can act on. Answering is refused for a referral not sent here, with "not yours" and "does not exist" answered identically; a second answer from a stale page is refused; a decline without a reason half-records nothing. Self-caught: the credential check repeated W124's trap — the banned-word list flagged the NOTE that promises no qualifications are shown — scoped to the list rows where a leak would actually appear, with the note's wording pinned separately so both parts are covered. Verify: pnpm verify green 1551 tests / 127 files; full e2e 98/98; axe zero violations with the page scanned POPULATED |
| W138 | done | builder-B | 2026-08-10T11:40Z | b791c0e | src/compliance/party-to-care.ts + statement on /privacy + e2e sweep. W89 drew this line for specialists, W134 between practices; **neither covers whether the PRODUCT reads as a participant in care** — a different question with a different failure mode, because it fails BY WORDING, not by architecture. Three reasons it earns a linter rather than a style note: (1) a patient who believes Meherr is involved WAITS ON US for something we will never do, and the waiting IS the harm — they stop chasing their practice because they think it is handled, W134's blur one layer out; (2) holding ourselves out as providing or directing care makes us a DIFFERENT REGULATED THING with different s 133 exposure and a different answer to whose duty of care is engaged, and nobody decides to become that — you become it one sentence at a time; (3) it misdescribes WHO IS ACCOUNTABLE, since the practice is the treating entity and the sender of every message. Statement written ONCE and rendered, never paraphrased (W23 `LANDING_COPY` pattern — a claim restated in four places is four claims). **The linter looks for MEHERR AS THE SUBJECT OF A CARE VERB, not for clinical words**, which appear legitimately all over a healthcare product: "your GP will review the results" PASSES, "we will review your results" does NOT. Both halves tested — 13 violations caught, 14 correct sentences left alone — because the W23 `no-ratings` rule has blocked correct wording three times and is documented as miscalibrated; a rule that taxes correct sentences gets switched off and then protects nothing. The sweep DERIVES its route list from W102's census rather than enumerating: three coverage lists in this tree have already fallen behind their routes (dossier map twice, axe sweep once) and a hand-written list here would be the fourth. It lints what the BROWSER RENDERS, not the source, because interpolated values are exactly where a sentence about "our clinicians" would arrive from. Result: **ZERO violations across every existing surface** — a ratchet on current behaviour, not a fix. Proven non-vacuous by injecting "Our doctors will review your results" into a live page and watching the sweep catch it, plus a floor on rendered text length so an errored page cannot pass by rendering nothing. Re-run on the merged tree after builder-A's W137 landed /console/referrals — the derived list picked it up automatically, which is the point. Verify: 32 unit + 6 e2e; pnpm verify green, 1583 tests / 128 files; a11y 4/4 and privacy 3/3 after the page change |
| W139 | done | builder-A | 2026-08-11T07:25Z | e82aa7b | src/referrals/compliance.ts + 15 tests. **THE GATE'S SECOND HALF IS THE DESIGN**: W6's rules APPLIED, not re-implemented. A second copy of the banned-language rules is WORSE THAN NONE — it starts identical, drifts silently, and the drift is invisible until somebody compares two files nobody has reason to open together. W114 split `lintMessageText` out of W6 so the rules could be REACHED; **this is the second consumer, which is when a seam like that either holds or turns out to be wrong.** It held: three linters delegated (W6 message rules, W23 landing rules, W66 relational condition-leak), none restated, and the wiring is tested AS WIRING — every rule name W6 and W23 can report must be reachable here, each delegated linter proven live by a string ONLY it catches, and the union deduplicated. What this unit ADDS is what no existing linter covers, because a referral has a SECOND PRACTICE in it: copy can tell a patient the other practice has TAKEN THEM ON (untrue until they answer — W134 — and it stops the patient chasing something still open); it can read as though an APPOINTMENT EXISTS, so a patient who needs to book does not; it can COMPARE CLINICIANS, the most regulated sentence this product could emit and the one a referral rail most invites; and it can imply MEHERR ARRANGED THE CARE, which is W89's line — the practices provide care, this moves paperwork between them. Each rule explains itself in terms of the HARM rather than the pattern, because a linter message naming a regex tells an author what to delete, not what was wrong. An empty condition catalogue is legitimate (a practice with no registers) and explicitly does NOT switch the other three linters off. `renderCompliantReferralCopy` throws (W6's posture: a list nobody reads is not a gate). Self-caught: the assumed-acceptance rule matched only the SINGULAR agreement, so "they are expecting you" — the more natural phrasing about a practice — went straight through; both forms now. Verify: pnpm verify green, 1566 tests / 128 files |
| W140 | done | builder-A | 2026-08-11T08:00Z | 43abb0f | src/referrals/scoping.ts + 11 tests + one real fix. **A SWEEP DONE ONCE IS TRUE ON THE DAY IT WAS WRITTEN** — W103's method walks the tree rather than the memory, and done as a DOCUMENT that is exactly what it produces: a list correct once and quietly wrong afterwards. So the triage is a REGISTRY CHECKED AGAINST THE TREE (W106's pattern applied to scoping): a new exported function in the referral layer fails the suite until somebody says which of three things it is. BOTH directions checked, because W102 found the STALE one is what nobody catches — a triage naming a function that has gone reads as more complete than it is. **THE SWEEP FOUND ONE REAL HIT**: `allActs()`/`allEvents()` returned the WHOLE RAIL with the console narrowing afterwards by referral id — correct in the path that existed and WRONG IN SHAPE, because the safety depended on the PROVENANCE of the referral id rather than on any check, so a caller passing an id from anywhere would have resolved another practice's acceptance state. Replaced by `actsFor`/`eventsFor`, scoped at the source to referrals the practice is party to ON EITHER SIDE (a receiving practice is party too — a rule recognising only the sender would blind the receiver, which W137's two reads exist to serve). Three answers because TWO OF THEM ARE DIFFERENT KINDS OF SAFE: `takes_practice_id` needs no trust; `scoped_by_argument` is safe BECAUSE OF THE CALLER, so those entries name what makes that true and are exposed as a set via `callerScoped()` — they are where a mistake would hide and a reviewer should read them together rather than hunt them. Two deliberate NON-scopings recorded so they do not read as oversights: erasure must reach BOTH SIDES of a handover, and `returnFor` is keyed by referral because both practices are entitled to their own referral's outcome. Behaviour tested with two practices in the store simultaneously, asserting a non-party sees nothing and that another practice's rows are ABSENT rather than filtered. Self-caught: the rationale-length check made me rewrite six entries that said things like "copy table" — true, and not a triage; if an answer is worth recording it is worth a sentence that would convince somebody who disagreed. Verify: pnpm verify green 1577 tests / 129 files; referral e2e 12/12 |
| W141 | done | builder-A | 2026-08-11T08:35Z | c2016ff | src/referrals/analytics.ts + 14 tests. W96 drew the line this inherits — the report is about the practice's follow-through PROCESS and does not rank or list patients — and a referral rail adds **a second population W96 did not have to think about**, which is exactly where somebody would want to know which practice answers fastest. **TWO ABSENCES, AND THEY ARE THE UNIT RATHER THAN CAVEATS ON IT.** (1) NO PATIENT APPEARS AT ALL — not ranked, listed, or counted in a way that identifies one; rows are STAGES and REASONS, a patient is a unit of arithmetic and never a subject, and no REFERRAL ID appears either because a row should identify a stage and not a case. (2) **NO CLINICIAN OR RECEIVING PRACTICE IS SCORED**: "which practice answers fastest" reads as neutral and FUNCTIONS AS A LEAGUE TABLE, and the first time it is quoted at somebody it becomes a reason to accept a referral they should have DECLINED. Both absences asserted on the SHAPE (key list + serialised payload), so the report cannot grow such a field without failing. Only referrals this practice SENT are counted — a referral sent here is the other practice's process, and counting it would flatter or damn this practice for somebody else's follow-through. Coverage stated with W135's distinction: referrals with NOTHING RECORDED are not "unanswered", because nobody wrote anything down — a different problem with a different fix — and `unknown` is reported rather than folded into `not_sent`. Caveats survive the complete-coverage case, which is where a report is most likely to be quoted as clean. Golden covers the whole rendered document and is asserted stable under reversed input on all three inputs. Two self-corrections: the golden was written from the DISPLAY LABELS while the tie-break sorts on the STATE KEY, so it was corrected to match the code rather than the reverse; and **W140's scoping triage — written one unit earlier — failed on this unit's two new exports**, the mechanism doing exactly what it was built for, on its author, immediately. Verify: pnpm verify green, 1623 tests / 131 files |
| W142 | done | builder-A | 2026-08-11T09:10Z | 24e3894 | docs/HARDENING-Q11.md + 2 fixes + 9 tests. **TWO FINDINGS, BOTH MINE, BOTH THE SHAPE Q10'S HARDENING NAMED** — and that is the finding. Q10 ended with "for any module that folds a collection into one answer, test two of the thing in both orders"; Q11 was written AFTER that rule existed, I applied it in W134 and W135, and both defects are still that shape, because I applied it to **the dimension I was thinking about rather than to every dimension that can tie**. **(1) HIGH — same-day acceptance resolved by ARRAY ORDER**: `acceptanceStatus` sorted by date and folded, and `Array.sort` is stable, so acts sharing a date kept input order and the last written won — accept-then-decline gave `declined`, decline-then-accept gave `accepted`. W134's own commit claims order-independence "tested over four orderings", ALL with distinct dates: **the claim was wider than the test**, and answering a referral then correcting it the same morning is ordinary administration. Fixed by making the tie-break a DECISION: day-granular dates cannot order two events within a day, so among same-day acts the one that leaves the REFERRING PRACTICE WATCHING wins (decline and hand-back beat accept) — not knowing whether a practice took a patient on must never resolve to "they did", which is W134's own thesis applied to its own tie-break; a LATER acceptance still beats an earlier decline, because that is a change of mind rather than a tie. **(2) HIGH — a corrected return report lost to array order**: `returnFor` used `.find()`, taking the FIRST match, so a report corrected and re-filed later was shown or hidden depending on storage order. A return report is CLINICAL COMMUNICATION and its outcome field is exactly what tells the referring GP whether they are still watching the patient (W132) — so the wrong copy INVERTS the answer the rail exists to settle. Fixed to latest-by-date, with two DIFFERENT reports sharing that date reported as AMBIGUOUS rather than resolved by position (W111's rule: two matches are ambiguous, never the first one), surfaced on the console as "check which one stands"; an identical duplicate is NOT a conflict, because calling it one sends a practice chasing a difference that does not exist. **SHARPENED RULE FOR Q12** (in the doc): for any fold, ask what the SORT KEY is and construct two records that TIE on it; if there is no principled winner the tie is a FINDING — pick the safe side and write down why, or report the ambiguity. The two fixes took different branches, and which is right depends on whether a safe side exists: for acceptance it does, for two disagreeing clinical letters it does not. Checked clean: W140's scoping sweep (this quarter's isolation audit, one hit already fixed there), erasure reaching the rail on both sides, no patient/referral/receiving-practice identifier in W141's analytics, no product-authored clinical text in W131/W132, W139's delegation wiring, and the acceptance gate re-verified after the tie-break change including that a same-day conflict yields NO obligation in either order. Verify: pnpm verify green 1632 tests / 131 files; full e2e 104/104 |
| W143 | done | builder-A | 2026-08-11T09:50Z | 10d3e8c | docs/GATE-DOSSIER-Q11.md. **THE DECISION Q9 ASKED FOR IS NOW A BLOCKED LEDGER ROW** — W117 said the A-or-B credential-visibility ruling was the closest thing to time-critical in the plan and that building W131 to the safe intersection would move the deadline to W133; that is exactly what happened, so this is the ACCOUNTING rather than a fresh ask. **THE G6 SECTION STATES WHY W133 HAS NO SAFE INTERSECTION WHEN EVERY NEIGHBOURING UNIT DID**: the pattern all year has been to find the gate-safe subset and build it (a loader with no values, scope machinery with no statements, a referral document with no credential field, a console showing no credential detail), and W133 is the ONE unit where that move does not exist — its content IS the decision, since a referral either shows a receiving clinician's credentials or it does not, and building either way settles the question. Under A the rail's clinical value is materially reduced if the founder wanted B; under B, cross-boundary credential visibility ships unauthorised in the exact quarter Q9 said to decide it first. Recorded so the block reads as a BOUNDARY rather than as somebody running out of road. **WHAT Q11 PRESERVED** is recorded too: `ReferralDocument` names no receiving CLINICIAN at all, only a practice, so option B is still implementable without migrating documents — the intersection work held. **THE G7 SECTION states the position as FOUR PROPERTIES THE RAIL ACTUALLY ENFORCES**, each traceable to a unit and a test: the product never SELECTS a clinician (W123 answers yes/no per clinician, deliberately not W82's ranking), never DECIDES that care transferred (W134: explicit acceptance, no timeout, no state where nobody is watching), never CONCLUDES FROM SILENCE (W135 reports `unknown` and reports disagreements rather than resolving them), and WRITES NO CLINICAL TEXT (W131/W139). My reading is that this sits inside G7's stated default and nothing needs a ruling to proceed — written down while fresh rather than reconstructed later, with the founder asked to sanity-check it. **TWO UNCOVERED ASSUMPTIONS RECORDED RATHER THAN DISCOVERED LATER**: there is no data-sharing arrangement between the two practices the rail moves patient-linked records across (APP 6 applies to the disclosing practice), and W138's responsibility posture is documented in the product but not necessarily matched in the pilot agreement — a mismatch being worse than either alone. Both are the same shape as the Y2 dossier's note about the interest register, which was theoretical until it was not. Four founder actions, and the Ahpra advertising review of `/` and `/finder` is now carried in a FOURTH consecutive dossier. Verify: pnpm verify green, 1632 tests / 131 files |
| W144 | done | builder-A | 2026-08-11T10:25Z | b9695c8 | docs/EDUCATION-BOUNDARY-W144.md. W89 pattern, and W89's reason applies with MORE force: Q12 is the first quarter where content leaves this tree to a THIRD PARTY, and the first where software would put words in front of a clinician ABOUT A SPECIFIC PATIENT. **THE SECTION THE UNIT EXISTS FOR IS THE SEVEN-STEP PATH** from "a library with a filter" to a regulated clinical service, each step individually reasonable — and **the line is drawn at STEP 4, not the obvious step 5, which is the substance**: SUPPRESSION IS A STRONGER ACT THAN SELECTION. Showing three items ordered by relevance leaves the GP the judgement; showing ONE, because the software judged the others less relevant FOR THIS PATIENT, has made a clinical judgement and hidden the evidence that it did — a GP cannot disagree with a list they were never shown. So the rule W145 needs is not only "never generate" but **"may order, must not withhold"**, and a show-more link is NOT a defence, because if the default view is one item then the default IS the product. **Step 6 is where the honest argument lives**: W120 already evaluates recorded facts against a signed-off pathway and is safe BECAUSE OF ITS DIRECTION, but rendering that same verdict inside a surface labelled "education", next to material about what to do, changes what it means to the reader — ADJACENCY IS HOW A PRODUCT IMPLIES A RECOMMENDATION WITHOUT WRITING ONE; recommended position is that the surface may say a verdict exists and link to it, never render both together. Three gates ARGUED not asserted, each naming the erosion it expects: G5's erosion is a practice handout that "does not obviously need sign-off" until half the library is unreviewed (position: one library, everything in it signed off); G7 gains a FIFTH property on top of the Q11 rail's four — **the product informs a clinician and never advises about a patient** — testable by a copy linter APPLYING W6's rules rather than re-implementing them (the W114 seam W139 proved), and explicitly NOT relying on a disclaimer, since W89 already recorded that a disclaimer does not fix a product whose shape implies otherwise. G8 gets two additions for the founder to rule on with it: de-identification must be asserted AT THE BOUNDARY rather than by each caller remembering to strip (W109's shape), and **clinician-authored FREE TEXT never leaves the tree** under any ruling short of one that names it — a referral narrative is patient-derived content by any reading and no field-stripping makes it safe. Two smaller positions recorded: the CPD trail BELONGS TO THE CLINICIAN with no practice-level "who has not read this" view, and ingested clinical content is UNTRUSTED INPUT treated as hostile by default. Four founder asks; W145/W148–W153 are buildable without any of them provided they are built to these positions. Also marked W146/W147 `blocked` — the plan says they are blocked on G8 and the ledger had them `available`, and a document asserting one thing while the ledger says another is how a gate gets walked through by somebody reading only the ledger. Verify: pnpm verify green, 1632 tests / 131 files |
| W145 | done | builder-A | 2026-08-11T11:00Z | e6d54ca | src/education/curation.ts + 15 tests. The NAMED gate (export-list, no generation entry point) is the easy half — the only way to smuggle generation in is to ADD an entry point, and an entry point has a name (W120's reasoning) — and it is backed by a stronger check: **every string in the result traces to an input item or to this module's own fixed copy**, so nothing is composed about a patient. **THE HALF THIS UNIT TURNS ON IS W144'S STEP-4 POSITION: MAY ORDER, MUST NOT WITHHOLD.** Showing three items ranked leaves the clinician the judgement; showing ONE, because the software judged the others less relevant FOR THIS PATIENT, has made a clinical judgement and hidden the evidence that it did. So `curate` returns a **PERMUTATION** of everything in scope, tested as a MULTISET property rather than by example — a `limit` parameter added next quarter fails here rather than shipping. No count, no threshold, no cutoff, and no result field that could hold what was dropped, asserted at COMPILE TIME (two `@ts-expect-error` cases) as well as on the key list. **THE DISTINCTION THAT MAKES THAT WORKABLE**: scoping on a PRACTICE-level fact is not withholding, scoping on a PATIENT-level fact would be — a practice that does not run a register has no use for material about it, and excluding that is a statement about the PRACTICE. Recorded facts affect ORDER AND NOTHING ELSE, with a test asserting the same items return in a different sequence. Out-of-scope material is **NAMED rather than omitted**: "we have material you are not seeing because you do not run that register" is a different statement from silence, and only one of them lets a practice notice it should run the register. The ranking is shown AS a ranking (position + matching-fact count + a plain-English basis saying nothing was held back and the order is where to start rather than what matters). Deterministic: ties break on item id, not input order. An item with NO TRACEABLE SOURCE is refused at the door — W152 makes that a type-level guarantee later, and refusing here means the weaker form exists from the FIRST unit rather than only from the last. Verify: pnpm verify green, 1647 tests / 132 files |
| W146 | blocked | — | — | — | **FOUNDER GATE G8 (PROPOSED, UNRATIFIED) — not buildable by any builder.** [P] De-identification gate on anything leaving the tree. Verify: fail-closed; no patient identifier can reach an API, asserted at the boundary not by convention. **BLOCKED on proposed gate G8** | **Why blocked:** the unit's whole purpose is the boundary that anything crossing it reaches a third-party model API, and G8 — *no patient-derived content, identified or not, is sent to any third-party model API until the founder has signed off the vendor, the data-flow and the retention terms* — is PROPOSED and not ratified (plan §5 Q12). Building the gate implies the traffic it gates. docs/EDUCATION-BOUNDARY-W144.md adds two conditions the founder should rule on with it: de-identification must be asserted at the BOUNDARY rather than by each caller remembering to strip (the W109 shape), and clinician-authored FREE TEXT never leaves the tree under any G8 ruling short of one that names it explicitly. |
| W147 | blocked | — | — | — | **FOUNDER GATE G8 (PROPOSED, UNRATIFIED) — not buildable by any builder.** Claude API adapter behind the gate. Verify: constructor refuses live endpoints until G8 opens (G1/G3 shape); instantiated nowhere outside tests. **BLOCKED on proposed gate G8** | **Why blocked:** an adapter for the Claude API, behind a gate that does not yet exist. Same posture as G1's PMS adapter and G3's SMS adapter — buildable to refuse live endpoints — but G8 is PROPOSED and unratified, and the plan states the loop must not decide it. See docs/EDUCATION-BOUNDARY-W144.md. |
| W148 | done | builder-A | 2026-08-11T11:35Z | 93f99f7 | src/education/triggers.ts + 14 tests. W144's boundary document put "show it when a patient with that condition is booked" inside curation, and this is that step. **CASE-TRIGGERED IS NOT CASE-SPECIFIC**: a trigger decides WHEN material is offered, never WHAT it is — the trigger fires and W145 then orders the whole in-scope library without withholding any of it. Keeping the two apart is what stops a trigger becoming a FILTER, which is precisely what the boundary document refused, so `triggersFor` takes facts and RETURNS TRIGGERS, never material, asserted on the result's shape. **RULES SHIP EMPTY** (G5: deciding that a recorded fact is a moment to teach is a clinical judgement about a condition), and W121's lesson about what empty MEANS lands differently and better here — an empty rule set CANNOT make this a no-op that looks like it works, because the education surface offers the whole in-scope library regardless, so no trigger firing means "nothing surfaced NOW" not "nothing available", and the copy says exactly that. **THE G7 HALF IS TESTED AS A PROPERTY, NOT BY EXAMPLE**: triggers match W120's recorded-fact codes and the test REACHES W120's own closed union rather than restating it — if a symptom source were ever added there, this is where the education engine's exposure would show. The rule has NO free-text field a symptom could arrive in (key list asserted), and its one prose field is a reviewer's rationale about the RULE, never matched against anything. **NOTHING RECORDED IS NOT RECORDED ABSENT**, carried through from W120: a teaching moment defined by a GAP must not fire because the practice simply never recorded the fact — those come back as `undetectable` and are reported, so a practice can see its own blind spot rather than mistake it for a clean record. Deterministic and independent of fact order. Verify: pnpm verify green, 1661 tests / 133 files |
| W149 | done | builder-A | 2026-08-11T12:10Z | 82b3c17 | src/education/cpd.ts + 21 tests. W144's boundary document recorded the position and this implements it: **THE TRAIL BELONGS TO THE CLINICIAN.** It is a record of a person's professional development and one small step from a MONITORING TOOL — "who has not read this month's update" is a management report nobody asked for and every practice would eventually run IF THE FUNCTION EXISTED to build it. So there is no practice-level read, **tested as an ABSENCE rather than as a permission**: a permission is a decision somebody can reverse; an absent function is not. `trailFor` takes the clinician id as the QUERY (W123's rule) and another clinician's entries are absent from the serialised result, not merely unshown. **CORRECTIONS ARE APPENDED, NEVER OVERWRITING** — the reading really did get logged, and a record that can be silently rewritten is not evidence of anything, which is the entire point of a CPD trail; the corrected entry stays in the export MARKED, with the correction alongside. A correction to somebody ELSE's entry is refused: a correction is a claim about a person's own professional record and one person cannot make it about another. A correction of a correction is refused so the trail stays readable. **IT RECORDS READING, NOT COMPREHENSION**: no field for understanding, agreement or action (key list asserted) and no `kind` describing attainment — the moment either exists, a practice has a measure of whether its clinicians are LEARNING PROPERLY, a different product and a worse one. The export carries its own caveats, because an export travels AWAY from the product that explains it and a reader will otherwise supply their own meaning for what "read" means; an empty trail renders without implying anything (no "behind", no "overdue"), asserted. Every entry carries a `sourceRef` traceable to signed-off content so an export can be checked. Verify: pnpm verify green, 1682 tests / 134 files |
| W150 | done | builder-A | 2026-08-11T12:45Z | a8f8959 | src/education/advice-lint.ts + 16 tests. W144's boundary document added a FIFTH property to the Q11 rail's four, and this is it: **THE PRODUCT INFORMS A CLINICIAN AND NEVER ADVISES ABOUT A PATIENT.** The difference is TESTABLE, which is why it is a rule rather than an intention — "this pathway changed on 3 March; here is what changed" informs; "you should review this patient against the new criteria" advises; the second sentence is ONE WORD from the first and a copywriter under deadline will write it. Four patterns owned here, and **the second is the one that matters: the POLITE recommendation** — "consider" and "worth reviewing" are recommendations in a softer voice, and THE SOFTNESS DOES NOT CHANGE WHO MADE THE JUDGEMENT; a test pairs the informational and advisory forms of the SAME FACT to show the rule is about AUTHORSHIP, not subject. Also banned: patient-directed claims ("this patient needs...", advice about that patient whoever the sentence addresses) and ACTION FRAMING ("next steps", "action required" — which converts a notice into a task list, and a task list implies somebody decided the task was warranted). **"EVERY EDUCATION SURFACE" IS A PROPERTY OF THE TREE, NOT OF MEMORY**: the declared copy modules are checked against `src/education/` so a new module exporting operator-facing copy FAILS until declared, and every string the existing modules ACTUALLY EMIT is run through the linter rather than a sample — the W102/W140 pattern, which has caught something four times this year. W6's rules APPLIED not re-implemented (the W114 seam, third consumer), with a test asserting this module's own rules do NOT OVERLAP the delegated ones, since an overlap is a copy waiting to drift. The pre-consult notice is deliberately THIN: it says THAT a pathway changed, when, by whom, and points at W122's diff — it has no field for a patient (key list asserted) and does not summarise the change, because summarising would be this module writing about clinical content. Verify: pnpm verify green, 1698 tests / 135 files |
| W151 | done | builder-A | 2026-08-11T13:20Z | 421ac44 | app/console/education + src/education/{store,console-copy}.ts + 12 e2e / 9 unit tests. **TWO OF W144'S BOUNDARY POSITIONS ARE DECIDED ON A PAGE, NOT IN A PURE MODULE**, because both are about what a reader sees NEXT TO WHAT — which is why they are e2e assertions rather than unit ones. **STEP 4 — ORDER, NEVER WITHHOLD**: every in-scope item is rendered with its rank shown AS a rank ("1 of 2"), no collapsed view, no top pick, no "show more" — a default view of one item IS a product that shows one item — and material for a register the practice does not run is **NAMED as out of scope**, asserted in the browser, because silence leaves a practice unable to notice it should run the register. **STEP 6 — THE EVALUATION AND THE MATERIAL ARE NOT RENDERED TOGETHER**: W120's verdict is honest where it lives; beside material about the same pathway it becomes ADVICE BY ADJACENCY, which is how a product recommends something without writing a recommendation. No verdict is rendered here at all, and an e2e sweeps the assembled body text for five verdict phrasings — the second net, because a page can put two compliant sentences together and produce a third meaning. **W149'S ABSENCE IS PRESERVED AT THE STORAGE LAYER**: `cpdEntriesFor` takes the clinician id as its QUERY and has no sibling returning everyone's, so the practice-level report cannot be reintroduced by a page that "just needs the list"; scoping and scrubbing are tested with TWO clinicians in BOTH insertion orders (the bug class Q9-Q11 found five times), and the refusal is STATED on the page a practice would go to looking for that report. **THE PAGE'S FIXED COPY LIVES IN `src/education/console-copy.ts` SO W150'S LINTER REACHES IT** — the first new surface after W150 either earns the tree-checked registry or quietly steps around it; declared in `EDUCATION_COPY_MODULES` and every value run through `lintEducationCopy`. Copy written into JSX is copy no linter sees, and that is now the thing a hardening week should look for. **ONE HONESTY FIX IN W145**: a library view supplies no recorded facts, so the sort key falls back to item id — the fixed `orderingBasis` claimed the order reflected "this patient's recorded facts" when it did not, an account a reader could not check. Now conditional; the no-withholding promise stays unconditional. Registered in W51's `STORE_RESETTERS` and W106's `RECORD_CLASSES` (`no_patient_identity`: triggers match fact CODES, the trail records an ITEM and never the case that surfaced it). W102 census + dossier row added. **TWO FINDINGS RAISED ON THE WAY OUT.** (1) W150's tree check FIRED FOR REAL during this unit's rebase: W152 landed a `provenance.ts` exporting `PROVENANCE_REFUSAL_COPY` and `renderable*` without declaring it, so `main` was RED at f9d2052 and the rebase then silently dropped W152's side of the shared hunk — two independent ways for the same registry to rot, both caught by the same assertion. Declared and its refusals linted here; sixth catch for the W102/W140 pattern this year. (2) **W152's module comment says "W151's console takes `RenderableItem`" AND IT DOES NOT** — the page renders `EducationItem` straight from the store, so the type-level provenance guarantee is real inside W152 and unrealised at the only surface that shows material. Not wired here because the resolution is a genuine trade-off, not a mechanical edit: the source catalogue is built only from `ApprovedContent`/`UsablePathway`, both empty at ship, so a wired console renders NOTHING and this unit's step-4 e2e assertions (the point of the unit) would be replaced by an empty-state assertion — unless the mock route drives W69's sign-off flow, which is a mock manufacturing a founder attestation and therefore a G5 question. **Owned by W154 (Q12 hardening), where the review skills can weigh it.** Verify: pnpm verify green, 1723 tests / 138 files, audit:gate PASS; 15 e2e green including axe ZERO violations on /console/education |
| W152 | done | builder-B | 2026-08-10T12:20Z | f9d2052 | src/education/provenance.ts + 13 runtime tests + 2 compile-time assertions. W145 refuses a sourceless item at the door and says in its own header that this unit makes it type-level. **The upgrade matters because of what a `sourceRef` IS: a string** — satisfied by "TODO", by a copy-pasted id that no longer exists, and by the id of content whose sign-off was withdrawn last week, all of which a non-empty check waves through. On a surface whose whole claim is "everything here traces to reviewed clinical content", the failure that matters is NOT fabricated material but **material that LOOKS sourced**. Does not invent a third gate — COMPOSES the two that exist: a catalogue can only be built from `ApprovedContent` (W69) or `UsablePathway` (W119), both branded and obtainable only through their own G5 sign-off, so there is no `addSource(ref: string)` and the constructor's PARAMETER TYPE refuses an unsigned source (export-list test for the back door). `RenderableItem` is branded in turn, so W151's console rendering an unsourced item is a COMPILE ERROR, not a remembered policy. **One property falls out rather than being built** (as W119 inherited revocation from W118's hash identity): the catalogue is assembled from whatever is signed off AT CALL TIME, so withdrawn content — or a pathway version withdrawn under W128 — is simply absent, and every item citing it stops being renderable that instant; nothing has to notice and no cached flag can go stale. Pathway sources key on W118's CONTENT HASH not a pathway id, so material citing version 3 cannot silently start citing version 4. ONE reason not two for a missing source: "withdrawn" and "never existed" are both simply absent from a call-time catalogue, and splitting them would mean guessing. Type claim verified in `tsc` (assertions in a never-called function), confirmed non-vacuous by deleting the brand and watching TS2578 — and since W119's equivalent file OVERSTATED what one assertion proved, each line here names the failure it actually guards; the check confirmed the labelling is right this time. Fixture drives W69's REAL workflow (draft → review → sign off) rather than hand-building a signed-off record, because a faked fixture would test the opposite of the unit's point. Verify: an item with no source is unrenderable by type; pnpm verify green, 1697 tests / 136 files |
| W153 | done | builder-A | 2026-08-11T14:05Z | 91b46d3 | src/security/{untrusted,instruction-sinks}.ts + docs/SECURITY-W153.md + 24 tests. **THE POSTURE IS POSITIONAL, NOT LEXICAL, AND THAT REFUSAL IS THE UNIT.** The obvious build is a scanner for "ignore previous instructions" / "SYSTEM:" — the same mistake W6 made and corrected in another domain: a phrase list is a PROXY, defeated by rephrasing, failing in the direction that LOOKS SAFE, and the text it censors here is somebody's NAME or a clinician's note about a patient. Mangling those to defend against a machine that does not exist yet trades a real harm for a hypothetical one. So nothing reads content for intent; the POSITIONS where content could act are enumerated and each made structurally unreachable. **CONTROL**: `matchDeclared` returns a member of a vocabulary THIS TREE WROTE or null — an attacker controlling the text controls at most WHICH OF OUR VALUES is selected, never what the value is; no unwrap/asCode/toString exists, asserted on the export list, and no near-match guessing (a fuzzy match is the software deciding what an outside party meant in the one place where it changes behaviour). **OPERATOR DISPLAY**: verbatim — a test asserts the injection phrase SURVIVES — but `quoteForOperator` returns a `QuotedText`, not a string, so a surface must place OUR attribution and ingested text cannot speak in the product's voice. **SPREADSHEET — A REAL DEFECT, FIXED**: `interestSignupsCsv` quoted every cell, which made the file safe to PARSE and left it unsafe to OPEN; a public-form name of `=HYPERLINK(...)` is a valid quoted cell and an executable formula the moment staff double-click /api/interest/export. Neutralised at the WRITER, every cell, and the test asserts NO cell in the rendered file leads with an operator rather than that the seeded one does not. **INSTRUCTION**: nothing in src/app/scripts names a model endpoint — scanned, with an EMPTY declared-exception list (W53's shape), markers assembled from fragments at runtime so the scanner does not match itself and needs no self-exclusion (an excluded file is a place to hide something), test files scanned too, and proven non-vacuous by feeding every marker to the detector. **THE DAY W147 IS BUILT THIS TEST FAILS — that is the design**: the failure is the reminder that the adapter needs a ratified G8. One payload CORPUS is run at EVERY sink, including ordinary clinical advice, asserting hostile and ordinary text are handled IDENTICALLY — any difference between them would be the phrase list this refuses to build. **HONEST GAP RECORDED**: the display sink's live consumer is /console/interest, whose populated branch is UNREACHABLE in the shipped product (`MEHERR_STAFF` ships empty, W105), so it is unit-tested and type-checked but not asserted end-to-end — granting a staff email to make it testable would be a founder act. The exploitable half (CSV) is fully covered. Doc carries the ingestion-boundary register for all seven boundaries and a what-this-does-NOT-cover section (a valid condition code written into a PMS is data integrity, not injection; free text still never leaves the tree under W144). Verify: pnpm verify green, 1748 tests / 140 files, audit:gate PASS |
| W154 | done | builder-B | 2026-08-10T12:50Z | bce55e6 | docs/HARDENING-Q12.md + fix. **ONE CRITICAL: the education console never applied the provenance gate.** W151's page called `curate()` and rendered the result directly, never touching `renderable()` — so W152's guarantee (every item traces to signed-off content) was enforced by the TYPE SYSTEM and applied BY NOBODY, on the only surface in the tree that renders education material. The seed made it concrete rather than theoretical: `/api/mock/education` gave each item a `sourceRef` like `"signed-off-source-a1"` — a string pointing at nothing — and the console rendered those strings in a field labelled **Source**. Exactly the failure W152's own header says it exists to prevent (not fabricated material but material that LOOKS sourced), live on the page while the module claiming to prevent it sat one import away. **WHY THE TYPE GATE MISSED IT, precisely: a brand stops you passing the wrong thing to a function that ASKS for the right one; it cannot make a surface ask.** W151 predates W152 and took `RankedItem`, a legitimate type — nothing was bypassed, the gate was never joined up. As much MY gap as builder-A's: W152 built a gate and did not wire its only consumer. Fix in three parts: console runs the curated order through `renderable()` and NAMES what it withholds rather than dropping it (W68's rule; not the withholding W145 forbids, which is about patient-level judgements); `getSignedOffSources()` is fed only by `addSignedOffContent(approved: ApprovedContent[])`, so the store cannot be handed a source that never cleared G5; and the mock now drives **W69's REAL sign-off workflow** instead of typing ref strings, with one item deliberately left unbacked so the withholding path runs every time. ALSO CORRECTED: the page claimed "every item is above, in full" unconditionally, which alongside a list of what was held back is the page CONTRADICTING ITSELF — now rendered only when true, with the withheld panel noting the ordering figures below it predate the check; e2e asserts both directions. Confirmed clean: no model client anywhere near the education engine (G8 holds because W146/W147 stayed blocked), triggers match recorded facts only, CPD clinician-scoped with a colleague's entry planted in the seed to prove it, W103 scoping sweep finds nothing, and W150's advice linter demonstrably reaches this unit's new copy — verified by injecting a violation. **FOR THE PLAN OWNER: nothing checks that a branded gate has CONSUMERS.** Second bug of that shape in two quarters — W103's B1 was a function nobody could call correctly, this is one nobody called at all; both invisible to typechecking, which verifies the call sites that exist and says nothing about the ones that should. Verify: zero criticals; pnpm verify green, 1748 tests / 140 files; education e2e 13/13 |
| W155 | done | builder-A | 2026-08-11T14:40Z | ce516ec | docs/AUDIT-Y3.md — **2 findings, BOTH FIXED, both the SAME CLASS**; whole tree, not the Y3 diff, and that choice is justified by the findings: one lives in W32 code Y3 never touched, the other in W93 code last changed in Q8. **Y3-1 HIGH at G3, latent: effective SMS CONSENT was decided by ROW ORDER.** `ingestFromAdapter` step 4 sorted provenance on `capturedAt` with a comparator returning 1 rather than 0 for equal keys — an INCONSISTENT comparator, not a missing tie-break — and took the last row. Worse: step 3 ALREADY DETECTED same-instant disagreement and filed a `ConsentConflict`, and step 4 never read it. **The product filed the ambiguity and then resolved it anyway**, which is worse than not detecting it because the conflict record makes the system look careful. Reachable via a batch export stamping every row with the export time. **The existing test was named "flags same-capture conflicting values INSTEAD OF SILENTLY PICKING ONE" and only asserted the conflict was RECORDED — the name claimed the property, the assertion checked something else.** Fixed so the ambiguity DECIDES: disagreement at the maximum capture resolves to NO CONTACT (absence of a clear yes is not a yes, W120/W125 applied to consent), and there is NO SORT LEFT AT ALL — the maximum is taken directly, so there is no comparator to be inconsistent and no position for a tie to resolve by. **Y3-2 MEDIUM, latent: a same-day referral chain was replayed in STORAGE order.** `replayReferral` broke ties by leaving them alone — stable, therefore deterministic on POSITION, which nothing guarantees matches what happened; the comment said "stable sort by time", which reads as a guarantee about time and is one about position. `at` is a DATE, and written + booked the same day is the ORDINARY case (the patient books at reception on the way out): delivered booked-first it rejected the booking and reported `leaked: true` about a patient in the waiting room. Fixed with a chain-position tie-break — not invented information, since the state machine already declares a referral must exist before anything happens to it — with different-dated events untouched (asserted), and the genuinely-unrecoverable cases (same kind twice, same-day cancel-and-rebook) FILED rather than guessed at. **BOTH CONFIRMED BY WRITING THE FAILING TEST FIRST, AND BOTH FIXES PROVEN NON-VACUOUS BY REVERTING THEM AND WATCHING THE NEW TESTS FAIL AGAIN.** The class is now EIGHT instances in this tree; report states the rule at full strength plus two new corollaries (if code DETECTS an ambiguity, check the code that RESOLVES it reads the detection; "stable sort" guarantees positions, not meaning). 16 controls confirmed clean incl. G8 — nothing in src/app/scripts names a model endpoint (W153's scanner). 7 items carried to Y4, 5 of them founder gates. Verify: pnpm verify green, 1753 tests / 140 files, audit:gate PASS |
| W156 | done | builder-A | 2026-08-11T15:15Z | c117f33 | **YEAR 3 COMPLETE.** §5d + 52 rows: Q13 vertical assembly W157–W169, Q14 outcome auditing W170–W182, Q15 directory W183–W195, Q16 reporting/compliance W196–W208. Ledger runs W1–W208, no gaps, no duplicates, plan and ledger checked unit-for-unit. **DERIVED FROM WHAT WAS BUILT AND FROM THE Y3 AUDIT**, five residues scheduled rather than hoped for: (1) **G5 IS NO LONGER A SCHEDULING PROBLEM, IT IS THE PRODUCT** — Y3 shipped nine empty registries AND every mechanism to fill them (W69 workspace, W119 sign-off, W152 branding), so unlike Q10 there is NO MECHANISM GAP LEFT TO BUILD IN FRONT OF THE RULING; Q13 therefore splits explicitly, machinery ships against synthetic verticals and W161/W162/W163 are `blocked` FROM DAY ONE rather than discovered blocked in week eleven; (2) **nothing has ever been sent and Q14 is about outcomes** — the send path has been unwired since W28/W29/W36, so outcome auditing audits the RECORDED rail and W174 is blocked on G3, because a dashboard measuring interventions nobody sent shows zeros and calls it monitoring; (3) **the console still cannot represent two practices** (Y2 finding B2, open two years) — tolerable while every surface was single-tenant, but Q15's directory and Q16's PHN reporting are both inherently cross-practice, so it moves onto the critical path as W166 instead of remaining an audit note; (4) **the order-dependence class is now EIGHT instances** and Y3's audit turned it into a stated rule — a rule that depends on the next reviewer remembering it is the control this tree has watched fail, so W167 makes it MECHANICAL (W102/W140/W150/W153 shape) and W178 turns the eight historical defects into a permanent regression corpus; (5) **one dated failure falls inside Q13** — both allowlist acceptances carry reviewBy 2026-11-09 and audit:gate starts failing that morning (W165). **PROPOSED GATE G9 — third-party organisational reporting — added to plan §4 for FOUNDER RATIFICATION, not decided here**: Q16 is the first time anything about a PRACTICE leaves this tree to an external body — one that also commissions services from that practice — and no existing gate covers it (G2 = holding patient data, G6 = public copy, G8 = model vendors). W202/W203 written to be buildable behind it and marked blocked on it. G8 remains unratified at Y3 close, so nothing in Y4 assumes model processing. 11 blocked rows across the ledger, each naming its gate AND why. **One pre-existing cosmetic defect NOT touched**: rows W61/W62 and W63/W64 sit out of numeric order from Y2; ids are unique and complete, and reordering another year's rows mid-expansion risks a conflict for no gain. Verify: 52 units appended with verify gates, gates inherited intact, ledger integrity W1–W208 checked programmatically (gaps, duplicates, plan↔ledger match, every blocked row names a gate); pnpm verify green, 1753 tests / 140 files, audit:gate PASS |

> **Year 4 — Network launch** (expanded by W156, 2026-08-11; definitions in docs/FIVE-YEAR-PLAN.md §5d). Q13 W157–W169 (**G5 load-bearing; W161–W163 blocked from day one**) · Q14 W170–W182 (W174 blocked on G3) · Q15 W183–W195 (**G6 load-bearing**; W185 blocked) · Q16 W196–W208 (**proposes gate G9 — third-party organisational reporting, awaiting founder ratification; the loop must not decide this itself**; W202/W203 blocked on it).

| W157 | done | builder-A | 2026-08-11T15:50Z | 43b13e7 | src/verticals/model.ts + 24 tests (21 runtime + 3 compile-time). **THE UNIT IS WHAT THIS MODULE DOES NOT DO: IT NEVER DECIDES WHETHER A MEMBER IS USABLE, IT ASKS.** `UsableVertical` holds `UsablePathway` (W119), `ApprovedContent` (W69) and `RenderableItem` (W152) — all branded, none constructible without passing its own gate — so an unsigned pathway is not rejected by a check here, it CANNOT BE PUT IN THE STRUCT because the caller cannot produce the value to put in. A fourth implementation of the G5 rule would be a fourth thing to keep in step and the copy is always what drifts, so **a test reads model.ts's own SOURCE and fails if it contains the vocabulary of sign-off** (signedOffBy, reviewedBy, specialist_review, revokedAt, contentHash, `state ===`) — proven non-vacuous by adding a signedOffBy reference and watching it fire. The type half asserts the composition in `tsc`: a fully-shaped fake `UsablePathway` with every W119 field present is rejected by the unforgeable symbol ALONE (tsc names it), and deleting the brand makes the suite fail with TS2578 — checked by deleting it. **AN EMPTY VERTICAL IS REFUSED, AND THAT IS THE VACUITY TRAP THIS TREE KEEPS HITTING**: "every member is usable" is VACUOUSLY TRUE of a bundle with no members, so without the rule the gate is passable by shipping nothing. **WITHDRAWAL PROPAGATES WITH NOTHING NOTICING** — evidence is assembled at CALL TIME, so a revoked review removes the pathway and the vertical citing it fails on the next call; no invalidation step exists because none is needed (W118→W119→W152's inherited property, tested). Identity is MEMBERSHIP (`verticalHash` over sorted refs), so "versioned as one" is structural: swap a member and it is a different version, while REORDERING the same members is not — a version that changed on reordering would make W160's migration rules fire on nothing. Refusal names EVERY unusable member, not the first (W6/W145 posture), and cannot distinguish "never existed" from "no longer signed off" — one reason, W152's line, because both are simply absent from call-time evidence and inventing the difference would be guessing. **ONE HONEST ASYMMETRY NAMED RATHER THAN PAPERED OVER**: three member kinds are gated by a BRAND, guideline intervals by VALIDATION (W56) — the module takes the CATALOGUE not an array, which is the strongest form available, and branding intervals is left alone because it touches the module whose VALUES are blocked on G5 (W163) and reshaping it from here would be this unit deciding that one. Verify: pnpm verify green, 1777 tests / 142 files, audit:gate PASS |
| W158 | done | builder-B | 2026-08-10T13:20Z | fb459dd | src/verticals/completeness.ts + 15 tests. W157 answers "can this ship now" in ONE BIT — right for a gate, wrong for the only question anyone asks about a half-built vertical: what stands between here and shipping, and WHO HAS TO DO EACH PART. So the report decomposes by OUTSTANDING ACT, not by member, because the blockers have different owners and a flat list of ids hides that: an unsigned pathway needs two people in order; content already reviewed needs the founder alone; a missing interval needs the G5 values ruling so NOBODY CAN ACT until it lands; a broken education reference needs an author and no gate at all. "4 members not usable" tells you none of that — "3 are waiting on one ruling and the fourth is a typo" is a plan, and it is the input W169 needs to price what G5 costs. **TWO THINGS IT REFUSES TO GUESS.** (1) From W157's evidence alone "exists but unsigned" and "names nothing" are INDISTINGUISHABLE — deliberately, since evidence is assembled at call time — and picking one would invent the more flattering answer roughly half the time; telling them apart needs a SECOND input (the pool of what exists), which is OPTIONAL, and without it the report says `indeterminate` rather than assuming. (2) It never says which STAGE of a gate a member is stuck at — knowing a pathway is unusable does not say whether a specialist has looked, and asking would mean a second copy of the sign-off vocabulary W157 went out of its way to avoid; it names the whole remaining chain FOR THE KIND, true of every member of that kind and unable to drift, with a test banning the stage vocabulary from the rendered output. **Shippability is ASKED OF W157, and the empty-vertical case shows why that is not deference for its own sake**: "every member is usable" is VACUOUSLY TRUE of a bundle with no members, so the obvious local implementation (`readyMembers === totalMembers`) reports an empty vertical as ready to ship — the test asserts BOTH the naive answer and the right one. Zero clinical content is COMPUTED not asserted (only worth claiming if something checked it) and holds today because W69/W119/W152/W56 all ship with nothing signed off. Coverage is not a footnote (W94's lesson, W96's placement): says whether it had the pool and what that cost, which kinds it assessed, and carries forward W157's honest asymmetry that "usable" is a WEAKER word for intervals, gated by validation rather than a brand. Verify: golden report as ONE EXACT STRING; states its own coverage; zero clinical content for the shipped state; pnpm verify green, 1792 tests / 143 files |
| W159 | done | builder-A | 2026-08-11T16:25Z | d4b49dd | src/verticals/consistency.ts + 19 tests, wired into W157. **THE SHAPE OF THIS UNIT WAS SET BY LAST WEEK'S AUDIT FINDING Y3-1**, where consent ingest detected a same-instant disagreement, filed it as a conflict, and RESOLVED THE FLAG ANYWAY by array position. The corollary drawn there — *if code detects an ambiguity, check that the code which RESOLVES it reads that detection* — is this unit's whole design: the detection is **WIRED INTO `usableVertical`, NOT OFFERED TO IT**, so a blocking contradiction cannot coexist with `usable: true`. Proven non-vacuous by short-circuiting the wire (the exact Y3-1 shape: detect, then answer anyway) and watching precisely those two tests fail. **TWO SEVERITIES, SPLIT ON "IS THERE A PRINCIPLED WINNER?"** — BLOCKING is mechanically undecidable (two versions of one pathway; two cadences for one condition; a pathway that excludes everyone it includes) and refuses; REVIEW is a real disagreement this module MAY NOT SETTLE (a fact one member takes a patient on for and another escalates on; opposite entry polarity) and is reported, carried WITH the vertical, never auto-resolved. **The second category is not Y3-1 repeated but its mirror**: Y3-1 resolved an ambiguity with no principled winner; this declines to, which is W142's rule — where no winner exists, reporting the tie IS the answer. Polarity is load-bearing on the unsatisfiability check: include-on-present + exclude-on-ABSENT is how pathways are normally written and is NOT flagged; only the SAME requirement on both sides is, because a check that fires on the ordinary case is a check that gets turned off. **Nothing here reads clinical meaning** — every test is structural (same code twice, same identifier with two values); telling a sensible criterion from a mistaken one is W119's two people, not the loop's. Determinism was treated as a hazard rather than a nicety, since producing order-dependent findings in the unit that CITES the order-dependence class would have been its ninth instance: pairs are canonically sorted, findings sorted on a composite key, each unordered pair reported once, and both-orders equality asserted. **INCIDENTAL FIX**: W157's and W158's pathway fixtures omitted `criteria`, so they stood in for a shape that cannot exist — the first real consumer of the field exposed it; corrected in both. Verify: pnpm verify green, 1811 tests / 144 files, audit:gate PASS |
| W160 | done | builder-B | 2026-08-10T13:50Z | 980efb0 | src/verticals/binding.ts + 16 tests. **THE FAILURE THIS PREVENTS IS THE QUIET ONE**: a practice reads a vertical, agrees, turns it on; six weeks later a new version publishes with a widened exclusion criterion — and if the binding names the VERTICAL, that practice is now running content nobody there ever read, with nothing broken to find out by. So the binding names a VERSION HASH, never a vertical id: W157 made identity = membership, so publishing v2 produces a different hash and the binding still points at v1. **"No silent upgrade" is not a rule anyone enforces — there is NO CODE PATH that could perform one**, because nothing here resolves "the latest version of X"; a test asserts the export list contains nothing matching latest/newest/auto/sync/refresh, since an `upgradeToLatest()` would reintroduce the silent upgrade AT THE MOMENT OF THE CALL. **W128's withdrawal semantics INHERITED not restated**, which is why the module is small: a binding holds a hash, whether the version still WORKS is asked of W157 at resolution time, and W157 assembles evidence from what is signed off right now — revoke a review and every bound practice learns at their next resolution, with no propagation step because none is needed (the same free inheritance W152 got from W119 and W119 from W118). It also RECOVERS ON ITS OWN when a withdrawal is reversed, because nothing was invalidated to repair. **The interesting case is a bound version going unusable, where two wrong answers both look helpful**: auto-migrating is the silent upgrade arriving through the back door at the WORST moment (unread content, and the trigger was a safety withdrawal); dropping the binding reverses a decision without telling anyone. So the binding STANDS and resolution reports it, naming what is missing — W134's shape, where the discomfort is the signal. Mutation-checked: injecting the auto-migration fails that test. **Writing the tests corrected an assumption of mine** — I had v2 as v1 WIDENED and then asserted v2 survives v1's pathway being withdrawn, but a widening cites the same members so one withdrawal takes both; that is the COMMON case (a practice whose vertical breaks usually has nowhere to move to) and it now has its own test, with the alternatives case using a genuinely disjoint bundle. Verify: bound practice stays on its accepted version, no silent upgrade, withdrawal inherited; pnpm verify green, 1827 tests / 145 files |
| W161 | blocked | — | — | — | **FOUNDER GATE G5 — not buildable by any builder.** Cardiometabolic pathway content → verify: two-person sign-off recorded per W119. **Why blocked:** Cardiometabolic pathway content is clinical content: inclusion, exclusion and escalation criteria a GP would act on. W119's two-person sign-off exists and has never been used, because a specialist reviewer and a founder signature are people, not code. |
| W162 | blocked | — | — | — | **FOUNDER GATE G5 — not buildable by any builder.** Early-CKD pathway content → verify: as W161. **Why blocked:** Early-CKD content, same reason as W161. Listed separately because the two verticals fail independently — one may be signed off without the other, and a plan that bundles them would hide that. |
| W163 | blocked | — | — | — | **FOUNDER GATE G5 — not buildable by any builder.** [P] Cardiometabolic/CKD interval values — W56's question at vertical scale → verify: every interval carries a citation and a review date. **Why blocked:** Interval VALUES. W56 shipped the container in Q5 and the values have been blocked ever since; this is the same question at vertical scale. A loop that picked a monitoring interval would be authoring guideline content. |
| W164 | done | builder-A | 2026-08-11T17:00Z | 47327da | app/console/verticals + src/verticals/store.ts + 10 e2e. **THE PAGE HAS TWO ZERO STATES AND THEY MEAN OPPOSITE THINGS** — that is the unit. NOTHING ASSEMBLED YET is work, and work the loop can do (deciding which pathways and material belong together needs nobody's sign-off); ASSEMBLED WITH NOTHING SIGNED OFF is the opposite message — no amount of building helps, it is waiting on a specialist, a founder signature and the G5 values ruling. A blank grid reads as neither, and the two lead a practice to OPPOSITE NEXT ACTIONS, so the page names which it is and an e2e asserts they are **never shown together** (a single "nothing here" message would pass a naive check and still say the wrong thing). Renders nothing it decides: W157 answers usable, W158 answers what is outstanding AND WHO HAS TO ACT, W159 answers which members disagree. **No pathway criteria anywhere** (W127's rule), asserted by seeding fact codes and checking the rendered body — with ONE deliberate exception, stated rather than discovered: a W159 finding names the fact code a pathway contradicts itself on, which is diagnostic rather than clinical, since a defect report a reader cannot act on without knowing WHICH code is not a defect report. Store is NOT practice-scoped (W127's reason: a vertical is a document; which version a practice ACCEPTED is W160's binding), and `assembleEvidence` decides nothing — it asks W119, W69, W152 and W56 at CALL TIME, which is what gives W157 its withdrawal-propagates property for free. **FINDING RAISED WHILE BUILDING THE FIXTURE, FILED FOR W168**: of W159's three BLOCKING contradiction kinds only ONE is reachable through the registries today — `two_versions_of_one_pathway` cannot happen because W118 supersedes the earlier version and `usablePathway` refuses anything not in force; `two_intervals_for_one_condition` cannot because SHIPPED_INTERVALS is empty until G5; only `pathway_unsatisfiable` is live, and it is what the e2e seeds. A guard that cannot fire is not wrong, but it should be KNOWN to be a guard. Attestations are seeded in the mock route following the posture W127 already established behind `assertMockRoutesEnabled()` — obviously-fake reviewers, SHIPPED_ATTESTATIONS still empty, nothing signed off ships. Registered in STORE_RESETTERS + RECORD_CLASSES (no_patient_identity), W102 census + dossier row added. Verify: pnpm verify green, 1827 tests / 145 files, audit:gate PASS; 13 e2e green including axe ZERO violations on /console/verticals |
| W165 | done | builder-B | 2026-08-10T14:20Z | c44f920 | Allowlist review + repair to src/security/reachability.ts. Advisory data UNCHANGED since W107 (patched `<0.0.0`, recommendation None, updated 2026-08-07), so re-recording it would have been theatre — this review asked the question W107 did not: **can the DEPENDENCY go, rather than the acceptance be renewed forever?** It cannot, and the reason is now on the record so November is not re-litigated from scratch: there is no patched image-size to override to (every version ≤2.0.2 affected, none newer), and removing `pptxgenjs` would **delete W46's CONTROL rather than just a dependency** — it exists solely so a test can regenerate the sales deck from traceable figures instead of the deck being a hand-made artefact nobody can check. STILL NOT EXTENDED: three months out, the November date is the forcing function. **THE SUBSTANTIVE FINDING WAS ELSEWHERE.** Re-confirming reachability against a tree grown from 16 to 20 console surfaces meant PRINTING the package set rather than watching the test pass — and it contained `"], string> = {\n  referring_practice: "`, which is not a package. Two bugs in the W107 scanner, both mine: `[^;]` SPANNED STATEMENTS (`export const X: Record<A, string> = {` plus a later `from "..."` matched as one import), and COMMENTS WERE SCANNED AS CODE (this tree is heavily commented and "from" is ordinary English, so a doc comment ending "…delete it from" started a match running into the next string literal). The nonsense entry was the visible harm; **the real one is that `exec` advances past the whole match, so a genuine import inside that span is never seen — a FALSE NEGATIVE in a control whose entire job is to prove something is unreachable.** Latent not active here (file count unchanged at 160, nothing was being hidden) — it would not have stayed latent. Fixed by excluding `=`, stripping comments before scanning, and rejecting anything not specifier-shaped; two durable guards added — no reported package may contain whitespace or braces, and the reachable set is PINNED EXACTLY so a new runtime dependency appears in a diff and a shrinking list means the walk stopped walking. **Observation for the plan owner, not fixed here:** no pnpm script regenerates the deck — `reports/meherr-sales-deck.pptx` is committed and only a TEST exercises `buildDeck`, so the artefact can drift from what the code would produce; a stale `reports/careyield-sales-deck.pptx` also survives the rebrand. Verify: audit:gate green, no acceptance past its review date, neither extended without a fresh rationale; pnpm verify green, 1829 tests / 145 files |
| W166 | done | builder-A | 2026-08-11T17:45Z | a6768d1 | src/console/store.ts restructured + 25 files migrated + e2e/two-practice.spec.ts. **CLOSES Y2 FINDING B2 AND Y3 AUDIT ITEM 6, OPEN FOR TWO YEARS.** `ConsoleState.practice` was one nullable practice and the id was the literal `prac-console` in three places, so a second practice COULD NOT EXIST and console-side scoping could not be tested in a browser at all — the finding was filed as PROCESS precisely because nobody could write the test. Now `practices: PracticeRecord[]` with generated ids. **THE PER-PRACTICE SPLIT WAS THE REAL DECISION**: rules config, roster, session dials and setup progress MOVED ONTO THE RECORD rather than staying global — leaving them shared while practices went plural would have been a worse lie than the singular model, a silent cross-tenant leak of exactly the W57/B1 shape. `memberships` and `auditEvents` stay on the state because both already carry a practiceId and are queried across practices by design. **THE SELECTION COOKIE CANNOT WIDEN ACCESS**: `activePracticeFor` honours it only when the email is already a member, so a forged cookie picks among practices the user already has — membership stays the grant, the cookie is a preference; asserted e2e by forging it as an outsider and landing on onboarding. Switcher renders only when there is MORE THAN ONE practice (a control offering one choice is furniture) with the single-practice case tested as the non-vacuity guard. **TWO BUGS I INTRODUCED AND CAUGHT**: the mechanical migration routed `onboard` and `saveStepPractice` through `requirePractice`, making the actions that CREATE the first practice bounce to the page they post from — forever; both now take `requireSession`, with the reason written down. **THREE TESTS RESTATED BECAUSE THE PROPERTY HOLDS DIFFERENTLY, NOT BECAUSE IT STOPPED HOLDING**: W26's create-only rule was about overwrite-and-reseat, which APPENDING CANNOT DO, so the assertion moved to what always mattered — a second onboarding cannot touch the first practice or its ownership, and the attacker holds nothing over it; W51's non-member refusal now reads "you have no practice" rather than "your role cannot", which is a more accurate answer, with the invariant (no outcome recorded) checked at the store. **OBSERVATION FILED**: `/console/dashboard` and `/console/usefulness` are NOT practice-scoped — they render the deterministic sim and the synthetic audit queue — so they are deliberately absent from the isolation sweep, noted in the spec so the day they show a real practice's numbers is the day it is obvious they belong. Also filed: the ROLE-refusal path (a member with insufficient role) needs its own fixture now that non-membership takes a different route. Verify: pnpm verify green, 1829 tests / 145 files, audit:gate PASS; **131 e2e green, zero failures** |
| W167 | done | builder-B | 2026-08-10T14:50Z | 53023e5 | src/quality/order-independence.ts + 12 register tests + 3 tie-break tests + 3 fixes. **A fold turns a collection into one answer, and when two records TIE the answer becomes whichever the input listed first — an order that came from a store query, an ingest or a Map iteration, none of which anybody chose.** The bug is not that the fold is wrong; it is that the fold is right ONLY ABOUT THE ORDER IT WAS GIVEN, and nothing says so. Not hypothetical: W118's `pathwayAt` returned the SUPERSEDED version for two published at the same timestamp, found in W128 only because a mutation check showed the compensating guard was untested — invisible to review, to types, and to every existing test. Register covers exactly the fold-to-one population (12 modules, all `.reduce`); there are NO sort-then-take-first sites, which is why the detector does not chase `.sort` — a sort produces an ORDER, this unit is about folds to an ANSWER. Checked against the tree in THREE directions (W102/W106 shape): undeclared fails; declared-but-no-longer-folding fails, because a register describing moved code reads as coverage; and the fold COUNT is declared, so adding a second unconsidered fold to an already-declared file fails too. Line numbers deliberately avoided — they churn on unrelated edits and get bumped without anyone reading the code. Exactly TWO dispositions allowed: a tie-break TEST or a written RATIONALE; "someone looked at it" is not one, and most sums earn a rationale in a sentence so the cheap cases stay cheap and the expensive ones stand out. **BUILDING IT FOUND THREE GENUINE TIE-SENSITIVE FOLDS, all resolving by store order**: `pathways/approval.ts` picked among attestations at the same instant, so the document whose job is to say WHO LOOKED would name a different reviewer on a refresh; `messaging/approval.ts` reports "the most recent withdrawal, the one a manager will be asking about" and gave different answers for two at the same instant; `verticals/binding.ts` resolved a practice's current version, so "which version am I on" could vary by row order — the silent version drift W160 exists to prevent, arriving by a different door. All three mutation-checked: reverting each tie-break fails its test. Property helper is deliberately tiny (forwards, reversed, same answer) and requires the CALLER to supply the tie, because only the caller knows what the fold compares — a helper that guessed would pass on collections that never tie, which is the vacuous version; a test asserts the helper FAILS on an untie-broken fold, or it proves nothing about the ones it passes. Verify: register checked against the tree, new fold site fails until it carries a test or a rationale; pnpm verify green, 1844 tests / 146 files |
| W168 | done | builder-A | 2026-08-11T19:30Z | 999c39e | docs/HARDENING-Q13.md + src/quality/ledger-integrity.test.ts + 4 fixes. **ZERO CRITICALS, FOUR FINDINGS, AND THREE OF THE FOUR ARE FAILURES OF CONTROLS RATHER THAN OF PRODUCT CODE** — a register that cannot see part of what it registers, a lock nobody checks, and a detector that reports zero for a family it cannot detect. All three fail in the same direction: they look clean. **Q13-1 HIGH — A SAME-DAY CONSENT PAIR RESOLVED BY ARRAY ORDER.** `consentFor` picked the operative decision with `sort(decidedAt).at(-1)`; dates are day-granularity, so a patient who refuses in the morning and agrees in the afternoon resolved to whichever row the store listed last. **The module already knew**: W129 rewrote the WITHDRAWAL rule over the whole set precisely because "day-granularity dates cannot order two events within a day", ending "refusing to guess is the safe direction for consent" — and twelve lines later the code guessed. Y3-1's shape exactly, in the module that decides whether a patient agreed. Fixed by applying the sentence already written: where the latest day's decisions DISAGREE the non-given one is the answer; no sort remains. Both orders regression-tested, plus two companions proving it does not over-refuse. **Q13-2 HIGH (process) — THE CLAIM LEDGER HAD A DUPLICATE `available` ROW.** My own W166 rebase kept both sides of a conflicted region: W166 twice, W167 twice, and the spare W167 still marked available next to builder-B's completed one — a builder following the protocol would have rebuilt a finished unit. **The lock had a duplicate key and said nothing.** Also found: three pairs out of numeric order, and **W41's row missing its closing pipe** (not a table row at all). Every expansion week checked integrity BY HAND and wrote "no gaps, no duplicates" in a commit message; this week that habit failed the way hand-checks always do. Now mechanical — duplicates, gaps, order, a SHA on every done, a named gate on every blocked, owner+timestamp on every claimed (which W54's staleness rule needs to function), and only protocol statuses. **It found the W41 defect on its first run.** **Q13-3 MEDIUM — W167's DETECTOR COULD NOT SEE HALF THE FOLDS IT REGISTERS**: its row concluded "there are no sort-then-take-first sites" from a matcher that only saw `.reduce(`; there were FIVE, and one of them was Q13-1. Widened, and all five declared with a disposition — two tie-break tests, three rationales each naming why position IS the fact there. **Q13-4 LOW — the widened detector counted folds MENTIONED IN PROSE** and matched itself; patterns now assembled from fragments (W153's trick, since an excluded file is a place to hide something) and comments stripped before counting. Carried forward: `src/verticals/store.ts` has no test of its own and nothing catches a missing test file; the role-refusal e2e needs a seated-member fixture; W164's unreachable-guard note stands. Verify: pnpm verify green, 1856 tests / 147 files, audit:gate PASS |
| W169 | done | builder-B | 2026-08-10T15:20Z | f67550b | docs/GATE-DOSSIER-Q13.md. The Y3 dossier said G5 had gone from "one ask among three" to holding two quarters — that framing COUNTS UNITS rather than saying what the company does not have, so this one answers three questions: which units unblock, in what order, and what the product gains on the day. **THE PRICE STATED PLAINLY: nine modules ship a COMPLETE, TESTED MECHANISM OVER AN EMPTY CATALOGUE** — 108 unit tests and 9 e2e behind gates that hold nothing — plus seven more that are not empty but have nothing to act on. So the price is not "some units are blocked", it is that **an assembled machine is inert**: a practice signing in today reaches a vertical console, a pathway console, an education library and a credential registry, and every one correctly renders a zero state saying nothing is signed off — the product working exactly as designed and also doing nothing. Unblock order is FORCED by the dependency chain: W163/W56 intervals first (cheapest ruling, largest unlock — it is what makes W58 produce care gaps at all), then W161/W162 pathway content, then W157's verticals become assemblable with **W158's completeness report as the instrument** that says what is still missing, then W186, then SUP-1. SUP-2 does NOT unblock — it needs a company-direction decision W89 says must not be answered as a clinical-content question. Also states what a G5 ruling does NOT unblock so it cannot be oversold: G1/G2/G3 untouched and the send path still unwired; G6 still gates the directory and W133; proposed G8/G9 unrelated. **And it argues the other side**, because a dossier that only lists gains is advocacy: signing off content makes Meherr the PUBLISHER, engages professional exposure, and starts a maintenance obligation that does not stop (W118's versioning exists because published guidance is re-reviewed, not edited) — the empty catalogue is also the only state in which none of that applies. Every figure MEASURED not recalled, and one claim did not survive: the Y2 dossier's /clinicians inconsistency reads as RESOLVED if you grep the page, because `app/clinicians/page.tsx` is now five lines delegating to a component — the content is still there in `clinician-walkthrough.tsx` (Rotterdam criteria, COCP suitability, metformin titration), and the dossier says where so the next reader can check in one command rather than concluding it went away. A vertical e2e count was corrected 10→9 the same way. Verify: dossier names exactly which units unblock, in what order, and what the product gains; pnpm verify green, 1856 tests / 147 files |
| W170 | done | builder-A | 2026-08-11T20:15Z | 976ffe7 | src/outcomes/model.ts + 19 tests. **THE TEMPTING DEFINITION OF AN OUTCOME MAKES A FAILURE STATISTIC OUT OF A RECORDS GAP**, and refusing it is the unit. A referral with no completion event may have completed with nobody writing it down; counting that as "did not go anywhere" measures a practice's RECORD-KEEPING and reports it as their CARE. So W120's rule is inherited wholesale because it is the same rule: three verdicts, never a boolean. `reached` and `stopped` are both POSITIVE statements resting on an event somebody wrote down; **`not_recorded` is the honest third answer and on a young rail it will be most of the population**. **THE AGGREGATE IS WHERE THE HARM WOULD LAND**, so it is guarded there too: `summarise` keeps three buckets that sum to the total, states the denominator caveat in words when anything is unknown, and the export list refuses anything matching /rate|percent|score/ — a single "success rate" is exactly the number somebody computes by dividing reached into total, silently treating every unknown as a failure. **EVERY VERDICT CITES ITS EVIDENCE**: a verdict that cannot name an event is `not_recorded` BY CONSTRUCTION, so the citation is a field rather than a debugging aid. **THIS IS NOT A QUALITY MEASURE AND THE SEPARATION IS STRUCTURAL** — W15 owns the clinician's own judgement of whether a visit was useful; nothing here reads a narrative, scores anything or returns an assessment, asserted on the export list and the key list, because a dashboard blurring the two would present records completeness as clinical quality: the most flattering error available and the hardest to notice. THE CHAIN IS DECLARED, NOT HARDCODED — booking (W10) and referrals (W93) keep their own state machines and this takes a `ChainDefinition`, so what counts as getting somewhere is data a reviewer reads in one place; an event kind the definition does not mention contributes NOTHING rather than being guessed at. Order-independence is BY CONSTRUCTION not by tie-break (the verdict is the furthest stage over the SET — no sort, no tie), asserted with every event sharing one timestamp in both directions, which is W167/W168's lesson applied at design time rather than after. A stop recorded AFTER a demonstrated completion does not reclassify the chain — that is an anomaly to look at, and quietly absorbing it would hide it. Verify: pnpm verify green, 1875 tests / 148 files, audit:gate PASS |
| W171 | done | builder-B | 2026-08-10T15:50Z | 3aca3fc | src/outcomes/escalation-monitor.ts + 18 tests. **W121 REPORTS AN ESCALATION AS `unrouted` — A SNAPSHOT — AND A SNAPSHOT IS THE WRONG SHAPE FOR THE PROBLEM.** An unrouted escalation is not a state, it is a question nobody has answered, and the only fact that makes it uncomfortable is how long it has been waiting; a list that looks the same on day one and day ninety is a list nobody acts on. So this ages them, and both consequences are INHERITED REFUSALS rather than choices this unit gets to make. **AN AGE IS A FACT; A THRESHOLD WOULD BE A CLINICAL JUDGEMENT** — W121 says it outright, "a timeframe attached to a clinical escalation is a judgement about urgency, and that judgement belongs in content the founder signs off", so **the obvious monitoring feature is the one thing this module must not build**: no `overdue`, no breach threshold, no red/amber/green, no severity. It reports days, oldest first, states ON THE PAGE that it is not saying whether that is too long, and a test scans every rendering — both empty variants and the populated one — for the vocabulary of a verdict; the export list is asserted off the real module namespace so no helper exists that could classify an age (W68's guarantee-as-absence). **NOTHING LEAVES THE LIST BY GETTING OLD**: routing is the only thing that closes an escalation, because ageing out would be W68's failure — the mechanism producing silence — arriving on a timer, and the older an unanswered question is the more wrong it would be to drop it (W134's unanswered-referral shape: the discomfort IS the signal). **AND ZERO IS NOT AN ANSWER** — an empty list has three quite different causes and the report names which: nothing evaluated at all (**the state today**, since W118's catalogue and W121's rules both ship empty), evaluated and none flagged, or everything flagged has a rule. "No escalations needed" would collapse all three into the most flattering one, and none of them means *needed* anyway, since a criterion only ever fires on a fact somebody recorded; coverage counts sit beside the answer (W96) and `oldestAgeDays` is **null, not zero**, because zero days reads as "something arrived today". `firstSeenAt` is preserved across sightings and that is the whole mechanic — **resetting the clock on every observation would leave the list still CORRECT about which escalations are open, so the failure would be invisible** — and a reopened escalation keeps its original `firstSeenAt` while incrementing `reopenings` ("outstanding since January, briefly answered in February" is the true shape; restarting the clock on withdrawal would flatter the record). Ties on age resolve by key, checked with W167's `foldIsOrderIndependent` in both directions. Mutation-checked both ways: resetting `firstSeenAt` fails 4 tests, collapsing the empty reason or returning 0 for the oldest age fails 3. Verify: pnpm verify green, 1893 tests / 149 files, build, audit:gate PASS |
| W172 | done | builder-A | 2026-08-11T21:00Z | 5b6127b | src/outcomes/agreement.ts + 17 tests. **SEEDED AND REPRODUCIBLE IS NECESSARY AND NOT SUFFICIENT, AND THAT GAP IS THE UNIT.** A seeded sample is reproducible; if the seed is a FREE PARAMETER at report time, somebody tries a hundred seeds and keeps the one with the best agreement rate — reproducibility then belongs to the flattering sample rather than defending against it. So **the seed is DERIVED, not supplied**: a hash of the period plus a fingerprint of the population. Re-rolling is still possible (it always is) but **cannot be done invisibly** — it needs a different period, which the report states, or a different population, which changes the fingerprint the report also states. There is no `seed` argument, pinned at COMPILE TIME by a `@ts-expect-error` on a fourth argument and on the export list. The draw is taken from a SORTED pool, so a store's row order cannot move the sample — the order-dependence class with a motive attached. **A DISAGREEMENT IS NOT AN ERROR, and this is the part that matters most**: treating the pathway as ground truth and a disagreeing specialist as a deviation INVERTS THE PRODUCT — the specialist is the clinical authority and the pathway is a written summary of criteria. So the basis vocabulary has four members naming four different findings with four different fixes (record incomplete · criteria too broad · criteria too narrow · judgement not encodable) and **a test asserts none of them matches /wrong|error|incorrect|failure|mistake|deviation/**. **EQUAL PROMINENCE IS IMPLEMENTED AS ORDERING**: disagreements render FIRST, in full, never truncated, with the same per-case fields as agreements and the reviewer's own words verbatim — a report leading with an agreement rate and listing disagreements underneath would be technically complete and read as reassurance. Counts sit alongside the rate rather than being replaced by it. W170's third answer carries over: an unreviewed case is `not_reviewed`, counted separately, because folding it into `agreed` would make an audit THAT HAS NOT HAPPENED look like one that went well — and a case reviewed outside the draw does not join the numbers, which would be the cherry-picking arriving by the back door. Verify: pnpm verify green, 1910 tests / 150 files, audit:gate PASS |
| W173 | done | builder-B | 2026-08-10T21:20Z | 8d48e09 | src/outcomes/dashboard.ts + app/console/outcomes + 14 unit tests + 6 e2e. W170 built what an outcome IS; this points it at the one rail with recorded events (referrals, W93) and the work is **carrying W170's refusals onto a SURFACE, because a dashboard is where they get lost**. **TWO DENOMINATORS, NOT ONE — and the second is the one nobody writes.** W170's `basis` answers "how many are unknown"; the question it does not answer is **which chains are on the rail at all**. A referral written on paper, or held in a system nothing has been ingested from, is not a chain with an unknown outcome — it is not in the population. "3 referrals, 1 unknown" invites the reading "we wrote 3 referrals", so `DASHBOARD_BASIS` states the population in words and both sentences render. The zero state is the same distinction sharpened: **an empty rail is not a rail whose outcomes are all unknown**, and the two lead an operator to opposite actions. **IT RANKS NOBODY, AND THE GUARANTEE IS STRUCTURAL RATHER THAN A DECISION NOT TO.** W83 named the failure exactly — a per-clinician breakdown "turns a capability graph into a productivity board" — and **outcomes are a far shorter step from there, because what is being counted is largely whether somebody at the OTHER end wrote anything down**, the thing a GP controls least. `ReferralEvent` carries no clinician, nothing here takes one, and the export list is asserted off the real module namespace: the only form of that promise a future edit cannot quietly drop. **No rate, percent or score** (W170's rule, its reason): one figure comes from dividing reached into total, silently treating every unknown as a failure, and on this rail the unknowns ARE most of the population. **THE UNKNOWN BUCKET IS NOT STYLED AS A PROBLEM** — identical classes on all three, asserted by COMPARING them rather than denylisting colours (a new warning class slips past a denylist), so the model's refusal cannot be undone in CSS. The page leads with **what would SETTLE the unanswered ones** (W120's move): a count an operator can only feel bad about is a scoreboard, "1 waiting on an appointment being recorded" is record-keeping somebody can do; asks are counted per event kind, never per person, and tie-break by name (W167). **W170's verdict is a max over a SET, so it cannot see a SEQUENCE** — booked/cancelled/rebooked reaches the same stages as booked once — so `appointment_cancelled` is left OUT of the chain definition rather than mapped to a stop it cannot reason about, and the page says W93's replay answers that instead of approximating it. **Two defects found while verifying, both mine, both invisible to review.** (1) The copy scan banned "by clinician" and tripped on the page's own sentence REFUSING to break outcomes down by clinician — W95's collision exactly — so it now subtracts the caveats section and asserts the subtraction was non-empty and that the caveats really contained the phrase. (2) The bucket `<dl>` put a `<p>` inside a dt/dd group; **axe caught it, review did not**. Mock referral seed gains an opt-in cancelled referral behind `?cancelled=1` so all three verdicts render populated, rather than widening a shared fixture the referral console's e2e pins. Surface census updated and re-checked in both directions (W102). Verify: pnpm verify green, 1924 tests / 151 files, build, audit:gate PASS; e2e/outcomes.spec.ts 6/6; **axe zero violations across every console surface including /console/outcomes** |
| W174 | blocked | — | — | — | **FOUNDER GATE G3 — not buildable by any builder.** Message-outcome auditing: did an invitation lead anywhere? → verify: outcomes trace to delivery receipts. **Why blocked:** There is no send path and nothing has ever been sent, so there are no delivery receipts to audit. Building this now would produce a dashboard of zeros that reads as 'no effect' rather than 'no attempt'. |
| W175 | done | builder-A | 2026-08-11T21:45Z | 1e65d7e | src/engine/arm-stability.ts + 12 tests. **THE PROPERTY ALREADY HELD BY CONSTRUCTION, AND SAYING SO HONESTLY IS MOST OF THE UNIT.** `assignmentUnit` hashes `practiceId:patientId` and nothing else, so a vertical version change cannot move an arm — the version is not an input to the draw. **A property that holds by construction is worth exactly what the construction's stability is worth**, so the work was making it hard to break rather than demonstrating it once. **THE CHANGE THIS GUARDS AGAINST IS NOT MALICE, IT IS A REASONABLE-SOUNDING IDEA**: "different verticals should get independent arms, so salt the hash with the vertical id". That single line silently re-randomises every patient at every practice on every version change — and **nothing else in the tree would notice**, because the result would still be balanced, still deterministic, still auditable, and completely different from last month's. So `holdout.ts`'s own SOURCE is pinned: a test fails if it mentions a vertical, version, binding or pathway, proven non-vacuous by adding such a reference and watching it fire. Compile-time too: `@ts-expect-error` on a third argument to the draw. **THE INVARIANT IS ALSO CALLABLE** — `assertArmsUnchanged` is a fail-closed guard a migration path can hold, throwing rather than returning findings (W6's posture), naming WHO moved and in which direction. Drift is matched BY PATIENT ID not by position, because a migration may reorder the panel and a positional compare would report everybody as moved or miss a real move offset by another; arriving and leaving are deliberately NOT moves, since burying them here would hide the one event this guard is about; and the report is sorted so two runs over the same drift read identically. **ONE PROPERTY DELIBERATELY NOT CLAIMED, AND IT IS THE MEASUREMENT-SAFE DIRECTION**: a patient who falls out of a vertical's scope KEEPS their arm, because dropping people from the control arm exactly where the intervention changed is how a control group stops being one. Also asserted: rate changes stay monotone (raising only ADDS holdouts, never releases one), so even if a migration ever did touch the rate the damage is bounded; and `VerticalBinding` carries no field a version change could reassign anybody through. Verify: pnpm verify green, 1936 tests / 152 files, audit:gate PASS |
| W176 | done | builder-B | 2026-08-10T22:10Z | ff819a1 | src/outcomes/time-to-escalation.ts + 16 tests. An escalation criterion fires on a fact somebody recorded; between the fact entering the record and the pathway being evaluated there is an interval, and "this sat there for eleven days" is a fact about the record nobody can currently see. **IT IS ALSO, OF EVERY NUMBER THIS TREE PRODUCES, THE ONE MOST OBVIOUSLY ASKING FOR A TARGET** — so the unit is mostly the refusal, and **none of the three reasons is "targets are bad in general"**. **(1) A TARGET WOULD CHANGE CLINICAL BEHAVIOUR, AND NOT IN THE DIRECTION IT LOOKS LIKE.** The measure is only defined for escalations that were RAISED, so **the cheapest way to improve it is not to notice faster — it is to escalate sooner, on thinner grounds, more often**, because each marginal escalation lands near zero days and pulls the figure down. That is a change in what clinicians do produced by a management number, and what it pushes toward has its own cost: the receiving clinician's attention and the patient's alarm. W121 refuses a timeframe because urgency is signed-off content (G5); **a target is that same judgement arriving as a KPI**. **(2) IT IS A SURVIVORSHIP FIGURE** — a practice that escalates nothing has no measurement, and *no measurement looks identical to nothing having gone slowly*. So `notEscalated` lives INSIDE the report: there is no way to obtain the measure from this module without also obtaining the size of the population it excludes (W170's rule, a different denominator). **(3) THE INTERVAL IS NOT A FACT ABOUT ANY PERSON** — `factRecordedAt` is when the fact entered the record, so **a slow import and an inattentive week are the same eleven days**, and attributing it would be W83's productivity board built on a number that partly measures an integration. Nothing here takes or returns a clinician. Consequently **no mean or median**: a central tendency is the figure a target attaches to, and here it would summarise only the escalations that happened — wrong the way W170's success rate was wrong. Every measurement is listed, with the spread; the spread is **null not zero** when nothing is measured, and the two kinds of nothing (nothing evaluated · evaluated-and-none-escalated) are distinguished. `WHY_NO_TARGET` makes the argument in full rather than summarising it — the reader it is for is the one about to ask for a target — and **it renders on the EMPTY report too, which is the one somebody looks at and says "so what would good look like?"**. The export-list pin exempts `WHY_NO_*` **as a rule rather than as an exception for one name**: an export saying why there is no X is the refusal, not the thing. New fold site **declared in W167's register rather than routed around** (dodging the detector by writing `count - 1` instead of `.length - 1` would have been exactly the hiding place the register warns about); rationale is `src/pms/ingest.ts`'s — the fold reads a NUMBER off the last element, so tied entries give the same answer either way round. Mutation-checked: dropping the excluded population from `basis` fails 2 tests, adding an `ACCEPTABLE_TARGET_DAYS` export fails the surface pin. Verify: pnpm verify green, 1952 tests / 153 files, build, audit:gate PASS |
| W177 | done | builder-A | 2026-08-11T22:30Z | 1bfbf84 | Practice audit-trail export → verify: golden export that carries its own caveats where it travels away from the product that explains it (W149's pattern). |
| W178 | done | builder-B | 2026-08-10T23:05Z | 002463c | src/quality/order-regressions.ts + 49 tests. W167 declared every fold site; **that stops a NEW fold arriving undeclared and does nothing about an OLD fix being undone** — nor does it say whether the fixtures that caught these defects still catch them. Each entry holds the tied input, the PRE-FIX fold reconstructed from the fix's own commentary, and the **REAL shipped function, imported and never copied** (a corpus holding its own copy of the fix tests the copy while the real code drifts away unwatched). Asserted per entry: the fixture still DISCRIMINATES the fix, the shipped fold is order-independent on it, and its answer is **the safe one** — order-independence alone is satisfied by returning the same wrong thing every time. **THE PLAN'S COUNT OF EIGHT DOES NOT SURVIVE BEING CHECKED, and building to it would have been the fiction the corpus exists to prevent.** §Y3 names "W123, W129, W137, W142, and the two here, plus two caught pre-emptively during Q11". W123's and W137's rows describe **no fold tie** (W123's was a seed helper doing a bulk replace, W137's was erasure not reaching the referral rail); the two Q11 pre-emptive catches were tie SENSITIVITIES fixed before anything shipped wrong, which is not a defect with pre-fix behaviour to reproduce; and the count predates Q13, so it omits W168 and W128. **Seven real defects, each one shipped, found and fixed.** **TWO FINDINGS ON THE FIRST RUN, both recorded not fixed.** **(1) `pathwayAt` IS STILL ORDER-DEPENDENT ON A TIE** — W128 removed the null (the fold no longer claims no version was in force) but **did not make two same-instant publications resolve the same way twice, and nothing at the time claimed it had**; `replayPathway` sorts with a comparator returning 0 for equal `at`, so storage order decides which supersedes which, and fed both ways this corpus gets a different version each time. Latent behind G5, but it is the version identity a verdict cites. Per W142 the answer is probably to REPORT the ambiguity, which changes the return type and every caller — **filed for W181**, not smuggled into a corpus unit. The entry carries `residual` and the suite **pins the defect as still present**, so a future fix announces itself as a test that must be updated rather than silently losing the finding. **(2) W167'S REGISTER DOES NOT WATCH REPLAYS** — its detector matches folds that pick ONE ELEMENT, and two of these defects lived in sort-then-replay code (`acceptanceStatus`, `replayReferral`) which folds a collection into one answer just as much. Not widened (that is W168's unit again, and a replay has no last element to tie on because the ITERATION ORDER is the answer); the cross-check names the two so the omission is written down instead of invisible. **Not every entry failed the same way and the shape had to admit it**: W128's fold returned null in BOTH orders — consistently wrong on a tie rather than resolving it badly — so the run-it-the-other-way check that catches the other five **would have passed there**; a mutation check found it. `preFixSymptom` records which, so the corpus never asserts a property an entry never had. **Three of my own errors caught by the gate rather than by review**: the first discriminates-check compared only the forward order, which an order-dependent fold passes BY DEFINITION; the W128 fixture used hand-written hashes with empty criteria so every draft was silently rejected and the fixture proved nothing; and the register flagged this module itself (its pre-fix reconstructions are folds) — declared with a tie-break disposition rather than excluded by name, because an excluded file is a place to hide something. Verify: pnpm verify green, 2004 tests / 155 files, build, audit:gate PASS |
| A11Y-1 | done | builder-A | 2026-08-12T00:40Z | 8fa10be | e2e/a11y.spec.ts `settle()`. The public-pages axe scan was red on main since 5843fab. **MY OWN W179 DIAGNOSIS OF IT WAS PARTLY WRONG AND IS CORRECTED HERE**: I recorded a permanent contrast defect. There is none — `--s-sage` #55663f on `--s-paper` #f7f3ea is **5.65:1**, comfortably AA. The 1.14:1 and 3.97:1 readings were that same token composited at ~0% and ~83% opacity **mid-fade**. **WHY W49'S EXISTING ESCAPE HATCH STOPPED WORKING IS THE USEFUL PART.** W49 scans under `prefers-reduced-motion` and applied it with `emulateMedia` AFTER `goto`, which is fine for CSS — CSS re-evaluates whenever the query changes, which is why it fixed the care finder's `.screen`. The storybook animates with **motion/react, whose reduced-motion gate is a HOOK read once during render** (`initial={reduce ? false : "hidden"}`). **A media override applied after mount cannot reach a decision JavaScript has already made**, so the suite went on sampling mid-fade. **THE FIX IS A DETERMINISTIC SETTLE, AND THE TRAP INSIDE IT IS THE FINDING WORTH KEEPING**: immediately after `goto` the eyebrow sits at opacity 0 with `document.getAnimations()` returning **ZERO** — React has not hydrated, so the animations do not exist yet. **An empty animation list means NOT STARTED, and any helper that reads it as FINISHED scans the most misleading frame available.** So `settle()` waits for networkidle FIRST (hydration + fonts, which is what registers the animations), then for the list to stop running, excluding infinite iterations so a decorative loop hangs nothing. **MEASURED, NOT ASSUMED, INCLUDING AGAINST MY OWN FIX**: with `settle()` in place the scan passes even with the reduced-motion emulation removed entirely, so the emulation is NOT what makes this green. It is kept (moved before the first navigation so it takes effect at all) because W49 chose to scan what a reduced-motion user sees, and the comment says plainly that it is not load-bearing — leaving the next reader to defend the wrong line is its own defect. **Non-vacuity both directions**: reverting `settle()` to the old post-goto emulation reproduces the red; and injecting a genuinely failing token (#a8b894, 2.0:1) at REST still fails the suite, proving the wait did not become a way to pass by outlasting problems. Stable 12/12 under `--repeat-each=3` (the original defect passed in isolation and failed under load). **Root cause recorded separately as A11Y-2, not fixed here** — it is a design decision and this firing owns one unit. |
| A11Y-2 | done | builder-A | 2026-08-12T01:45Z | a3a9a70 | app/story-landing.tsx + e2e/landing.spec.ts + src/quality/landing-motion.test.ts. **The root cause A11Y-1 diagnosed and did not fix.** The storybook server-rendered **24 elements at inline `opacity:0`, including the `<h1>`** — motion/react renders `initial` on the server and `useReducedMotion()` cannot read a media query during SSR, so the hidden state shipped unconditionally. Headline unreadable until hydration, unreadable **full stop** if the bundle never arrived, and reduced-motion readers got the entrance anyway. **FIX: ANIMATE FROM A VISIBLE STATE** — slide and easing unchanged, fade gone. Alternatives rejected with reasons: a mounted flag **suppresses the animation entirely** (motion reads `initial` once, so flipping it later does nothing), and `<noscript>` rescues scripting-disabled readers but not a bundle that fails to load. **The trade is a design call so it is written into the component, not buried**: this page loses its fade, on the judgement that a health product's landing copy must be readable when the JavaScript is not. Outcome and mechanism pinned separately: e2e loads `/` in a **genuinely JS-disabled context** and asserts computed opacity, plus a refusal of any `opacity:0` in the SSR HTML of every public page (named per page); `landing-motion.test.ts` pins the source (W175's pattern) because **the regression is a reasonable-sounding edit** — "this section should fade in" looks harmless in review, still passes axe once hydrated, and only breaks for a reader who files no bug report. **TWO OF MY OWN TESTS WERE VACUOUS AND THE MISTAKES ARE THE INSTRUCTIVE PART.** (1) The no-JS test first used `toBeVisible()` alone and **went on passing with the defect restored** — Playwright's visibility is "has a box and is not `visibility:hidden`", which an `opacity: 0` element satisfies; a check for invisible content that calls invisible content visible is worse than no check. (2) The anti-deletion pin first matched any `y:` value, which `y: 0` satisfies, so **flattening every offset to zero passed the test written to prevent exactly that**. Both now assert the thing they name. An e2e asserting the animation still runs at runtime was written then **removed rather than retried** — whether an animation is in flight at a given instant is a race, and a flaky guard on a green-commits-only tree is a liability; the source pin covers it deterministically. Non-vacuity both directions on the final tests. Verify: pnpm verify green, 2024 tests / 157 files, build, audit:gate PASS; landing + axe e2e green. |
| W179 | done | builder-A | 2026-08-11T23:35Z | b17aa0b | src/ops/silence.ts + 16 unit tests + 4 e2e, wired into /console/ops. The page rendered **"No offers outstanding."** — true of a quiet week and true of a feed that died on Tuesday, and **those send an operator in opposite directions**. **THE LOAD-BEARING RULE: THE REASSURING READING REQUIRES PROOF; EVERY ALARMING READING IS REACHABLE BY DEFAULT.** `nothing_eligible` ("do nothing") is the only cause demanding positive evidence on all three — feed healthy, data fresh, records actually delivered — and is never a fallback or an else-branch, because being sent to check a healthy feed costs five minutes while being reassured through a three-day outage costs a practice its patients. W36's stale-data posture aimed at a different question: not *may we act* but **may we reassure**. **CAUSES ARE A SET, NOT A WINNER** — a feed down while sending is paused has two answers, and naming one produces the worst outcome available: an operator releases the switch, sees the zero persist, concludes the product is broken. An e2e releases the switch and asserts the feed cause still stands. **ABSENCE OF EVIDENCE IS ITS OWN ANSWER**: `OpsState.feed` defaults to null → `cannot_determine`, never "quiet" (W120's third value at the surface most likely to be misread as reassurance). The zero is a **union** (`Reading`), so a caller cannot obtain one without its causes — the defect lives in the type, not in a review comment; `SILENCE_COPY` is `Record<SilenceCause, …>` so a new cause fails the build until its copy exists, since **a cause inheriting another's sentence is how two states quietly start rendering the same**. Copy checked with **W23's `lintCopyBundle` and W54's `lintPartyToCare` APPLIED, not re-implemented** — which caught a real hit: a draft said "Treat this as unknown" and the genuine no-clinical-claims rule matches the bare verb where my hand-rolled regex did not. The copy changed, not the rule. **Out of scope and named**: a NON-zero count off a stale feed is also misleading; W36 already refuses to send from stale data so the damage is display-only, and claiming coverage here would be worse than declining it. Non-vacuity three ways: making the reassuring reading a fallback fires the gate test + exhaustive sweep; returning one cause fires the three set-not-winner tests; restoring the page's original sentence fires all four e2e while both pre-existing W19 specs keep passing. Verify: pnpm verify green, 1984 tests / 155 files, build, audit:gate PASS; ops e2e 6/6; console-surface axe green incl. /console/ops. |
| W180 | done | builder-B | 2026-08-11T01:55Z | 8b12693 | 6 record classes declared in W106's registry + 12 tests. **Q14 BUILT A RAIL ON TOP OF A RAIL, AND FOR THAT SHAPE THE PRIVACY QUESTION IS NOT "WHAT DOES IT STORE" — IT STORES NOTHING — BUT "DOES ERASING THE SOURCE EMPTY IT".** If yes, erasure needs no new code and **gains no new place to be forgotten**; if no, somebody has to REMEMBER to scrub it, which is precisely W51's critical finding. So the composition is PROVED, not asserted: seed the referral rail, scrub a patient through **W137's real scrub — unchanged, and unaware an outcome rail exists above it** — and ask the dashboard. Their verdicts are gone; the other patient's are untouched, because **a scrub that emptied everything would pass the first check too, and reaching too far is a different failure rather than a safer one**. Five modules hold no patient identity; `dashboard.ts` is `derived`. **Declaring a module that holds nothing IS the point** — "holds nothing" is a CLAIM, and this registry is where claims about patient identity get written down and checked. Two structural assertions back the classifications instead of trusting them: **nothing under src/outcomes holds a globalThis store**, so `derived` cannot quietly stop being true, and **no Outcome names the patient whose referral it is about** — asserted against an input that demonstrably carries both patient ids, so the absence is not vacuous. Mutation-checked: leaking patientId onto the returned outcome fails that test. `audit-export.ts` is the class where handling is **not a convenience** — an export leaves the product, so **erasure can never reach it; a downloaded file is gone** — and holding no patient identity is the only safe answer there. **FINDING, recorded not fixed: W106's detector only sees globalThis-backed stores, so it never looked at any of these** — its test passes today whether or not Q14 exists. **Tree-wide, 42 modules under src/ mention patient identity and are undeclared.** Most are pure transforms where identity passes through without persisting, **but "most" is not a control.** Scoped this unit to the Q14 surface, where the directory check cannot fall behind, and left the tree-wide sweep as the larger unit it is. One imprecision of mine caught by the existing gate: W106 requires a `derived` rationale to EXPLAIN its derivation and my first wording said "at request time" without saying derived — the registry's own test refused it. Verify: pnpm verify green, 2032 tests / 158 files, build, audit:gate PASS |
| W181 | done | builder-A | 2026-08-12T02:45Z | 45ce161 | Q14 hardening across W170–W180 + the A11Y pair, run with the review skills (law 5). **Five findings, each verified against the code before acting rather than taken on report.** **CRITICAL — THE OPS QUEUE WAS NOT PRACTICE-SCOPED AT ALL.** `queueView()` folded every invitation in the rail with no filter while `Invitation` has always carried `practiceId`; pre-W166 there was one practice and it was invisible, post-W166 any signed-in practice saw another's counts, invitation ids, patient ids and session dates. **Two things make it worse than it sounds.** The leak also **SUPPRESSED W179's silence explanation** — a non-zero global count meant the reader never learned their own feed had delivered nothing, so the exposure *hid an outage*. And chasing it surfaced that the rail seeds `prac-demo` while the console mints `prac-${n}`: **the page was not showing a superset of this practice's queue, it was showing a different practice's queue entirely**, and the missing filter is precisely what hid the mismatch. `SEED_PRACTICE_ID` exported; the fixture re-keys deliberately instead of relying on a coincidence that used to work. `OpsState.feed` was likewise global behind second-person copy — one practice's outage explaining another's zero is **a confident wrong answer, worse than none** — now keyed by branded `PracticeId`. **HIGH — THE AGREEMENT REPORT SHRANK ITS OWN DENOMINATOR.** Sampled cases with no record were dropped, so `total` became "cases someone happened to file": a sample of ten with two reviews reported on two, rendering **"Sampled 10 of 40" above "All 2 sampled case(s) have been reviewed"**. That is **the survivorship reading the module exists to refuse, arriving through the back door** — the reassuring number computed over exactly the cases that could not contradict it. Untested because every existing test seeded a record for every sampled id. `noRecord` now counted separately from `notReviewed`: never-opened and opened-but-unanswerable **ask different people for different things**. **HIGH — the outcomes console asked "did what we wrote go anywhere" with the TENANCY query.** `eventsFor` scopes to party-on-either-side (right for *may they see it*, wrong for *did they write it*), so an inbound referral landed in both practices' totals **under descriptions that cannot both be true**. New `sentEventsFor`, kept separate rather than added as a flag, for `sentBy`/`sentTo`'s reason. **MEDIUM — `wouldSettleIt` read the NEXT stage's stop kinds instead of the current stage's**, so a referral at "written" was told an appointment would settle it but never a cancellation, leaving `SETTLEMENT_ASK_COPY.referral_cancelled` dead. **TWO EXISTING TESTS PINNED THE DEFECT AND ARE CORRECTED HERE** — `dashboard.test.ts` asserted the buggy list *directly beneath its own comment saying "ref-a waits on an appointment or a cancellation"*. The comment stated the intent; the assertion had been written to match the code. **MEDIUM — an empty chain definition returned `reached` with no evidence** (both indices −1), against the module's rule that no verdict issues without a citation; now refused. Also removed a dead `redirect` import and unused `getConsole()` stranded since W166 (nothing in verify catches unused locals). **Security review over the diff: no new HIGH/MEDIUM.** The mock route's new `practice` param was considered and dismissed — the handler 404s outside the synthetic phase via W13's guard and already reset the whole rail; its one observation (bare `string` feed keys) is applied. Verify: pnpm verify green, 2041 tests / 158 files, build, audit:gate PASS; ops + outcomes + two-practice e2e 17/17 including the existing "one practice's rail is not visible from another". |
| W182 | done | builder-B | 2026-08-11T02:58Z | 9c58398 | docs/GATE-DOSSIER-Q14.md + src/messaging/send-path.test.ts (4 tests). **Q14 COMPLETE.** Q13's dossier could price G5 as "nine modules ship a tested mechanism over an empty catalogue" because G5 is one decision that fills a container; **G3 is not that shape, and saying so is most of the unit**. **THE FINDING, STATED FIRST: G3 ALONE OPENS NOTHING.** To send a real SMS to a real patient you need a real patient (G2), a real record of who they are and what is due (G1), and permission to text them (G3) — **opening G3 by itself authorises sending to a population that does not exist in this system.** Read the other way that is the useful news: **G3 is not the bottleneck it is usually described as.** The Y2 audit's "the send path is the largest gap between modelled and operative" has been true for two years and **is not waiting on the SMS ruling — it is waiting on G1 and G2**, with G3 the last small step rather than the first big one. So the ask is not "open G3"; it is **"say whether G1/G2 are being pursued at all"**, and answering G3 first changes nothing. **WHY THE QUARTER WAS NOT WASTED WAITING**: W170 made the chain a DECLARATION rather than a hardcoded state machine, so **an invitation chain is a new `ChainDefinition`, not a new engine** — `sent → delivered → booked → attended` with undelivered/failed/STOP as stop kinds is a DATA change, and the three-verdict rule, evidence citations, denominator copy, no-rate export guard and the whole dashboard already work over any chain. W179's silence module is the one written FOR live data; W180's `derived` classification holds for an invitation rail by the same argument. **AND THE CENTRAL CLAIM IS NOW A CONTROL RATHER THAN A CLAIM.** Every previous dossier established "the send path is unwired" by somebody looking, and **Q13's found a two-year-old inconsistency that survived precisely because looking is something people stop doing.** `send-path.test.ts` fails the day anything outside the simulator constructs an SMS adapter or calls `send` — the day the dossier needs rewriting — and **names the dossier in its failure message**; same move as W102/W106/W107/W167. Verified: the ONLY non-test construction in the tree is `src/sim/harness.ts:143`, a `MockSmsAdapter` sending to `synthetic:<patientId>`. Mutation-checked by wiring one into the outreach page — caught. **Every factual claim re-derived rather than carried forward, which caught one of mine**: I wrote that `approval.ts` "ships with nothing approved", implying an empty catalogue like G5's registries; it has **no catalogue at all** — approvals are passed in and `approveTemplate` is called from nothing but its own tests. Counter-argument recorded: after G3 every refusal stops being enforceable by inspection and becomes operational, and **"tested" is not "has never once been wrong in production"**. Verify: pnpm verify green, 2045 tests / 159 files, build, audit:gate PASS |
| W183 | done | builder-A | 2026-08-12T04:00Z | c93e7a9 | src/directory/profile.ts + 17 tests. **A profile is the first thing this product would publish ABOUT A NAMED PERSON to people who are not the practice** — everything else the tree emits is internal or about the practice, and that changes what a mistake costs: a wrong dashboard figure is a wrong figure; **a wrong sentence beside a clinician's name is regulated advertising with their registration attached to it**. **SO THE MODEL IS THE CONTROL, NOT A LINTER OVER THE MODEL.** W114 already refuses the specialist title beside a narrow scope and that check stays — but **a check runs, and a check that runs can be routed around by a caller assembling the object another way**. The gate is that the bad shape cannot be WRITTEN. **THE s 133 CONSTRUCTION**: using "specialist" without specialist registration is an offence, and the damage is worst beside a narrow scope where it reads as **"specialist in <condition>" — a claim nobody made and everybody hears**. A profile is one of two exclusive shapes: a GENERAL profile carries declared focus areas and no specialty; a SPECIALIST profile carries a recognised Ahpra specialty and its `focus` is typed **`never`**. Writing a specialty beside a focus list is **a type error — not a lint failure, not a runtime refusal, not a rule somebody can forget to call**. `RecognisedSpecialty` is a closed list rather than a string, so "specialist in PMOS" cannot be invented; **the nearest representable thing is a general profile with a declared focus, which is what such a clinician actually is** — refusing a shape is only half an answer and the other half is pinned too. **ABSENT BY CONSTRUCTION**: no rating, star, score, review, testimonial, endorsement — and **no free-text biography, which is the interesting refusal**: the field nobody can lint meaningfully, that everybody wants, and where every refusal above reappears in prose. Every human-authored string is a `ScopeStatement` minted only by W114's constructor (W23 + W6 rules), **reached through its own module rather than copied**, with a test asserting this file implements no linter of its own. `REFUSED_FIELDS` states WHY each is refused as data, because **a refusal nobody wrote down is indistinguishable from an oversight and gets "fixed"** — removing one now means deleting an argument. `PROFILE_FIELDS` is tree-checked against the keys of a real profile of each shape, which is **what lets W184 promise its linter reaches every field a profile CAN emit rather than every field somebody remembered**. **FOUNDER GATE G6**: `SHIPPED_DIRECTORY_PROFILES` empty and pinned empty (the W161–W163 device) — the model may exist before the Ahpra advertising review; the content may not. **One of my own tests needed fixing and the failure was instructive**: the banned-field scan matched `REFUSED_FIELDS`, whose keys ARE the banned names, so **the registry documenting the refusals looked exactly like a declaration of them and the check reported the refusal as the violation**. Now scans interface bodies and asserts it found them, so an empty scan cannot pass. Non-vacuity three ways, **the first two at compile time**: letting a specialist carry a focus list, and opening the specialty list to `string`, each make a `@ts-expect-error` directive UNUSED and fail tsc; adding a `rating` field fails the declaration scan. Verify: pnpm verify green, 2062 tests / 160 files, build, audit:gate PASS. |
| W184 | done | builder-B | 2026-08-11T04:00Z | 9bc8488 | src/directory/copy-lint.ts + 14 tests. W183 made the bad SHAPE unwritable and that stays as the stronger control; **this covers what a shape cannot — the bad SENTENCE.** "Dr Smith specialises in diabetes" carries no `specialty` field and not the word "specialist", **passed every existing check, and says the same thing to every reader**: a linter that only bans the word has been routed around by anyone writing naturally. **THE FINDING: three profile fields are free text and none was linted anywhere** — `displayName`, `languages`, `location.suburb`. **`displayName` is the one that matters.** W183 calls it "the only unlinted free text, because it is a fact about them" — true of a name, and **a name field is also exactly where a title goes**. "Dr Jane Smith, Diabetes Specialist" is one string, the most natural thing in the world to type, and nothing in the tree would have stopped it being published beside a registration number. **RULES APPLIED, NOT RE-IMPLEMENTED** (W139's device, second use): W23's and W6's sets are unioned from their own modules and `directoryCopyRules` proves it — **handed a rule that does not exist in the tree it still comes back**, which is only possible because the union is computed rather than transcribed. Added: the specialisation claim in prose, expertise claims, comparisons between named clinicians. **EVERY EMITTING FIELD DISPOSED OF, CHECKED BOTH DIRECTIONS** against `PROFILE_FIELDS`, so a new field fails until somebody says how it is linted (mutation-checked); `focus` is exempt because W114's constructor already brand-checks it at mint time, and the entry NAMES what checks it. **TWO ERRORS OF MINE, BOTH CAUGHT BY MY OWN TESTS.** First I ran only the directory-specific rules over a name, reasoning that surnames collide with superlatives — **which dropped `no-specialist` from the field most likely to carry a title, the exact hole the unit exists to close**. Second fix was to exempt the colliding RULES, also wrong: **W6's `no-benefit-claims` bundles best|leading|expert|specialist into one pattern**, so exempting it to publish Dr Best would have exempted the title claims too. The collision is with three words that happen to be surnames, not with any rule, so **the exclusion is by matched WORD** and each entry carries its argument. Residual stated rather than hidden: "Dr Jane Smith — Best in Coburg" is permitted, the price of Dr Sarah Best having a publishable name, with G6's Ahpra review of every field as the mitigation. **A third slipped past vitest and was caught by typecheck in the gate** — two fixtures used enum members that do not exist; runtime tests do not typecheck fixtures. Verify: pnpm verify green, 2076 tests / 161 files, build, audit:gate PASS |
| W185 | blocked | — | — | — | **FOUNDER GATE G6 — not buildable by any builder.** Public directory launch → verify: Ahpra advertising review passed; every profile field traced to a reviewed claim. **Why blocked:** Public directory launch requires Ahpra advertising review of every profile field. The model and the linter (W183/W184) ship; the launch is a founder act. |
| W186 | blocked | — | — | — | **FOUNDER GATE G5 — not buildable by any builder.** Dermatology pathway content → verify: two-person sign-off per W119. **Why blocked:** Dermatology pathway content, same reason as W161. |
| W187 | done | builder-A | 2026-08-12T05:10Z | 95a3c56 | src/directory/render.ts + 19 tests. **The sentence the whole directory turns on**: a clinician who has spent years on women's health IS more use to a woman looking for help, so saying nothing makes the directory worthless — and saying it the obvious way is an s 133 offence for a general registrant. **W183 made the bad SHAPE unwritable, W184 the bad SENTENCE unpublishable; NEITHER SEES THIS ONE — EVERY FIELD CAN PASS AND THE COMPOSITION STILL MAKE THE CLAIM.** "Dr Jane Smith" is a clean name, "General practitioner" a clean profession, "Women's health" a linted label; rendered as **"Dr Jane Smith — Women's Health GP"** they are an implied specialty **no per-field linter ever saw, because the offending string does not exist until a template joins three compliant ones**. So the composition is the control, in three parts. **(1) THE SCOPE NEVER TOUCHES THE REGISTRATION** — focus labels live only in the framed block, and a test asserts no rendered line carries both a focus label and a registration word, which is what makes "Women's Health GP" *unrenderable* rather than discouraged. **(2) THE FRAMING SAYS WHOSE CLAIM IT IS** — the clinician told us, Meherr did not check it and cannot (W111's rule in public copy, where a reader has no other way to know which parts were verified); registration is presented as checkable *with the number*, focus as declared. **(3) THE DENIAL IS EXPLICIT, BECAUSE SILENCE IS WHERE THE INFERENCE LIVES** — a list of narrow interests under a doctor's name reads as a specialty unless something says it is not one (W120 at its most expensive), and the denial is carried **even on a profile with no focus areas at all**. Framing text is itself linted against the same union, since a claim smuggled into framing appears on *every* profile; professions are named in full because **"GP" is correct and unsafe — an abbreviation is short enough to sit on a line with an interest**. W184's text linter exported and APPLIED, not reimplemented. **THE FINDING THIS UNIT SURFACED, being the first to turn a profile into copy: W183's SPECIALIST VARIANT CANNOT BE RENDERED AT ALL.** W183 modelled it because s 133 recognises the title and a model denying it would misstate the law; W114 ruled Meherr publishes **no specialist titles whatsoever**, deliberately stricter than the law, because whether a clinician may use one is a fact on a register this tree does not read. **Both are right and they collide here.** Resolved by keeping the shape representable and the copy unpublishable: `renderProfile` refuses by name **as a GATE, not a lint violation** — "your copy breaks a rule" would send a clinician away to reword something and **no rewording would help**. A test proves the refusal is necessary rather than cautious (any honest wording of specialist registration contains the protected word). **Deleting the variant instead would have the model claim no such registration exists — a different and worse untruth.** Non-vacuity three breaks: joining a focus label to the heading fires the juxtaposition + heading tests; dropping the denial fires three framing tests; disabling the specialist refusal fires the gate test. Verify: pnpm verify green, 2095 tests / 162 files, build, audit:gate PASS. |
| W188 | done | builder-B | 2026-08-11T05:05Z | 771665f | src/directory/membership.ts + 18 tests. A directory is a list of practices and the only interesting question is how one gets on it. **There is a cheap answer available and it is wrong**: the tree already knows which practices refer to each other (W93), which answer (W134), which return reports (W142) — **deriving membership from that is an afternoon's work and it is W57's defect arriving where the consequence is public.** **MEMBERSHIP IS A RECORDED DECISION AND NO PATH INFERS ONE.** `MembershipBasis` has two members and both are ACTS: joined, left. No `inferred_from_referrals`, no `active`, no `implied` — **the absence is the feature**, so no caller can obtain a derived membership and no future edit can widen the basis without deleting a stated refusal. `REFUSED_BASES` holds the four temptations (referral activity, acceptance rate, tenure, proximity) as DATA with a reason each, so the test reads the same list a human does. **WHY IT MATTERS MORE THAN AT W57**: a derived register membership gives a practice a wrong internal list; **a derived DIRECTORY membership publishes their name in a network they never joined**, which under G6 is advertising with somebody else's business attached. *A practice that refers a lot has not consented to anything; it has been busy.* Structural rather than aspirational: **the module imports nothing at all**, so it could not read the rail if asked. Leaving keeps history (W57's second rule) — a departure is a new event, `isMember` answers AS AT A DATE, and **a rejoining starts a new tenure** rather than restoring the old one, because the original date would overstate it. **NOTHING RANKS**: ordered by practice id which carries no meaning, no count, no tenure score, no position on a membership. W83 refused ranking clinicians internally, W184 refused comparative claims in copy, and **a membership list is the third place the temptation appears — the one where an order would read as an endorsement by the network itself.** Same-day join+leave resolves to NOT a member: day-granular dates cannot order two decisions within a day (W142), so it is a decision about **which way to be wrong**, and the safe reading is the one that does not publish a practice's name. Declared in W167's register with that tie-break test; mutation-checked by resolving it positionally. **TWO OF MY OWN CONTROLS CAUGHT THIS UNIT AS IT LANDED, which is the intended behaviour rather than friction**: W167's detector flagged the new fold site before I had declared it, then **W178's cross-check refused the declaration until the module was classified** — correctly a PRE-EMPTIVE catch, since the tie was handled at design time and nothing shipped wrong. One collision of mine, same class as W184's an hour earlier: I scanned the source for "referral" to prove independence and **it failed on this module's own refusal explaining why the rail is not a basis**; zero imports was already the whole guarantee. Verify: pnpm verify green, 2113 tests / 163 files, build, audit:gate PASS |
| W189 | done | builder-A | 2026-08-12T06:20Z | bce4bee | src/directory/search.ts + 20 tests. **THE G7 LINE RUNS THROUGH THIS MODULE AND NOT WHERE IT IS TEMPTING TO PUT IT.** The boundary is not the ANSWER — software does not cross into regulated CDSS at the moment it names a doctor. **It is the QUESTION, and specifically whose body the question is about.** "Tell us what is wrong and we will find you the right doctor" takes a description of a patient's symptoms, reasons over it and returns a clinical recommendation; "these clinicians have told us they see a lot of women's health, here they all are" takes an attribute **OF A CLINICIAN, declared by that clinician about themselves**. Nobody described a patient; nothing was inferred about anybody. **SO THE SEARCHER NEVER DESCRIBES THEMSELVES.** Every field is an attribute of a clinician or a place; **there is no free-text box, because free text is where somebody types "pain when I"**, and a product receiving that sentence is reasoning about a patient whether it meant to or not — the type has no field it could arrive in (W183's posture applied to input), with `REFUSED_SEARCH_FIELDS` stating why each is refused. **`focusLabel` is the field that LOOKS like the G7 problem and is not, and that distinction is the unit**: the searcher picks a label a CLINICIAN published, from a list generated out of the profiles themselves, matched **exactly** — because **a near-miss silently becoming a hit is the product deciding what somebody meant, which is the same act as interpreting a symptom**. **THE RESULT IS NOT A SELECTION**: no score, no relevance, no best match, no pagination, **no limit parameter to pass** — "top 5" is a selection made by software, and **a product showing five of nine has chosen four clinicians' worth of invisibility on a basis it never stated** (W151: order, never withhold). **THE CLAUSE WITH TEETH — THE STATED BASIS DESCRIBES THE ORDER ACTUALLY USED.** A page saying "sorted by distance" over a list sorted by signup date is **worse than saying nothing, because a reader told the basis stops looking for one**. `ORDERING` is ONE declared list: the comparator is built from it and the reader-facing sentence is generated from it, so **there is no second place to edit and the description cannot drift**. Tested by sorting real profiles and checking the sentence PREDICTS the result, rather than matching the sentence against itself. **ORDERING IS NOT RANKING, which is why the keys are what they are**: every list has an order and every reader reads the top first, so **a neutral order is not available and pretending otherwise would be the dishonest option**. What is available is an order carrying no judgement about care — taking new patients first (a fact about availability, which is the question actually being asked), then suburb, then name. W83 refused a productivity board internally; **the same claim about a named person in public is worse**. Ties break down to the name, so two profiles compare equal only where order cannot carry meaning. Non-vacuity **four breaks, each firing its own test and nothing else**: sorting by name first while leaving the sentence alone fires the basis test (the exact drift the rule exists against); adding a `symptom` field fires the G7 scan; fuzzy-matching fires the exactness test; truncating to two fires the no-selection tests. Verify: pnpm verify green, 2133 tests / 164 files, build, audit:gate PASS. |
| W190 | done | builder-B | 2026-08-11T06:05Z | 584a5a7 | src/directory/correction.ts + 17 tests. A directory publishes claims about a named person, so that person needs a way to fix it — and **the obvious shape, "let them edit their profile", is the one thing this must not be.** W113's sentence is inherited wholesale: every action a clinician has can only REDUCE what the system claims about them. W113 held that line because credentialing starts with the practice, which holds the evidence; **here the reason is sharper — an edit control on a public profile is a SELF-PUBLICATION CHANNEL, and W183 and W184 spent two units making sure a claim cannot reach the directory without passing a type and a linter. A free-text "fix your profile" box hands it straight back.** `CorrectionEffect` has `removes` and `corrects` and **no `adds`** — no argument produces one, so a future control must delete a stated refusal rather than pass a different flag; `REFUSED_CORRECTIONS` records five refused additions with a reason each, including the ones W79 and W183 already refused from the other side. **THE INTERESTING ONE IS `acceptingNewPatients`, AND IT IS WHY THE RULE MUST BE CHECKED PER DIRECTION RATHER THAN PER CONTROL**: the same boolean is a reduction one way and an addition the other — turning it OFF withdraws an offer the profile was making; **turning it ON asserts current capacity nobody has checked and that goes stale by itself**. The correction exists in one direction only and the other sits in the refused list. **The property is MEASURED rather than argued**: `claimCount` counts what a profile asserts and applying any correction must leave it the same or lower, so **a control that added a claim would raise it whatever its name said about itself**; each removal is separately asserted to remove the thing it names, because a no-op would also satisfy "no more than before". `correct_display_name` is the only surviving free text — getting somebody's name wrong in public is the failure a correction right exists FOR — and it goes through W184's linter **called, not re-implemented**, so the s 133 title claim cannot enter through the correction door, **which is the door it would most obviously be tried on since it is the only one the clinician holds**. W184's surname exclusion survives the path too: a clinician named Best can be corrected as well as published. Mutation-checked by bypassing the lint. **Third naming collision of this shape today, recorded as a pattern**: my additive-verb heuristic matched `claimCount`, which MEASURES claims rather than making one; W184's copy scan tripped on the page's own refusal and W188's import scan on its own refused-basis text. **A scan whose subject matter is the thing it bans will match the sentence doing the banning** — the pinned export list is the guarantee, the heuristic is only the net. Verify: pnpm verify green, 2150 tests / 165 files, build, audit:gate PASS |
| W191 | done | builder-A | 2026-08-12T07:30Z | 238c445 | src/verticals/dermatology.ts + 21 tests. The first vertical declared as a **concrete bundle** rather than a fixture — and **the whole unit is a refusal**: nothing is signed off, nothing can be, and the deliverable is that the refusal is **COMPLETE, NAMED and STABLE rather than a bare false**. **WHY DECLARE A VERTICAL NOBODY CAN USE**: the alternative is discovering the shape of the blocker on the day somebody wants to ship it. W158 exists on the argument that "4 members not usable" is not a plan while "3 wait on one ruling and the fourth is a typo" is; **the same argument applies a level up**. A spec that refuses today prices the dermatology decision — which members, which gates, whose signature — **from code that keeps being true rather than a document that was true in August**. **THE G5 LINE IS WHAT MAKES IT SAFE TO WRITE**: a member ref is a POINTER, which is exactly why W157 chose refs over embedded members — **a spec can never become a second copy of gated content**. Declaring a dermatology vertical WOULD contain two pathways is governance; writing what they say is W186, blocked. So each member carries **the gate it waits on and nothing else**: the obvious field to add is a one-line description of what each member does, and **for a clinical pathway that line IS the gated content**, so the field does not exist rather than existing and being carefully worded (pinned: exactly three keys). **THE LINTER CHECK EARNED ITS PLACE IMMEDIATELY.** My first draft described W119's chain as "a specialist, then the founder" — which trips `no-specialist`, **W114's blanket refusal applying everywhere and not only in patient-facing copy**. The fix made the sentence **MORE accurate, not less**: the vocabulary W119 actually enforces is *reviewer* and *signatory*; "specialist" was my word, not the code's. The note stays in the source because **the next person adding a member will reach for the same word**. Evidence assembles from the tree's own `SHIPPED_*` registries rather than empty literals, so it **cannot claim nothing is signed off while something is** — a hand-written empty object is a second claim about the same fact, **and the second claim is the one that goes stale**. The refusal names every member with its kind, gives **one reason for all of them rather than guessing between "unsigned" and "does not exist"** (W157 refuses that distinction deliberately, and a spec whose members do not exist at all is where inventing the flattering answer is most tempting), and reports no contradictions because **members must exist before they can disagree**. **The half-signed state is pinned**: sign one member off and it still refuses, naming only what remains — a vertical flipping to usable when the first member cleared would be the worst bug available here. Outstanding work applies W158's report (not a re-derived list) and reports `indeterminate` without a pool of what exists, which is also the true answer. Non-vacuity three breaks: naming only the first missing member fires three tests; clinical language in a `waitsOn` fires the linter check and the condition scan; a `description` field fires the shape test. Verify: pnpm verify green, 2171 tests / 166 files, build, audit:gate PASS. |
| W192 | done | builder-B | 2026-08-11T07:00Z | 6b363f8 | src/compliance/public-surfaces.ts + 21 unit + 4 e2e over all 8 public surfaces. Axe was already green (Q15 shipped no public routes — G6 keeps the directory unpublished), so the work was the copy sweep — and **taken literally the gate could not pass on the day it was written**: `/clinicians` carries Rotterdam criteria, COCP suitability and metformin titration. **The Y2 dossier noticed it, Q13's re-checked and noticed it again, and both filed it; filing it a third time would be the control this tree has watched fail.** **SO THE SWEEP CLASSIFIES BY AUDIENCE AND THE CLASSIFICATION IS THE UNIT.** "No clinical claim" is not one rule because it is not one hazard — a patient page saying "we treat diabetes" is what the advertising rules are about; **a walkthrough addressed to GPs is professional communication, and applying the patient rules to it means the product cannot describe what it does to the people who would use it.** Every public route classified with an argued reason, checked against the tree both directions, so a new page fails until somebody decides who it is for. **THREE FINDINGS ON ITS FIRST RUNS, none expected.** **(1) W6 SHOULD NOT RUN OVER A PROFESSIONAL SURFACE AT ALL** — I exempted its rules one at a time and reached SIX OF EIGHT (`no-clinical-necessity` fires on "required"), and **a six-entry exemption list is not an exemption, it is the wrong instrument**: W6 lints a message SENT TO A PATIENT, so every rule asks the wrong question of a GP page. Absent by construction now. **(2) A PRIVACY NOTICE IS A THIRD CLASS** — `/privacy` names "diagnoses" and "test results", which **APP 1 REQUIRES a privacy policy to do**; a notice that cannot name the kinds of information held cannot comply, so `patient_notice` is a classification (still carrying every marketing rule). **(3) THE LANDING PAGE SAYS "my own diagnosis"** — the founder's first-person account of her own diagnosis in the paragraph about why she started the company. **Autobiography, not an offer, and the linter cannot see the difference because the difference is WHOSE diagnosis it is.** Rewriting a founder's account of her own illness to satisfy a regex is the wrong move by a wide margin; widening the rule would drop the word everywhere it matters. **Accepted in W53's audit-allowlist shape** — exact path, rule and matched text, an argument and a review date (2027-02-11) — as is `no-ratings` matching "review" on the ADM page, where the notice is REQUIRED to say how a decision can be reviewed. **The distinction that fell out: an allowlist entry is a SENTENCE, an audience is a PAGE CLASS.** Both directions checked — an acceptance for a finding the page no longer produces is stale, and a stale acceptance reads as coverage. Swept over **RENDERED text, not source**, for a concrete reason: a source scan of `/clinicians` reports `no-ratings` on "review", which is the identifier `is-reviewed` in a className. **And the bound is stated rather than implied** — the linters know "diagnosis" and "diabetes" but not "metformin", "COCP" or "Rotterdam", so **a green sweep means the vocabulary was clean, not that the concept was**. Widening it is its own unit. Verify: pnpm verify green, 2192 tests / 167 files, build, audit:gate PASS |
| W193 | done | builder-A | 2026-08-12T08:40Z | e8bc17e | src/directory/disclosure.ts + 19 tests. **EVERY PIECE OF PRIVACY WORK IN THIS TREE SO FAR HAS A PATIENT AS ITS SUBJECT** — W33's access request, W51's missed store, W106's registry. **A directory profile is the first thing that discloses a NAMED CLINICIAN, to everybody, permanently, and none of that machinery is pointed at them.** Two properties make it unlike every export already built. **(1) IT CANNOT BE RECALLED** — W177 said this of a downloaded file and it is more true of a page that is indexed, cached and quoted: "remove it from the directory" removes it from the directory. **Erasure is not available as a remedy, so the only control that works is applied before publication** — hence a boundary, not a report. **(2) THE CLINICIAN'S OTHER DATA IS EVERYWHERE**: 33 modules hold a `clinicianId` — credentials and their evidence, the capability graph, referral acts, CPD reading, session config, complaints. **The distance between "profile" and "disaster" is one careless join, and a join is a thing somebody writes on a Tuesday.** **SO THE CONTROL IS THE TYPE.** A profile holds five scalars, a location, a language list and `ScopeStatement`s; there is no field typed as a credential, an experience count, a complaint or a referral — **publishing one is not something a caller can do carelessly, it is something they would have to build a field for**. Six `@ts-expect-error` cases name real clinician-keyed types and show none fits, **including a `notes` field, because free text is how everything else refused would arrive**. **That is the only guarantee worth having here**: the alternative, a runtime scrubber on the way out, must be right about every field that exists today *and every field added later* — **exactly the shape of the failure W51 found**. **WHAT DOES LEAVE IS ENUMERATED WITH ITS BASIS.** `DISCLOSED_FIELDS` is tree-checked against W183's `PROFILE_FIELDS` **both directions**, and each entry says whose claim it is — checkable on a public register, or declared by the clinician and checked by nobody. **A reader of a page cannot tell those apart**; holding the distinction as data stops the page and the privacy record drifting into disagreeing about what this product has actually verified. The registration number is classed *checkable* deliberately: **it is what makes every other claim confirmable BY THE READER rather than on our word — the whole alternative this product offers to testimonials.** Location is a suburb not a street, because **precision beyond what the question needs is disclosure without purpose**. `CLINICIAN_RECORD_CLASSES` is **W106's device pointed at a new subject**: every entry `reachesPublicSurface: false`, **which is the point — a list somebody has to consciously edit to make a mistake**, not a classification exercise with interesting cases. Each carries a reason because **"no" without one is indistinguishable from nobody having looked**, and module paths are checked against the tree so a rename cannot leave **a stale entry reading as coverage**. Non-vacuity three breaks: an index signature on the profile makes **all six `@ts-expect-error` directives unused** and fails tsc; dropping a field from the registry fires the enumeration test; pointing a record class at a missing module fires the tree check. Verify: pnpm verify green, 2211 tests / 168 files, build, audit:gate PASS. |
| W194 | done | builder-B | 2026-08-11T08:00Z | f89d4d1 | docs/HARDENING-Q15.md + 2 fixes + 11 tests, run with the review skills (law 5). **Zero criticals. Two findings, BOTH MINE, and both of a shape a per-unit test cannot reach.** **Q15-1 (MEDIUM, fixed) — A CLINICIAN COULD BE PUBLISHED BUT NOT RENDERED.** W184's `lintProfile` lints a profile's FIELDS and excludes three surnames that collide with the marketing rules, so **Dr Sarah Best passes**. W187's `renderProfile` lints the COMPOSED lines — the sentences a template makes from several fields, which no per-field check sees — and reached the heading through the unfiltered primitive. **The heading IS the name**, so Dr Sarah Best was refused with `no-superlatives` on her own surname. **NEITHER HALF WAS WRONG**: W184 was right that a surname is not a superlative, W187 was right that composed lines need linting and that the primitive is the thing to call. **They were SEPARATELY RIGHT about one publication path, which is exactly why no test in either unit could catch it — each passes its own.** The direction matters: this leaks nothing and publishes nothing false, **it stops a real person being listed, and the workaround a practice reaches for is to misspell her name beside her registration number**. Fixed by making the exclusion a FUNCTION (`lintName`) rather than a filter inside one caller — one definition of "the rules that apply to a name" — and W190's correction path inherits it, closing a third disagreement (a clinician could otherwise have corrected their name into a profile that would not render). **The control is the AGREEMENT, not either half**: the same profiles through both paths, same answer required, driven from `NAME_WORD_EXCLUSIONS` so adding a surname without teaching the render path fails. Mutation-checked — reverting the one-line fix fails 4 of 10 cases. **Q15-2 (LOW, fixed) — A DEAD ENTRY IN MY OWN EXCLUSION LIST, found by mutation-checking Q15-1, which is the only way it could have been found**: reverting the fix failed `best` and `leading` and **did not fail `top`**, because no rule matches the bare word (`no-superlatives` carries "top-rated"). The entry excluded nothing. **A dead exemption is the stale-register-entry class this tree keeps catching** — it makes a list look like it is doing more than it is, and the next reader trusts its size rather than its contents. Removed, and the register is now checked BOTH DIRECTIONS: every entry must be a word some rule would otherwise have caught. **METHOD NOTE, THE FINDING BEHIND THE FINDINGS.** Q10: fold a collection, test both orders. Q11: apply that to every dimension that can tie, not the one you were thinking about. **Q15 adds the next turn — WHEN TWO MODULES CHECK THE SAME THING FOR DIFFERENT REASONS, TEST THAT THEY AGREE.** A per-module test cannot find a disagreement because each module passes its own; **the defect is in the PAIR, and nothing owns the pair unless somebody writes a test that does.** Three instances now: W154 had a gate and never called it, W192 had a rule never applied to a surface, Q15-1 had two rules applied to one path disagreeing — **the same question at three distances: is the check that exists the check that runs, and is it the only one?** Confirmed clean: membership's zero-import independence, W189's search ordering, W193's disclosure enumeration, the specialist render refusal, G6 empty and pinned in both registries, W192's acceptances narrow and checked both ways. Verify: pnpm verify green, 2220 tests / 169 files, build, audit:gate PASS |
| W195 | done | builder-A | 2026-08-12T09:50Z | e721cb8 | docs/GATE-DOSSIER-Q15.md + src/directory/dossier-claims.test.ts (12 tests) + a ledger correction. **Q15 COMPLETE.** The unit's own gate said to name "which Y3 units (W117, W133) unblock with it" — **checking that WAS the work, because neither is true as written**. **W117 is not blocked: it is done (d79a99b) and it IS the Q9 dossier.** **W133 is blocked but NOT ON G6** — its row said `FOUNDER GATE G6` while its own why-blocked text describes Q9's **action 1** (may credential detail cross a practice boundary: A, presence only, or B, detail inside a consented network). The Q9 dossier is explicit: action 1 *"Blocks: W131, W133, W137"*, action 5 *"Blocks: G6 itself"*. **W133 routes IN-NETWORK ONLY and renders nothing public, so an Ahpra advertising review of profile copy cannot answer it.** Corrected in-unit rather than asked of the founder — a bookkeeping fix to a label a build unit set at Q11, not a decision; **the unit stays blocked either way, but the tree's most time-critical open decision is no longer hidden behind a gate nobody is scheduling.** **So G6 unblocks exactly ONE unit: W185.** **WHAT G6 DOES NOT OPEN, which is most of what the label suggests**: **not specialist titles** (W114's blanket refusal is a separate ruling — Q9 action 4 — and W187 made it concrete by refusing to render a specialist profile at all; **that survives G6 untouched**); **not a directory** (no page imports `src/directory` — **seven modules, 3,215 lines, 133 tests, ZERO consumers**; W185 is where pages get built and G6 is its precondition, not its content); **not membership** (`SHIPPED_MEMBERSHIPS` empty — a reviewed directory with no members publishes nothing). **THE FINDING I DID NOT EXPECT: W168'S OWN MATCHER WAS ENFORCING THE MISLABEL.** It required `/FOUNDER GATE G\d/` on every blocked row — but **not every founder blocker is a numbered gate**, and W133's is not, so **the only way to satisfy the check was to name a G-number, true or not**. Widened to accept a `FOUNDER DECISION` that **points at the dossier where the question is written**, and deliberately NOT widened to "mentions a founder": an un-numbered decision must be findable. **The loosened matcher carries its own non-vacuity test in the suite**, asserting it still rejects a row naming nothing and a decision with no document. Dossier claims are **pinned by tests rather than left as prose that goes stale silently**: both registries empty, no page importing `@/directory`, the specialist refusal in force, and the Q9 action-1/action-5 split still present in the document the central finding rests on. Carries W194's finding forward because **an Ahpra reviewer reads rendered pages, not modules**. Five asks, four carried unchanged from Q9 with original numbering; **the Ahpra review ask has now been open for more than a year across three dossiers**, so this one names the cost of not answering rather than asking a fourth time in the same words. Non-vacuity three breaks: a page importing a directory module fires the zero-consumers pin; softening the W133 finding fires the gate test; reverting the ledger row fires the ledger-agreement test. Verify: pnpm verify green, 2233 tests / 170 files, build, audit:gate PASS. |
| W196 | done | builder-B | 2026-08-11T09:00Z | 841b4db | src/reporting/model.ts + 17 tests. **Q16 is the first time anything about a PRACTICE leaves this tree to an outside body — one that also commissions services from that practice.** Built behind proposed gate G9 the way W183's profile model was built behind G6. **THE AGGREGATION FLOOR IS DECLARED DATA, NOT AN ARGUMENT, AND THAT IS THE UNIT.** A floor passed per report is a floor somebody TUNES: compute the cell, see it is 3, pick 3. **The tuning need not even be dishonest — "this one is fine, it's a big practice" is how it happens — and it is undetectable afterwards, because the report carries the number and not the decision.** So `figure()` takes no floor parameter and no options object that could grow one; it reads a register fixed **before anybody has seen the data** and stamps the floor onto the figure so the report carries it. Mutation-checked by adding the parameter back. The floors are **the loop's proposal, not a ruling** — G9 is unratified and the numbers are the founder's; what this unit fixes is that they are SET where a reviewer can read them. They differ by kind because re-identification risk does, and **the two referral counts share a floor deliberately: reported side by side, a lower floor on one would let a reader subtract and recover the cell the other was protecting.** **EVERY FIGURE NAMES THE RECORDED FACTS IT COUNTS** — a number handed to a commissioner with no basis is read as a measurement of care. A `Figure` cannot be built without a basis, and **a basis over zero records does not produce 0, it refuses**: zero is the most dangerous number here, because "0 referrals completed" and "we have no record of any referral completing" are the same digit and opposite claims, and only the wrong one is flattering on a young rail (W170's rule, sharper because **the reader is not the practice**). **NO FIGURE IDENTIFIES A PATIENT, and not by filtering** — a Figure is a kind, a count and a basis with nowhere to put one, asserted on the real value's keys so a field added to the type fails. `REFUSED_FIGURES` records five considered and refused with a reason each: anything per-clinician (**W83's floor one level up, with money attached**), a completion rate (W170's error with a commissioner reading it), demographics, sub-floor condition counts, and comparison between practices. **It DECLARES the floors and does not apply them — suppression is W197, and the split is deliberate: the number that decides whether a cohort is publishable should not be authored in the same place as the logic that lets it through.** A sub-floor figure is still built here carrying its floor, and no export in this module is named for suppression. Verify: pnpm verify green, 2250 tests / 171 files, build, audit:gate PASS |
| W197 | done | builder-A | 2026-08-12T11:00Z | d8eb23f | src/reporting/suppression.ts + 23 tests. **SUPPRESSED, NOT ROUNDED — and every rounding scheme is wrong differently**: rounding 3 up to 5 publishes a false figure a commissioner may act on; rounding down to 0 publishes a different false figure that reads as **"this practice does nothing"**, the most damaging misreading available; a band `<5` is honest and **is still a number in a column, and readers add columns**. So the test hunts for the value in the RENDERED document rather than checking the happy path — **a band, a round-up, a round-down and a zero substitution all put something there; only true suppression leaves nothing to find**. **STATED, NOT LEFT AS A GAP** (W145): a blank cell and a suppressed cell look identical and mean opposite things, and **a reader assumes the one that needs no explanation** — that nothing was measured. Every withheld figure is named with its reason, and **the report-level sentence is present even when NOTHING was suppressed**, because its absence is itself information: a reader who sees it only sometimes learns to read its presence as *"something was hidden here"*. **THE FINDING: TWO FIGURES THAT EACH CLEAR THE FLOOR CAN DISCLOSE A CELL THAT DOES NOT.** `referrals_with_recorded_completion` is a SUBSET of `referrals_written`; publish 40 and 38 and both clear a floor of 5 comfortably — **and the reader now knows exactly two referrals have no recorded completion**. No per-figure check can see it because neither figure is small: **the small cell is the difference, and it never appears in the report at all.** W196 anticipated the shape in its own floor rationale (*"or the pair can be differenced to recover a suppressed cell"*); this is that argument reaching its conclusion — **a shared floor is necessary and not sufficient**. Nesting is declared as data, and the SUBSET is withheld rather than the whole, because publishing the whole tells a reader only that the part lies between 0 and it, while the reverse bounds the whole from below. **MY FIRST DRAFT'S SECOND RULE WAS UNREACHABLE, and the note stays in the module.** It suppressed any figure whose BASIS fell below the floor ("0, computed over 2 records" names a group of two). **The reasoning is right and the rule can never fire**: W196 refuses value > basis, so a basis below the floor forces the value below it too and rule 1 has already withheld the whole figure, basis included. The concern was real and **already closed; the rule was decoration**. Recorded because **a rule that cannot fire looks exactly like a rule that works** — I only found it when the test I wrote for it could not be given a fixture that reached it. **Not claimed, stated rather than assumed**: this handles channels visible inside ONE report; a commissioner holding two quarters can still subtract, no per-report rule can see that, and **W203's recipient allowlist is where it would be addressed**. `PublishableFigure` is branded so a report cannot be assembled from raw figures and route around the suppression; no threshold argument and no options object that could grow one. Non-vacuity four breaks, each firing its own tests: banding fires three; dropping the complementary rule fires three; omitting the statements from the document fires the render test; withholding the whole instead of the part fires four. Verify: pnpm verify green, 2273 tests / 172 files, build, audit:gate PASS. |
| W198 | done | builder-B | 2026-08-11T10:00Z | 142983d | src/directory/fees.ts + 20 tests. Placed beside the directory rather than in `src/reporting`: **a fee is read by a patient deciding whether they can afford an appointment, not by a commissioner.** A price is the most comparison-shaped fact a directory can hold — sort them, badge the cheap one, say "great value" — and beside a clinician's name each of those is regulated advertising. **THE SHARP REFUSAL IS NOT ABOUT ADVERTISING AT ALL.** "Standard consultation $95" is something the practice knows; **"you'll be about $53 out of pocket" depends on concession status, safety-net position, which item numbers the consultation attracts and whether they are bulk billed on the day — none of which this product holds.** The error direction is the bad one: **told they will pay less than they do, a patient finds out at the counter, after the appointment.** No out-of-pocket field, no rebate arithmetic, no estimate; the type has nowhere to put one and `REFUSED_FEE_FIELDS` gives a reason for each. **NO COMPARISON AND NO RANKING BY CONSTRUCTION**: `feeLines` returns the practice's declared order, no comparator is exported or accepted, nothing reads `feeCents` for ordering — **a price list sorted by price is a price-comparison site and it would be one `sort` away** (mutation-checked by adding it). A fee **says when it was stated** — worse than W183's `acceptingNewPatients`, because a stale price is not out of date, it is a misrepresentation a patient acted on — and **zero is never a price**: a bulk-billed line renders "No charge", since 0 would read as a price of zero rather than as no charge arising. **FINDING, FILED RATHER THAN FIXED: W23's `no-ratings` matches the bare word "review", and "Care plan review" is one of the most commonly billed consultations in Australian general practice.** That is the **third** false positive from the same word on the third surface — W192 hit it scanning `/clinicians`' SOURCE (the identifier `is-reviewed`) and again on the ADM notice, which is REQUIRED to say how a decision can be reviewed. **Three is a mis-specified rule, not three unlucky pages**: a patient rating is already caught by the star, 5/5 and "rated" branches. Narrowing it edits W23, whose landing tests depend on the current pattern, so it is `RATING_RULE_OVER_BROAD` **in the suite** for the next hardening week and handled here by the narrowest exemption that lets a practice name its services. **METHOD NOTE — the clearest instance of something that has now happened in FIVE CONSECUTIVE UNITS: a scan whose subject matter IS the thing it bans will match the sentence doing the banning.** W184's copy scan tripped on the page's own "no breakdown by clinician"; W188's import scan on its own refused-basis text; W190's additive-verb heuristic on `claimCount`; W192's twice; and here **the caveat that exists to say "this is not an estimate" was refused for containing "you will pay"**. Every one was caught by a TEST rather than by review — the system working — but the recurring fix is worth naming: **assert on STRUCTURE where you can, and where it must be over prose, scope it to the prose that makes the claim rather than the prose that refuses it.** The caveat is reworded rather than exempted, because **a disclaimer that has to phrase itself as the thing it disclaims is one that will be quoted out of context.** Verify: pnpm verify green, 2293 tests / 173 files, build, audit:gate PASS |
| W199 | done | builder-A | 2026-08-12T12:10Z | b38aecf | src/reporting/report.ts + app/console/reporting + 15 unit / 6 e2e + axe. **THE FIRST THING TO SETTLE WAS WHO IT IS FOR, because the obvious answer is the one proposed gate G9 forbids.** G9 asks whether practice-identifiable data may go to a third-party organisation at all and is unratified, so this report has **one reader: the practice it is about, looking at its own data**. `buildPracticeReport` has **no recipient parameter, no delivery, no export-to-anywhere**; the page has no send control **and says so**, because an absence that is merely unadvertised reads as a feature nobody got to, and **an adapter that exists is one configuration change from disclosing** (G1/G3's shape). An e2e walks every button and link asserting none offers to send, share, submit, publish or disclose. **COVERAGE IS NOT A FOOTNOTE**: the period repeated on every figure (**a figure quoted out of the document loses the heading first**); the denominator W196 built in (**"4" is an impression, "4, counted over 12" is a fact**); and what is NOT in it, because **a report listing four figures reads as a description of the practice when it is a description of four things the record happens to hold**. **THE COVERAGE SECTION CARRIES TWO LISTS — the unit's one real design decision.** W196 refuses to emit a figure over zero records rather than producing a 0, which is safe and has a cost: an absent kind is indistinguishable from one this product does not report. **Three silences — withheld, nothing recorded, zero — lead to three different next questions**, so the document names which is which; **a withheld kind counts as REPORTED, not nothing-recorded, because withheld means measured.** **TWO THINGS CAUGHT BY TREE-CHECKS RATHER THAN REVIEW.** (1) **W102's surface census failed the moment the route existed** — a served route with no dossier row and no census line; both added. (2) **My first denominator e2e asserted a figure that does not exist**: the seeded rail carries three referrals, every figure is below the floor of 5, so everything is suppressed and **the denominator rendering — the thing W196 and W199 both exist for — was never exercised**. A test that only ever sees the withheld path is not testing the page, so the mock route gained a `reportable=1` knob. **The retry then asserted the number the knob ADDS rather than the number on the rail** (the seed already carried two); it now derives the count from the fixture's own response, because **a test that recomputes the fixture's arithmetic gets it wrong the day the seed changes**. Non-vacuity three breaks: dropping the denominator fires four tests incl. the golden; collapsing the coverage lists fires three; counting a withheld kind as nothing-recorded fires two. Verify: pnpm verify green, 2308 tests / 174 files, build, audit:gate PASS; reporting e2e 6/6; full axe green with `/console/reporting` in the sweep. |
| W200 | done | builder-B | 2026-08-11T11:00Z | e74f417 | src/compliance/cdss-boundary.ts + docs/CDSS-BOUNDARY-W200.md + 11 tests. The gate's word was RE-DERIVED, and doing it rather than carrying forward is the whole unit. Properties one to four each had a Y4 surface that could plausibly have broken them and each survived on its own construction — W189's search returns clinicians for a patient's need and still never selects one; Q14 audits outcomes over the referral rail and never infers a transfer; W170/W179/W171/W196 STRENGTHENED the silence property; W191's vertical is a spec awaiting G5, not shipped clinical text. **PROPERTY FIVE HELD AND ITS ENFORCEMENT DID NOT.** W150's declared copy surface is `EDUCATION_COPY_MODULES` — six files, all under src/education/ — and every quarter of Y4 added operator-facing copy outside it that NO LINTER EVER REACHED. Re-running the advice rules over all of it finds no advice about any patient, so the property was true; it was true by care rather than by control, and the distance between those is why this tree writes registers. So the declared surface now covers **every Y4 module, with membership read off each module's own `// W<n>` header** rather than a list — the first version was a hand-written list of nine, which is the same shape as the thing this unit found wrong with W150, and it missed nineteen. Each entry declares which exports an operator READS, not which are strings: eleven string exports of Y4 trip the advice rules and eight are the register machinery itself, quoting the words it exists to refuse. Sixth instance of W198's pattern. **THE THREE REAL HITS ARE ACCEPTED PER STRING AND THE RULE IS NOT LOOSENED** — the sharp one is W179's `SILENCE_COPY` saying "No action needed" about an appointment feed, caught by `no-action-framing`. Teaching that rule about negation is the tempting fix and it is WRONG: in education copy "this pathway changed, no action needed" is a clinical judgement about whether to review anybody. Same phrase, opposite meanings, and the difference is the SURFACE — W192's audience finding arriving from the other direction. Accepted by module + export + rule + exact match + argument + reviewBy 2027-02-11 (W53's shape). Mutation-checked three ways; the third matters — `copyTexts` walking one level (its first version) reports `SILENCE_COPY`, a record of OBJECTS, clean over zero texts, so the non-empty guard is what makes the clean result mean anything. Known bound stated, not filed quietly: this reaches EXPORTED copy, and prose composed inline in a renderer (`search.ts`'s "Ordered by …") is not reachable by name. Residual for a hardening week: ~30 pre-Y4 modules export `_COPY` that nothing lints — close it by lowering `Y4_FIRST_UNIT`, not by writing a second register. Verify: pnpm verify green, 2304 tests / 174 files |
| W201 | done | builder-B | 2026-08-11T12:47Z | c7f71b9 | src/privacy/automated-decisions.ts + rewritten app/privacy/automated-decisions/page.tsx + 12 tests. **W102 REVISED THIS PAGE AND LEFT THE FIX IN A COMMENT** — "keep this page in the same commit as any change to what the software decides" — and three years of product went by. An instruction in a comment is a hope, so the list is now a REGISTER THE PAGE MAPS OVER, checked against the source in both directions: a module that decides something about a patient and is not declared fails the suite. **THE HOLDOUT ARM WAS NEVER DISCLOSED AT ALL.** W8 assigns every patient to an arm by hashing (practice, patient) and the holdout arm is not invited — an automated decision that withholds an offer of an appointment from a specific person, live since Year 1 and absent from the published ADM statement since Year 1. **FOUNDER: this is a disclosure that should have existed since W8 and now does; it is first in the list.** Also newly disclosed: escalation to a human (W73, four LIVE triggers — free-text reply, complaint, repeated DNA, opt-out with a message), referral follow-up nudges, referral outcome verdicts, and the pathway engine. **STATUS IS READ FROM THE TREE, NOT ASSERTED** — a decision claiming to be dormant must point at an EMPTY registry and one claiming to be live at a non-empty one, so the pathway engine cannot quietly start deciding about people while the notice still says it does not; mutation-checked by filling `SHIPPED_PATHWAYS`. The detector states its bound: "touches a patient" is the UNION of two scans (names `PatientId`, or exports a `*Reason|*Refusal|*Exclusion|*Verdict` union) because neither is sound alone — the union scan misses `registers/escalation.ts`, the identifier scan misses `engine/eligibility.ts`, and the register exists because both were missed. 70 modules classified, 12 decisions + 45 ruled out with a reason each. **The ADM copy answers to W192's `patient_notice` sweep, not to the advice rules**: `lintEducationCopy` bundles W6's patient-MESSAGE vocabulary and a legal notice must say the words a message may not — running it flags five strings and all five are the notice refusing the thing ("no diagnosis", "not a judgement about whether you need to be seen"). Seventh instance of W198's pattern, second time the answer is the AUDIENCE rather than the string; cross-checked with W200's register per W194's note so the two cannot disagree. One W192 acceptance died because the rewrite removed the phrase it covered — deleted rather than left to rot, as its own e2e instructs. **ALSO FIXED A RED MAIN**: W200's Y4 register and W199's reporting module were each verified green and are red in combination — a tree-wide register turns any parallel merge into a possible red and NO PRE-PUSH VERIFY CAN CATCH IT; both undeclared modules added, plus a duplicate guard after W205 and W201 declared the same module inside one merge window. Verify: pnpm verify green, 2347 tests / 177 files; e2e privacy + public-sweep green |
| W202 | blocked | — | — | — | **FOUNDER GATE G9 (proposed, unratified) — not buildable by any builder.** Practice consent to disclosure: a practice decides what leaves it → verify: fail-closed; no report can be produced for a recipient the practice has not consented to. **Why blocked:** A practice consenting to disclosure presumes a decision about whether such disclosure happens at all, which is exactly what G9 proposes and the loop must not decide. |
| W203 | blocked | — | — | — | **FOUNDER GATE G9 (proposed, unratified) — not buildable by any builder.** PHN/health-system delivery → verify: recipient allowlist; constructor refuses live endpoints until the gate opens (G1/G3 shape). **Why blocked:** Delivery to a PHN or health system is the disclosure itself. Blocked with W202 rather than built behind a flag, because a delivery adapter that exists is one configuration change from disclosing. |
| W204 | done | builder-A | 2026-08-12T13:30Z | 919a95e | src/reporting/retention.ts + W106 registry entry + 12 tests. **THE FINDING IS ALSO THE UNIT: THIS PRODUCT KEEPS NO REPORTS, AND THAT IS NOT A GAP.** W199 renders on demand from live rails — no store, no `producedAt`, no artefact table — so the work is **declaring that answer in W106's registry rather than leaving the class undeclared because it has no rows**. An undeclared class is exactly what W51 found: **"there is nothing to declare" and "nobody declared it" are indistinguishable from outside, and only one survives the next unit adding a table.** **Not keeping it is the RIGHT default, not merely the easy one**: a stored report is a second copy of figures derived from records with their own retention — purge a referral under W33 and last quarter's stored report still contains it, **erasure reaching the source and not the derivative, W51's failure in a new place**. Recomputing makes erasure automatically effective **because there is no figure to erase**. **AND THE DAY G9 OPENS, THAT REVERSES.** W202/W203 would send a report to a PHN, and **there is no accountable disclosure without keeping a copy of what was disclosed** — *"what did we tell them in Q2?"* is the first question after any dispute, and **a product that recomputes cannot answer it because the rails have moved on**. So a disclosure log becomes **MANDATORY rather than optional**, as a stored class with a real life. **This is a genuine tension, not a wrinkle: the retention posture that is safest while nothing is sent is the WRONG posture the instant something is.** `PROPOSED_DISCLOSURE_LOG` states what would exist, proposes 7 years to match the tree's health-records posture, and **names the open question it does not settle** — whether the log holds the FIGURES sent or only the fact of sending (one answers fully and becomes a lasting copy of practice-identifiable data; the other is cheap and half-useful). **None of it is built, because a store that exists is a store something can be written to** — G1/G3's shape applied to a store. **THE STATED LIFE IS CHECKED, NOT MERELY STATED.** "We never persist a report" is exactly the claim that is true when written and **false two units later, failing silently because nothing about a new table announces it contradicts a comment in another file**. Tests scan `src/reporting` for a store, assert the report is built from arguments rather than a fetched history, and walk an enumerated artefact list — **including the copy a practice took away, named as outside this product's reach rather than quietly omitted**. **Builder-B's W200 CDSS-boundary registry failed the moment the module existed** — same class of catch as W102's census on W199, working as intended. Declaring `report.ts` and `retention.ts` there **also closed a gap that predated this unit: report.ts shipped in W199 with no boundary entry**. Non-vacuity three breaks: removing the registry entry fires two tests; adding a `globalThis`-backed store under `src/reporting` fires the store scan; reclassifying as `stored` fires two more incl. the erasure-reach assertion. Verify: pnpm verify green, 2331 tests / 176 files, build, audit:gate PASS. |
| W205 | done | builder-A | 2026-08-12T14:40Z | e574177 | Q16 hardening, run with the review skills (law 5). **Zero criticals. Nine findings reported, six confirmed by execution and fixed — and the top three were against units I built two firings ago.** **CRITICAL — THE REPORTING PAGE PUBLISHED A TRUE NUMBER UNDER A FALSE PERIOD.** `sentBy().length` is every referral a practice has EVER written, stamped with a hardcoded quarter; the rail dates every referral 2026-02-01, **outside** it — so the page rendered *"Referrals written: 8 … 2026-04-01 to 2026-06-30"* where the true count in the stated window was **zero**. **Exactly the failure W196 and W199 were both written to prevent, arriving through the one input neither validates: the period is not a figure, so nothing checked it against the figures.** Worse, **my own e2e locked it in** by asserting the quarter as a literal. Figures are now counted within the window they are labelled with, and the spec asserts the fixture falls inside it rather than asserting the string. **HIGH — THE COVERAGE SECTION MADE THE CLAIM IT EXISTS TO PREVENT.** `nothingRecorded` was "every kind the caller did not pass", and nothing in the tree derives register membership or care gaps — so **every live report told a practice "the record held nothing to count" about two figures nobody had ever tried to count**: a claim about their rails made on the strength of a gap in Meherr. `buildPracticeReport` now takes the kinds ATTEMPTED (**required, not defaulted — a default restores the bug**) and coverage has three lists, the third saying *"it is a gap in Meherr, not in your record"*. **HIGH — W197'S OWN SUPPRESSION STATEMENT WAS THE BAND W197 REFUSES.** "The difference between them is fewer than 5 people", beside a published 40, bounds the withheld subset to [36,40] and the hidden group to [0,4] — **the `band` treatment `REFUSED_SUPPRESSION_TREATMENTS` refuses two screens further down. The module contradicted itself and the explanation WAS the leak.** Now unquantified, with the asymmetry argued: for a plain small cell the floor is the reader's only calibration and reveals nothing new; **for a complement it IS the disclosure**. The test that should have caught it **was vacuous** (checked only that the literal "38" was absent); it now asserts no digit at all. **MEDIUM ×4**: `figure()` accepted **NaN** — every comparison against NaN is false, so neither `value > basis` nor `value < floor` caught it and **a NaN figure was published past the floor**; `care_gaps_detected ⊆ register_membership` was an **undeclared nesting** (30 and 28 difference to a group of two); **the fee linter let a rating through** — the service-word exemption FILTERED findings and a linter returns the first match per rule, so *"Medication review clinic, rated 5/5 by patients"* produced one `no-ratings` violation matching "review", which the filter removed, **while the same sentence without "review" was correctly refused** (now masks the words in the TEXT); and the CDSS copy-acceptance expiry compared against a **frozen date**, so no acceptance could ever expire — **a review date recorded and never enforced is a control that looks exactly like a control that works**. Note on the fix to the fix: masking with a fold tripped **W167's fold-site register**, so I removed the fold rather than declaring one — **the register is for folds that have to exist**. Security review over the diff: no new findings; both new regexes are built from module constants. Verify: pnpm verify green, 2334 tests / 176 files, build, audit:gate PASS; reporting e2e 7/7; console-surface axe green. |
| W206 | done | builder-A | 2026-08-12T15:50Z | 0bfd688 | docs/AUDIT-Y4.md + fix + 4 tests. Whole-tree review, W51's method — **and the finding justifies the method: it lives in W43 code from Year 1 that Year 4 never touched, and became a live leak because of something Year 3 did somewhere else.** **Y4-1 HIGH — EVERY PRACTICE COULD READ EVERY OTHER PRACTICE'S COMPLAINTS.** `app/console/complaints` rendered `getComplaints()` — **the entire store, unfiltered** — to whichever practice was signed in, and the console home rendered an unfiltered count as *"N open complaints — review now"*. **Complaint records carry a `patientId`, so this was patient-linked data crossing a tenancy**, and the banner was also a false call to action: a practice with none of its own was told to review somebody else's, and clicking through showed them. **THE FIELD EXISTED THE WHOLE TIME.** `Complaint.practiceId` has been on the type since W43; **the WRITER ignored it** — `intakeComplaint` stamped the literal `"prac-console"`, an id no console has ever minted — so every complaint belonged to nobody and a scoped read would have returned nothing. **The readers were unscoped BECAUSE the writer was.** Before W166 there was one practice and the literal was harmless; **W166 made two practices real and converted a dormant modelling gap into a live leak** — exactly W181's causal shape, found by re-running that sweep across EVERY store rather than only where Q16 had touched. **PRIV-3 HAD RECORDED HALF OF IT SINCE YEAR 2 AND NOBODY JOINED THE HALVES.** It names the literal as a modelling gap; **what was never written down is what the literal would DO once multi-practice landed. A recorded finding is not a closed one, and a finding filed as a modelling gap does not announce itself when it becomes a disclosure.** PRIV-3 updated rather than closed — `src/audit/store.ts` carries the same assumption and this audit did not reach it. Fix: intake takes the practice (**the action had it in hand and was dropping it**); `complaintsFor` is the scoped read, filtered as the query. **Deliberately NOT scoped and tested as such**: `complaintsForPatient` and `scrubPatientFromComplaints` — erasure must reach every practice a patient has touched, and **adding a filter there is the change that would break erasure silently**. Non-vacuity: unscoping fires two tests; restoring the literal fires **four, including a Year-1 guardrail test that had been passing on the wrong data all along**. **Confirmed clean, re-derived from source**: 13 `SHIPPED_*` registries (12 empty+pinned; the 13th legitimately non-empty, operational-only, every `conditionCode` null); no frozen clocks left in source; no surviving vacuous assertions; **all four completeness registries check both directions — proven by having failed correctly three separate times during Q16 when new modules landed undeclared.** Verify: pnpm verify green, 2338 tests / 176 files, build, audit:gate PASS; complaints + two-practice e2e 8/8. |
| W207 | done | builder-A | 2026-08-12T17:00Z | 7aa601b | Y4 gate dossier: G5, G6 and proposed G9 in one place → verify: every outstanding founder decision priced, with the units each unblocks named. **`docs/GATE-DOSSIER-Y4.md` + `src/quality/gate-dossier-y4.test.ts` (12 tests).** Cross-cuts the quarter dossiers instead of repeating them. **Finding: the gate labels track neither urgency nor cost.** G6 has been open since Y2 and releases ONE unit (W185); Q9's action 1 — called time-critical then, and the answer W133 still waits on — has no gate number, carried `FOUNDER GATE G6` for two quarters, and was invisible to every sweep matching `FOUNDER GATE G\d`, W168's own ledger check included. **Four of the thirteen blocked units (W146, W147, W202, W203) wait on gates nobody has ratified OR rejected**, which is the absence of a decision rather than a decision. Asks ordered by arithmetic: Q9 ruling first, ratify-or-reject G8/G9 second, clinical reviewers for G5 third (largest at seven, longest lead), Ahpra fourth. The test re-derives every count from this ledger and caught two errors before the document shipped — the total is 13 not 11 (my grep required the `**FOUNDER GATE` bold prefix; SUP-1/SUP-2 name their gate mid-sentence), and the blocker matcher read W195's *"corrected this attribution FROM G6"* as an attribution TO G6, reproducing the mislabel inside the tool built to detect it. Non-vacuity: re-attributing W133 to G6 fires 2; dropping SUP-2 from the G5 table row fires 2; listing W186 under G6 as well fires 3. **The first pin only checked each blocked id appeared SOMEWHERE in the document and the SUP-2 break left it green** — mentioned is not counted, so the pin is row-by-row on the table and every blocked row must appear in exactly one row of it. Gate: 178 files, 2363 tests, build, audit:gate PASS. |
| W208 | done | builder-B | 2026-08-11T17:47Z | 3e38b3b | **YEAR 4 COMPLETE.** §5e + 52 rows: Q17 response graph + deterministic matching W209–W221, Q18 capacity forecasting W222–W234, Q19 FHIR/e-referral + payer interop W235–W247, Q20 expansion verticals / platform APIs / five-year review W248–W260. Ledger runs W1–W260. **DERIVED FROM `docs/AUDIT-Y4.md` AND `docs/GATE-DOSSIER-Y4.md`, NOT FROM THE YEAR-1 THEME LINE**, and the first residue is the one that changes the quarter: (1) **THE HEADLINE OF Y5 COLLIDES WITH A PUBLISHED LEGAL NOTICE.** §6 wrote Q17 in Year 1 as "matching optimisation … learned ranking second". W201 has since published an ADM-transparency statement whose *never automated* list says **"No ordering of patients by need or by how unwell they are"** — a learned ranker over patients IS that sentence, and it sits on G7's line besides. So Q17 ships the response graph and deterministic matching, W216 prices the question, and **W217 is `blocked` from day one on a founder decision that would require CHANGING A PUBLISHED NOTICE, not a config**; (2) the response graph has no responses — nothing has ever been sent (G1/G2/G3, W174 still blocked) — so the structure ships against W12's sim and every figure states its basis, Q14's posture reused; (3) **PRIV-3's sweep is unfinished and its severity changed under it** — Y4-1 was a HIGH cross-tenant leak created when W166 made two practices real, and `src/audit/store.ts` was never reached, so W209 finishes it in week one; (4) **A RECORDED FINDING IS NOT A CLOSED ONE** — PRIV-3 recorded half the problem for two years and nothing announced itself when the modelling gap became a disclosure, so W210 makes a latent finding carry the condition that would make it live and fails the suite when it does; (5) six founder decisions outstanding and four on gates nobody has ruled on, so every unit needing a ruling is blocked in the expansion rather than half-built; (6) the plan runs out at W260, so W260 renews the expansion rule that wrote it. **W156 CHECKED THE LAST EXPANSION BY HAND** and wrote "plan and ledger checked unit-for-unit" in its commit; W168 exists because the previous hand-checked ledger property broke silently the moment nobody was looking. Same class, so this ships `src/quality/plan-ledger.test.ts`: same units both directions, a verify gate on every planned unit, whole quarters of thirteen, and **every gate a blocked row names must be DEFINED in §4** — "founder gates inherited, never expanded away" as a check rather than a promise, mutation-checked three ways. **G10 PROPOSED for founder ratification** (payer/insurer data flows — G9 governs a body that commissions services, a payer has a financial interest in the individual patient's care; W240/W241 buildable behind it), on the W104/W156 precedent that the expansion unit proposes the gate for the year it expands. Also **scoped W207's dossier arithmetic to Year 4**: a point-in-time document pinned against the LIVE ledger is pinned against a moving target, and it went red the moment Y5 was planned — every future year-close dossier inherited that bug. Verify: pnpm verify green, 2370 tests / 179 files |
| W209 | done | builder-A | 2026-08-12T18:00Z | dd28e74 | PRIV-3 completion: finish the practice-scoping sweep Y4-1 started, `src/audit/store.ts` and every remaining unscoped store read → verify: every exported store read either takes a practice or is declared cross-practice with the erasure reason (W106's shape), checked both directions; Y4-1's non-vacuity method reused — unscope the read and watch named tests fail. **TWO more instances of the class, and a registry so the next one is a build failure rather than a discovery.** **(1) The usefulness audit** — `AuditState` carried ONE `practiceId`, the literal `"prac-console"`, so every seeded visit belonged to nobody and the page read the whole store. **Worse than Y4-1's read: a WRITE across the boundary.** A clinician at B could record "this appointment was worthwhile" against A's patient visit and A's tally would carry it; `submitUsefulness` authorized against its own practice and then wrote to a queue that had no practice at all, so the grant was checked at a boundary the write did not cross. The assumption was IN THE TYPE — `AuditState` no longer has a practiceId, each visit carries its own, a foreign visit is refused as `unknown_visit` (the same answer as one that does not exist, because a distinct error would confirm the appointment is real and held elsewhere). **(2) The CPD trail, found by the sweep and not by reading** — W166 mints `clin-${n}` from a per-practice counter and said "a clinician id is only ever resolved inside the practice that minted it"; `src/education/store.ts` resolved one WITHOUT a practice, so `clin-1` named a different person at every practice and the education console rendered somebody else's reading record as the reader's own. `CpdEntry` now carries `practiceId` and the reads take the pair. **The module header claimed the right thing in prose while the code did the wrong thing — which is why the registry reads signatures, not comments.** **(3) `src/tenancy/store-reads.ts`: 79 entries over 12 store modules,** each classified, everything not `practice_scoped` stating in writing why, checked both directions against source. A declaration cannot lie: `practice_scoped` must really take a practice and `patient_keyed` must really NOT (erasure has to reach every practice, so that check runs the other way). Every `store_handle` names its scoped alternatives and they must exist and be practice-scoped — the raw handle is the loophole Y4-1 went through. `sessionAppointmentType` is recorded as a **NAMED RESIDUAL** rather than laundered: it matches a clinician id with no practice and is safe only because the seeded rail holds one practice's sessions, a property of the fixture rather than of the code. W200's CDSS registry failed correctly on the new module (fourth time). Non-vacuity, six breaks each firing its own named tests: unscope the audit read (1), unscope the CPD read (1), declare a patient-keyed erasure as practice-scoped (1), unscope the audit WRITE (2), add an undeclared store export (2), rename a declared store function (4). Gate: 180 files, 2392 tests, build, audit:gate PASS; e2e usefulness/education/two-practice/console/privacy pass. |
| W210 | done | builder-B | 2026-08-11T18:47Z | 33f8056 | src/quality/latent-findings.ts + 10 tests. **THE OBVIOUS READING OF PRIV-3 IS FALSE, AND THE UNIT TURNS ON THAT.** The trigger WAS written down — W103 recorded it in bold in Year 2: *"the first store change that lets `ConsoleState` hold more than one practice must thread practice identity from the session in the same unit."* W166 made two practices real, the trigger fired, and nothing happened for forty-three units and FOUR live cross-tenant defects (W181's ops queue, Y4-1's complaints leak, W209's usefulness-audit write and CPD trail). The row was re-read three times in that window — W154 corrected its status, W206 updated it, W209 closed it — by people who understood the sentence perfectly well. **So the missing thing was never the record; it was that a sentence in a markdown cell cannot be EVALUATED.** Every other control in this tree derives a property from the artefact (W102, W106, W150, W167, W168, W200); a latent finding was the last place the tree kept a claim in words and hoped somebody re-read it at the right moment. A finding now carries both forms — the sentence a person reads and a predicate the suite runs — and **an OPEN finding whose predicate is true fails the build**, naming it on the firing that makes it live rather than in the audit that finds the damage. Three open findings, each a real predicate over this tree, each mutation-checked by MAKING ITS CONDITION TRUE and watching both its own test and the headline test fail: `TENANCY-1` (W209's named residual — `sessionAppointmentType` matches on clinician id with no practice, sound only while the seeded rail holds one practice's sessions; the trigger reads the rail), `DOSSIER-1` (a gate-dossier test reading the live ledger without a year bound — W207's did, and W208 tripped it), `CENSUS-1` (a module shipping with no `// W<n>` header is invisible to W200's Y4 copy census; counted against a pinned floor of eleven, all Y1 infrastructure). **PRIV-3 stays in the register, closed, with its predicate still returning `true`** — the proof the mechanism works, since had this file existed at W103 then W166 could not have been committed green; a test runs that counterfactual rather than asserting it. A finding with no stated trigger is refused at RUNTIME by `declareFinding`, because `triggerStatement: ""` typechecks. **Known bound stated rather than hidden: this register is hand-kept.** Every other register here is checked against the tree in both directions and this one cannot be — there is no mechanical detector for "a comment that files something for later", and a pattern broad enough to catch the tree's dozen phrasings would have a false-positive rate that makes it worthless — so that half is a habit, not a control, and saying so beats a scan that would pass over a note nobody read. Verify: pnpm verify green, 2402 tests / 181 files |
| W211 | done | builder-A | 2026-08-12T19:00Z | bb421c9 | [P] Intervention-response model: what a response to an intervention IS, as recorded facts → verify: no response can be constructed without the intervention it answers; an absent response is `not_recorded` and never "no response" (W170's rule inherited, not restated). **`src/outcomes/response.ts` + 20 tests.** **A dangling attribution is unrepresentable:** `Response` is branded with one constructor and `respondTo` takes THE INTERVENTION VALUE, not its id (W114/W152's shape applied to causality) — there is no literal a caller can write, so a response naming an intervention nobody can produce has nowhere to come from. **W170 inherited, not restated:** `OutcomeVerdict` and `OUTCOME_VERDICT_COPY` are imported and the `not_recorded` wording is asserted IDENTICAL rather than similar, because two three-valued enums drift and the drift is invisible (W177). The consequence is a tested absence — no `hasResponded`, no `responseRate`, no `nonResponders`, each of which is a boolean or denominator built by reading silence as "no". **An opt-out is `stopped`, not silence:** somebody who told this product something is not somebody who told it nothing. **A response must come AFTER what it answers** — checked because the error it prevents is the flattering one, and refused with a reason rather than dropped (W205's conflation prevented, not repeated). Three registries failed correctly on the new module and were extended in the same commit: W200's CDSS copy surface, W201's ADM register (new `built_not_in_use` decision — nothing consumes it yet), W106/W180's record classes (`no_patient_identity`; neither input type has a field a patient identifier could occupy, so erasure stays COMPOSED). Founder gate: a test asserts no condition, symptom, severity or priority word appears anywhere in the intervention/response vocabulary — this must not become a triage. Non-vacuity, six breaks: drop the brand (**typecheck fails on an unused `@ts-expect-error`**, so the type-level test is load-bearing); return a negative instead of `not_recorded` (2); accept an event predating the intervention (2); restate W170's wording (1); export a `responseRate` (1); declare a kind with no way to end (1). Gate: 182 files, 2422 tests, build, audit:gate PASS. |
| W212 | done | builder-B | 2026-08-11T19:46Z | f20c6d5 | src/outcomes/response-graph.ts + 13 tests, and **W211'S ORDERING RULE HAD TO BE AMENDED — that is the unit.** W211 refused an event recorded at OR BEFORE the intervention, with a test pinning the same-instant case as refused; it was a deliberate, tested decision. Run over W12's sim it linked **NOTHING**: the spine stamps one timestamp per simulated week, so all **1,552** recorded responses in a six-week run share the instant of the offer they answer and **zero** are strictly after. Not merely inconvenient — it would have made `responseState` return `not_recorded` for 1,552 interventions the record plainly shows were answered, **the exact false claim W211's third value exists to prevent**. The mistake underneath: `at` was treated as a total order on recorded facts, when an append-only log's total order is its SEQUENCE and the clock is a coarser projection of it. So strictly-earlier events are still refused (that is what the flattering error looks like) and same-instant events are linked with the `Response` recording **which it rests on** — `ordering: "clock"` when the timestamps separate them, `"recorded_link"` when only the record does. Every edge carries the split, and the golden shows this graph resting **entirely on recorded links and zero on the clock** rather than presenting the same number as though the clock had confirmed it. Mutation-checked three ways; reverting W211's comparison collapses the graph to nothing, which is the regression guard for the amendment. Graph proper: edges carry a W196 `RecordedBasis` (denominator, period, source); **an absence is never an edge** so no denominator can acquire one; a graph over nothing is REFUSED rather than returned empty; three declared intervention kinds the sim never performs are listed as `unobserved` with a reason rather than as edges of weight zero (W179's distinction). Zero real interventions by construction — the only producer takes a `SimResult`, `provenance` is a union of one, `SHIPPED_RESPONSE_GRAPHS` pinned empty, and nothing has ever been sent (G1/G2/G3). The spine→response translation is declared data checked BOTH ways, with `SPINE_NOT_RESPONSES` giving a reason per excluded kind. **The unanswered branch is tested on a fixture, not the sim**: every one of the 1,552 offers resolves in six weeks, so the sim never produces an unanswered intervention and the branch would have gone untested behind a golden that looks thorough. **W116 caught a NUL byte** in a template-string separator — invisible at runtime, would have rendered the file "Binary files differ" in review. Four registers (W106/W180, W200, W201, W116) caught the new module; all declared. Verify: pnpm verify green, 2436 tests / 183 files |
| W213 | done | builder-A | 2026-08-12T20:00Z | 851d55d | [P] Matching explainability floor, built BEFORE any optimisation exists → verify: W86's total-explanation posture extended to matching — every decision renders its reason, no fallback branch, adding a reason without copy fails to typecheck. **`src/matching/explain.ts` + 17 tests.** **The ORDER is the unit:** W86 explained routing after routing existed and got away with it because routing was simple; an optimiser is the thing that becomes unexplainable while nobody is looking — v1 explains itself, v2 adds a tie-break, v3 adds a fallback for what the tie-break misses, and the fallback is where "the algorithm decided" ships. So the floor lands first and W214 must be built through it. **Three enforcements, three KINDS of check, because the rule fails three ways.** (a) Exhaustive copy is a TYPE property — `Record<MatchReason, string>` with a `@ts-expect-error` on a map missing one key. (b) "No fallback branch" is a property of what the source may CONTAIN, so the test greps this module's own file for `default:`, `_COPY[...] ??`, `_COPY[...] ||` and literal `"unknown"` — **a fallback returns correct-looking copy for every input a test would think to try, which is exactly why testing the output cannot find one.** (c) Total accounting is checked BOTH directions; `explainPlan` THROWS rather than rendering partially, because a dropped candidate is the failure that looks like success — every rendered decision is correct and the missing one is invisible. **The structural assertion W214's gate asks for:** a matcher never receives a `Patient`, only a `MatchCandidate` that has nowhere to put `chronicCare` or `activeRecall`, reachable only through a deliberately lossy projection. Two patients differing only in a clinical marker project to an EQUAL candidate, asserted by value — and `lastAttendedAt` is dropped too, since "longest since last visit" is how an ordering by need arrives without looking like one. **NAMED, NOT FIXED HERE — a live collision for W214:** `rankCandidates` in `src/engine/pool.ts` has sorted chronic-care patients FIRST since W5 and still feeds the live invitation pool. That is an ordering by a clinical attribute sitting in a reasonable-looking line of code that inspection has missed for eight quarters, which is the whole argument for checking this structurally. Changing who gets invited is W214's decision and its own unit, so this is recorded rather than left to be rediscovered (W145). W200's CDSS copy surface and W201's ADM register both failed correctly on the new module and were extended in the same commit (`NOT_A_DECISION` — it renders and refuses; the decision it constrains is W214's). Non-vacuity, five breaks: loosen the copy map to `Partial` (**typecheck fails**); add the `??` fallback (1); let the projection read `chronicCare` (2); drop the candidates-without-decisions direction (2); make `explainPlan` render partially instead of refusing (2). Gate: 184 files, 2453 tests, build, audit:gate PASS. |
| W214 | done | builder-B | 2026-08-11T20:47Z | 5c34c0b | src/matching/match.ts + 11 tests, built THROUGH W213's floor rather than beside it: fewest options first, earliest feasible slot, one decision per candidate from W213's closed reason vocabulary, whole plan passes `floorVerdict`. **ORDER-INDEPENDENCE IS ABOUT TIES** — two are real (candidates with equal option counts, slots at the same instant) and each is broken on an identifier, **arbitrarily and on purpose, because any tie-break that meant something would be a judgement about who deserves an appointment more** (W189's argument). Asserted by rotating BOTH inputs, not trusted from the sorts. Position cannot depend on a clinical attribute because W213's `MatchCandidate` has nowhere to put one; asserted by value — two patients differing only in `chronicCare`, `activeRecall` and `lastAttendedAt` project to an identical candidate. **NOT ENTERED IN W167's FOLD REGISTER, deliberately and against the letter of this row's gate:** that register counts FOLDS (`.reduce`, `.at(-1)`, last-index) and this matcher contains none, so an entry would fail its own both-directions check. The gate assumed a fold; the property is asserted directly instead, which is stronger. **AN OVERSTATED JUSTIFICATION WAS CAUGHT BY MUTATION CHECKING AND CORRECTED.** The module first claimed that recomputing option counts as slots fill would reintroduce order-dependence; the mutation left every test green, because the priority sort completes before the first assignment and a live recount would be deterministic anyway. The real reason to compute once is MEANING — W213's copy says an appointment "went to somebody who had fewer options", and recomputing silently changes that to options still LEFT, a different claim about a different person. Recorded rather than quietly fixed: an overstated justification is worse than none, because the next reader trusts it. Also stated: this greedy is **not the maximum matching** — augmenting paths would sometimes offer one more appointment, and are refused because "a chain of reassignments elsewhere" cannot be rendered as the one sentence W213's floor requires. **MATCH-1 RECORDED IN W210's REGISTER, and it is not a code smell.** `rankCandidates` has ordered the LIVE invitation pool by `chronicCare` and then by time since last visit — its own comment reads "older date = longer overdue = first" — while W201's published ADM notice says in its *never automated* list: **"No ordering of patients by need or by how unwell they are."** A published legal notice and the live ranker disagree. W213 saw the code; reading it against the notice is what makes it a contradiction. **FOUNDER: resolving it means changing who gets invited OR changing a published notice — the loop must not choose.** W216 prices it; the latent trigger fires the day this matcher becomes a live path. Verify: pnpm verify green, 2464 tests / 185 files |
| W215 | done | builder-A | 2026-08-12T21:00Z | fb95454 | [P] Counterfactual accounting: what would have happened without the intervention → verify: the holdout arm is the only comparator; no model-based counterfactual exists as a function; the figure refuses over a cohort below W72's floors. **`src/outcomes/counterfactual.ts` + 15 tests, and two live defects in the practice-facing headline.** The question is unanswerable — nobody observes the same practice both messaged and not messaged over the same weeks — so there are two honest postures and everything between them is a guess a practice will read as a measurement. **`Comparator` is a ONE-MEMBER UNION whose membership is asserted BY VALUE**, because a single-member union that quietly grows is the whole failure mode; no strategy, method or estimator parameter exists (`@ts-expect-error` pins both). **Per-patient is refused STRUCTURALLY, checked on the SIGNATURES not the names** — `estimateFor(patientId, …)` passes any name check, and the break confirms the signature check is what catches it. **THE GAP THIS UNIT FOUND: W72 ruled in Y2 that a cohort below a stated minimum reports its counts and withholds its CLAIM — and that floor was applied to register cohorts and NEVER to the practice-wide figure.** `countAttribution` withholds only on an EMPTY holdout arm, so three holdout patients produced a north-star number on the console dashboard. `MIN_ARM_PATIENTS` is imported rather than restated, and `src/console/results.ts` now derives its headline from the counterfactual instead of reading the raw arithmetic — which is exactly how that figure escaped the floor for two years while every register cohort honoured it. **Second defect, same family:** `app/console/dashboard/page.tsx` rendered `(attr.incrementalAttended ?? 0).toFixed(0)` — **a confident `0` for a practice with no comparison group**, a withheld figure printed as a measured zero, beside a sibling tile that correctly showed an em dash. Both now withhold and the page states why in words. **W42's own test was checking the symptom, not the cause:** it nulled the derived `incremental*` fields while leaving the holdout arm populated, so it verified that nulls propagate rather than that a practice without a comparison group gets no claim; the fixture now empties the arm. Three registries failed correctly and were extended in the same commit (W200 CDSS copy, W201 ADM as `NOT_A_DECISION`, W106/W180 `no_patient_identity`). Non-vacuity, five breaks: second comparator (1); drop the floor to pre-W215 behaviour (**8, across two files**); per-patient estimator (1, via the signature check); floor the difference at zero so the product cannot report its own harm (1); point `results.ts` back at the raw arithmetic (1). Gate: 186 files, 2481 tests, build, audit:gate PASS; e2e dashboard/results/roi pass. |
| W216 | available | — | — | — | Q17 dossier: the learned-ranking question, priced → verify: names the collision with W201's published ADM statement and with G7, states what a ruling would release and what it would cost, and takes no position; counts pinned by a test in W207's shape. |
| W217 | blocked | — | — | — | Learned ranking of patients → verify: n/a until ruled. **Blocked. FOUNDER DECISION — Q17 action 1, recorded in docs/GATE-DOSSIER-Q17.md: whether the product may order patients by anything a model learns, which would require changing the published ADM notice.** |
| W218 | available | — | — | — | [P] Response-graph privacy classification → verify: W106's record classes extended in the same commit; the graph holds no patient identity it does not need, and erasure reaches every class it does. |
| W219 | available | — | — | — | Intervention attribution v2 over the response graph → verify: cohort-level only; per-patient effect estimates are refused BY ABSENCE — no function exists, asserted on the module namespace. |
| W220 | available | — | — | — | [P] Q17 console: the response graph as a practice reads it → verify: e2e + axe; no clinical claim; the empty state distinguishes nothing happened from nothing recorded (W179). |
| W221 | available | — | — | — | Q17 hardening → verify: code-review + security-review skills; every new register checked both directions; W201's ADM register re-derived against anything Q17 added. |
| W222 | available | — | — | — | [P] Capacity model: sessions, slots and recorded utilisation → verify: over the synthetic practice; a session with no recorded history yields no forecast rather than a default. |
| W223 | available | — | — | — | Forecast as a stated interval, never a point — "open 6 slots Thursday → 4 to 6 fill" → verify: every forecast carries its basis and its uncertainty, and refuses below a floor of recorded weeks rather than emitting a confident number over thin data (W196's zero argument). |
| W224 | available | — | — | — | [P] Forecast honesty: every forecast is scored against what actually happened → verify: back-test over the sim; the score is recorded and rendered beside the forecast, so a forecaster that is usually wrong cannot present as one that is usually right. |
| W225 | available | — | — | — | Session-opening recommendation, addressed to the PRACTICE about its own diary → verify: no patient id can enter the recommendation type; asserted as an absence, not a filter. |
| W226 | available | — | — | — | [P] Recommendation copy and refusals → verify: compliance linter; W201's ADM register updated in the same commit, which is the rule W201 made mechanical rather than hopeful. |
| W227 | available | — | — | — | Seasonality and public holidays as declared data with a source → verify: nothing seasonal is inferred from the practice's own history; the calendar is data with provenance, W56's shape. |
| W228 | available | — | — | — | [P] Forecast drift monitor → verify: a forecaster that has stopped tracking reality is REPORTED, never silently recalibrated (W120's rule: report the disagreement, do not resolve it). |
| W229 | available | — | — | — | Capacity console → verify: e2e + axe; empty states distinguish no data from no capacity. |
| W230 | available | — | — | — | [P] Q18 privacy pass → verify: W106 classification; a forecast is practice-level and no figure can identify a patient, by type rather than by scrubbing. |
| W231 | available | — | — | — | Forecast → invitation-volume coupling, shipped explicitly OFF → verify: the coupling exists as a declared, disabled control; enabling it is a practice decision recorded with a reason, and the disabled state is pinned by its own test. |
| W232 | available | — | — | — | [P] Q18 dossier: what a forecast implies operationally, priced → verify: states what changes the day a practice acts on one. |
| W233 | available | — | — | — | Capacity attribution: did opening slots help? → verify: holdout-based only; refuses to answer without an arm rather than answering from the trend. |
| W234 | available | — | — | — | Q18 hardening → verify: review skills; registers both directions; zero criticals. |
| W235 | available | — | — | — | [P] FHIR R4 resource mapping as data → verify: round-trip over synthetic records; an unmapped field is NAMED in the output rather than dropped silently. |
| W236 | available | — | — | — | e-referral document profile → verify: W131's structured referral rendered to the profile; no clinical text is authored, generated or edited by this tree (G7's fourth property re-derived at the boundary). |
| W237 | available | — | — | — | [P] Interop conformance harness → verify: contract tests against recorded synthetic fixtures in W27/W28's shape; no live endpoint exists to call. |
| W238 | available | — | — | — | Terminology binding (SNOMED CT-AU, LOINC) as declared data → verify: every code carries provenance; an unbound code is refused rather than guessed, and the refusal names the code. |
| W239 | available | — | — | — | [P] Outbound disclosure ledger → verify: what left, to whom and when; W204's unresolved question — whether the log holds the FIGURES or only the fact of sending — is named in the module and left to the founder, with the model built so either answer is a one-line change. |
| W240 | blocked | — | — | — | Payer/insurer integration model → verify: n/a until ratified. **Blocked. FOUNDER GATE G10.** |
| W241 | blocked | — | — | — | Payer claim-status read → verify: n/a until ratified. **Blocked. FOUNDER GATE G10.** |
| W242 | available | — | — | — | [P] Interop credentials posture → verify: no credential in the tree; the loader enforces the gate rather than the values doing it (W56's shape); G1 named as the blocker for anything live. |
| W243 | available | — | — | — | Consent-to-disclose model → verify: a disclosure without a recorded patient consent is refused BY TYPE; silence is never consent (W135), and no timeout grants it (W134). |
| W244 | available | — | — | — | [P] Interop error semantics → verify: a failed or unacknowledged exchange is `unknown`, never "delivered" — W170's rule applied at the one boundary where the tree cannot see the other side. |
| W245 | available | — | — | — | Q19 dossier: G10 priced → verify: what G10 releases, what it costs, and what it does not cover; counts pinned by a test. |
| W246 | available | — | — | — | [P] Interop console → verify: e2e + axe; shows what was exchanged and, more importantly, what was not. |
| W247 | available | — | — | — | Q19 hardening → verify: security-review skill over every new boundary; the disclosure ledger's own W106 classification. |
| W248 | available | — | — | — | [P] Women's health vertical assembly, machinery only → verify: W157's model reused, not re-implemented; the vertical is refused with each missing member named; asserts zero clinical content present. |
| W249 | blocked | — | — | — | Women's health pathway content → verify: two-person sign-off recorded per W119. **Blocked. FOUNDER GATE G5.** |
| W250 | available | — | — | — | [P] Respiratory vertical assembly, machinery only → verify: same machinery; W158's completeness report states exactly which members are missing and who must act. |
| W251 | blocked | — | — | — | Respiratory pathway content → verify: two-person sign-off recorded per W119. **Blocked. FOUNDER GATE G5.** |
| W252 | available | — | — | — | [P] Vertical scaling: the registers at N verticals → verify: order-independence and a stated time budget over 20 synthetic verticals; the budget is asserted in the test body, W48's shape. |
| W253 | available | — | — | — | Platform API surface, read-only and practice-scoped → verify: every endpoint takes a practice as the QUERY (W123's rule); no endpoint can return cross-practice data, asserted the way Y4-1 should have been. |
| W254 | available | — | — | — | [P] API scope model → verify: scopes are declared data checked against the endpoint census in both directions; no production credential enters the tree. |
| W255 | available | — | — | — | API refusal semantics → verify: no patient data on any error path, asserted over every refusal branch rather than sampled. |
| W256 | available | — | — | — | [P] Five-year full-system audit (W51 method: the whole tree, not a diff) → verify: every sweep re-run from source rather than carried from AUDIT-Y4; independence of the reviewer stated plainly. |
| W257 | available | — | — | — | Five-year gate dossier: every decision still outstanding, priced → verify: counts derived from the ledger and pinned row-by-row by a test, W207's shape, so the document cannot go stale. |
| W258 | available | — | — | — | [P] The ADM register at five years → verify: W201's decision register re-derived against everything Y5 added, not assumed to have survived; the published notice regenerated from it. |
| W259 | available | — | — | — | The G7 boundary at five years → verify: W200's five rail properties re-derived; Q17's matching optimisation tested against them explicitly, since it is the first Y5 work that could have moved the line. |
| W260 | available | — | — | — | **YEAR 5 CLOSE.** Y6 horizon plan + expansion-rule renewal → verify: next-horizon plan written from the W256 audit and the W257 dossier; §6's expansion rule states what succeeds it now the five-year arc is spent; `plan-ledger` green over the whole ledger. |

| SUP-1 | blocked | — | — | — | Cohort-level specialist review (W89 hook 2) — FOUNDER GATE G5. Buildable only as a de-identified aggregate view whose output is register content; not buildable as anything patient-linked. See docs/SUPERVISION-HOOKS-W89.md |
| SUP-2 | blocked | — | — | — | Named-patient specialist consultation (W89 hook 3) — FOUNDER GATE G5 **plus** a company-direction decision: whether Meherr is willing to become a party to individual clinical care. Not a clinical-content question and should not be answered as one. See docs/SUPERVISION-HOOKS-W89.md |
| PRIV-1 | done → W105 @ 46f6e00 | — | — | — | **W102 finding 3.** `/api/interest/export` streams the whole community interest register as CSV (name, email, audience) behind `verifySession` alone — no practice scoping, no role check, and `src/interest/store.ts` carries no practice id, so any console user at any practice can download every signup. The register is a Meherr-run community program, not practice data, so the correct control is "Meherr staff only" — **and no such role exists**, which is why this is a product decision (add a staff role, or move the register out of the practice console) rather than a patch. Synthetic-only under G2 today; the exposure becomes real with the first stored signup |
| PRIV-2 | done → W106 @ 57638ed | — | — | — | **W102 finding 4.** APP 12 access and APP 11 retention do not know about Y2 record classes. `exportPatientData` returns invitations/appointments/audit/outcomes; `RetentionConfig` prunes the last three. Neither names register membership (W55, `0004_registers.sql`), referral or barrier records (W92–W95), or clinician capability records (W79, `0005_capability.sql`) — the last including W80's DERIVED case mix, information generated about a person rather than collected from them, which is exactly what an access request exists to surface. Harmless only because those stores are in-memory: the migrations already exist. **Trigger: the first store that persists any of these classes — export, delete and retention must grow in the same unit** |
| PRIV-3 | done | builder-A | 2026-08-12T18:00Z | dd28e74 | — | — | — | **CLOSED BY W209 (2026-08-12), two years and one disclosure after it was recorded.** All three named sites are addressed: `src/complaints/workflow.ts` in W206, `src/audit/store.ts` in W209, and `src/console/store.ts`'s literal went with W166's practice minting. The wider claim — that several modules assumed one practice — is now checked mechanically rather than remembered: `src/tenancy/store-reads.ts` declares every exported store read in both directions, so the next unscoped one is a build failure. **What this row cost by staying open: W181's ops queue, Y4-1's complaints leak, W209's usefulness-audit write and CPD trail — four live cross-tenant defects from one recorded-but-unclosed modelling gap.** Its remaining residual is named in the registry (`sessionAppointmentType`) rather than carried here, because a residual in a row is a thing to remember and a residual in a registry is a thing the tree checks. **W103 finding B2.** The console cannot represent two practices: `ConsoleState.practice` is a single nullable practice and the id is the hardcoded literal `"prac-console"` in `src/console/store.ts`, `src/complaints/workflow.ts` and `src/audit/store.ts`. So `openComplaintCount()` filters by no practice and `tallyOutcomes(records)` takes none — correct ONLY because the store holds one practice, which also means every console-side scoping bug is structurally unobservable. W97's multisite work makes the multi-practice day foreseeable, and on it all three sites become defects at once. **Trigger: the first store change that lets `ConsoleState` hold more than one practice must thread practice identity from the session in the same unit** **STATUS CORRECTED 2026-08-10 (W154 firing): the W105/W106 window closed WITHOUT this landing** — W105 was the staff gate and W106 the record-class registry; neither touched the console's practice identity, and all three `"prac-console"` literals are still there. The row read as scheduled-and-handled, which is the quietly-wrong record this tree keeps catching. NOT built now on purpose: W103's own recommendation is that the identity threading lands IN THE SAME UNIT as the store change that lets `ConsoleState` hold more than one practice, and doing it ahead of that would be building against a shape nobody has chosen yet. Trigger unchanged. **W206 UPDATE (Y4 audit): the write path is fixed and the row stays open.** `intakeComplaint` no longer stamps `"prac-console"` — it takes the practice — and both complaints console reads are scoped, closing the cross-tenant leak that literal caused (see docs/AUDIT-Y4.md Y4-1). What remains is the rest of the row's claim: `src/audit/store.ts` was not swept and may carry the same one-practice assumption. **The severity of this row changed without anybody editing it**: it was recorded as a modelling gap in Y2 and became a patient-data disclosure the day W166 made two practices real. |
