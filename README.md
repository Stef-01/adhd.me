# Meherr

GP demand-matching platform: continuity-yield outbound wedge → credentialed extended-scope GP
network. Venture brief + research live in Stefan-Brain (`wiki/entrepreneurship/startups/careyield.md`).

- `docs/FIVE-YEAR-PLAN.md` — five-year vibecoded build plan (unit definitions §5)
- `BUILD-STATE.md` — unit ledger + claim protocol (the loop's lock)
- Built continuously by `careyield-build-loop-a`/`-b` cloud Routines (≈ every 30 min, one
  verified week-unit per firing, parallel-safe via the claim ledger)
- Synthetic data only; founder gates G1–G7 per plan §4

Verify gate: `pnpm typecheck && pnpm test && pnpm build`.
